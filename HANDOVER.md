# 인수인계 — LG Aimers KBO 제구 예측 (2026-08-31 기준)

마감 **2026-09-02 09:59**. 이 문서는 "지금 당장 뭘 하면 되는가" 만 담는다.
왜 그런지, 무엇이 왜 죽었는지는 전부 `claude.md` 에 있다.

---

## 1. 현재 상태

```
리더보드 최고점   1,150.2601   (v10wph)
100인 컷          1,163.8707   (2026-08-29 실측, 하루 약 +5 상승 중)
격차              +13.61       (마감 시점 외삽 ~+27)
```

**채점은 최고 점수 기준이다.** 나쁜 제출이 좋은 제출을 밀어내지 않으므로
마감까지 계속 시도해도 잃을 것이 없다. '안전하게 고정' 은 이 대회에서 의미가 없다.

### 최고점 구성 (v10wph)

```
v5      Optuna + fold10 + seed3 + 조건부통계 + 재중심화       990.95
v9m     + 월/요일 제거                                        996.99
v10w    + 당해 시즌 성적 복원 (wseason5)                    1,058.72
v10wb   + 타자측 복원 (wsbat)                               1,088.83
v10wq   + 보조 타겟 aux_reverse                             1,103.32
v10wz   + 타겟 5분류 (multicls)                             1,116.17
v10wzc  + 범주형 조합 2개 (cnt12 + hand4)                   1,121.89
v10wt13 + 팀13 x 2023-05 체제전환 지시자                    1,142.47
v10wph  + phteam (pitcher_hand x pitcher_team, 26칸)        1,150.26   <- 현행
```

파일: `experiments/v10w/aimers_v10wph.ipynb`
제출 zip: `kout_ph/submit_v10wph.zip`

---

## 2. 지금 당장 할 일

### 제출 대기 중인 zip (검증 완료)

| zip | base | 기대 | 비고 |
|---|---|---|---|
| `kout_phm/submit_v10wphm.zip` | v10wph | +2 | + 4분류. `mc4` 단독이 +1.88 이었다 |

### 돌고 있는 커널

| 커널 | 내용 | 기대 |
|---|---|---|
| `aimers-v10wtw` | v10wph + `tmw5` (pitcher_team x w_success 5분위, 60칸) | **+5~20** |

`tmw5` 가 현재 가장 큰 카드다. `tools/eda54.py` 에서 강도 51.8 / 2023<->2024 상관 0.76
으로 나왔고, 이는 `phteam`(곱 15 -> 실측 +7.79)의 **2.6배**다.
⚠️ 다만 `w5q` 는 수치형 파생이라 트리가 임계값으로 싸게 쪼갠다 — 회수율이 낮을 수 있다.

### 커널 끝난 뒤 절차 (항상 이 순서)

```bash
# 1) 회수
mkdir -p kout_tw && PYTHONUTF8=1 kaggle kernels output homekeggle/aimers-v10wtw -p kout_tw

# 2) 검증 3종 — 하나라도 실패하면 올리지 말 것
PYTHONUTF8=1 python tools/audit_independence.py kout_tw/submit_v10wtw.zip     # 행 독립 (규정)
PYTHONUTF8=1 python tools/verify_ws_live.py     kout_tw/submit_v10wtw.zip 600 ws_   # 투수측 상수 생존
PYTHONUTF8=1 python tools/verify_ws_live.py     kout_tw/submit_v10wtw.zip 600 wb_   # 타자측 상수 생존

# 3) 데이콘에 zip 업로드
```

⚠️ 검증 출력은 `tail` 로 자르지 말고 판정 문자열로 `grep` 할 것 (판정 줄을 놓친 적 있다).
⚠️ `ws_` 검증이 `KeyError: w_n` 으로 끝나면 그건 **통과**다 — 상수가 없으면 script.py 가
   하드 실패한다는 뜻이고, 도구가 그렇게 읽도록 고쳐져 있다.

---

## 3. 무엇이 통했고 무엇이 안 통했나 (요약)

### 실측 이득 (리더보드)

```
wseason5   +61.72   당해 시즌 성적 복원        <- 역대 최대
wsbat      +30.11   타자측 복원
t13        +20.58   팀13 x 2023-05 체제전환
multicls   +12.85   타겟 5분류
auxrevns   +13.50   보조 타겟 reverse
phteam      +7.79   pitcher_hand x pitcher_team
cnt12h      +5.72   범주형 조합 2개
```

**공통점: 전부 '다른 행/다른 테이블에서 정보를 끌어오거나, 트리가 만들기 비싼 조합을
직접 준 것' 이다.** 한 행 안에서 계산되는 파생피처는 세 번 독립적으로 0 이었다.

### 닫힌 축 (다시 열지 말 것)

용량(반복수·깊이·앙상블) · 보정/isotonic/재스케일링 · 최근성 가중 · 전이손실 공격 ·
트리 계열 다양화 · NN · 증류 · 구종 축 · 캘린더 · 세그먼트 재중심화 · 이적 · 구장 ·
표적 분할 · `cond_*` 섭동 · 저카디널리티 조합(105쌍 전수).

각각 왜 닫혔는지는 `claude.md` 에 실측과 함께 있다.

### 하이퍼파라미터 튜닝은 기대 0

반복수는 5분류 배포본에서 직접 쟀다 — 트리 700~1000 의 기여가 **0.17점**이다.
파라미터 변경의 전달률은 0.25 이므로 +13.61 을 얻으려면 홀드아웃 +54 가 필요한데,
Optuna 전체 탐색이 v5 시절에 찾은 것이 +39 였고 그중 대부분은 GPU 기본값 버그 복구였다.

---

## 4. 판정 규칙 (이 프로젝트에서 비싸게 배운 것)

1. **2024 홀드아웃으로 후보를 거르지 말 것.** 거짓 양성 4번, 거짓 음성 3번.
   `wsbat` 은 홀드아웃 +3.0 인데 리더보드 +30.11, `cnt12h` 는 **-6** 인데 **+5.72** 였다.
2. **스크리너(2023)도 +40 아래면 부호를 못 맞힌다.** 큰 발견 확인용으로만 쓴다.
3. **판정은 리더보드로만 한다.** 제출은 남아돌고 최고점 채점이다.
4. **변경은 한 번에 하나씩.** 비가법성이 5번 확인됐다
   (`bsx`+`cnt12` 가 각각 +16.2/+3.4 인데 합치면 **-1.2**).
5. **집계 통계로 축을 닫지 말 것.** `eda48` 에서 10개 구장을 평균 내 "천장 2점" 으로
   닫았는데, 그 안에 있던 팀 13 하나가 **+20.58** 이었다. 개별 시계열을 눈으로 볼 것.
6. **상수 보정이라고 전달률 1.0 이 아니다.** 세그먼트 상수는 네 해 부호 일관 +
   세 전이 전부 양수였는데 리더보드 **-6.48** 이었다.

### 제출 전에 점수를 계산하는 도구 (검증됨)

```
재중심화 어긋남 = 100000 x Δ평균² / r(1-r)        r ~= 0.475
수축           = (1.92λ - λ²)/0.9216,  λ* ~= 0.96,  λ = std비
표적 분할 비용  ~= -23 (5분류 기준, 출력당 약 -6)
```

`n700` 이 이 식으로 -1.3 을 예측하고 실측 -1.47 이었다.
평균이나 퍼짐을 건드리는 변경은 **제출 전에 부호를 알 수 있다.**

---

## 5. 새 후보를 찾는 방법 (마지막 이틀에 유일하게 통한 것)

`t13`(+20.58)과 `phteam`(+7.79)은 같은 절차에서 나왔다:

```
1. 저카디널리티 축들의 2차 상호작용 잔차 강도를 전수로 잰다
      r_ab = mean(y|a,b) - mean(y|a) - mean(y|b) + mean(y)
      강도 = sum 비중 x r²   ->   401,000 x 강도 = 완벽히 잡았을 때의 점수
2. **2023 잔차 vs 2024 잔차 상관**으로 거른다 (2025 는 후반 체제다)
3. 강도 x 상관 이 phteam(15) 급이면 후보. hand4(50)가 상한 교정점이다
4. 범주형 조합으로 직접 준다
```

도구: `tools/eda52.py` (원본 범주형 105쌍), `tools/eda54.py` (w5_* 교차).

⚠️ **시즌별 강도가 아니라 상관이 판정 기준이다.** `pitcher_team x batter_team` 은
2023 120.6 / 2024 186.4 로 커 보이지만 상관이 **-0.14** 라 죽었다.

### 아직 스캔 안 한 대상

- 3차 조합 (`t13` 효과의 상황별 분산은 재봤고 천장 최대 3.9점이라 닫혔다)
- 트랙맨 파생 x 기존 축 (트랙맨은 1년 묵으면 77% 소실이라 기대는 낮다)

---

## 6. 환경·운영 함정

```
PYTHONUTF8=1        캐글 CLI 는 항상 이걸 붙일 것 (cp949 크래시)
kaggle CLI 취소 불가  웹 UI 에서 사람이 눌러야 한다
돌는 커널에 재푸시 금지  좀비가 남아 슬롯이 샌다. 새 태그로 만들 것
enable_gpu != task_type  둘 다 확인. 한쪽만 고쳐서 GPU 233분을 태웠다
디스크         커널 회수물이 870MB/판. 100% 차면 zip 이 손상된 것처럼 보인다
               kout_*/model 과 validation_sandbox 는 zip 의 사본이라 지워도 된다
pkill/pgrep 없음  프로세스 관리는 PowerShell 로
```

### 빌더 사용법

`tools/mk_*.py` 는 **노트북 하나를 받아 한 가지만 바꾼다.** 환경변수로 base 를 지정한다:

```bash
TW_BASE=experiments/v10w/aimers_v10wph.ipynb PYTHONUTF8=1 python tools/mk_tmw5.py
```

전부 **학습 시작 전에 터지는 가드**를 삽입한다 (4-12: 학습 뒤 셀에서 죽으면 GPU 2시간을
통째로 날린다). 가드를 넣을 때는 **앵커가 실행 순서상 정의 뒤인지 확인할 것** —
그걸 안 봐서 세 번 죽었다 (각 3분).

---

## 7. 마감까지 권고

```
9/1   tmw5 결과 확인 -> 양수면 그 위에 phm 스택
      eda54 의 2·3순위(w5q x wnq 곱 27, w5q x wb5q 곱 14) 를 순서대로
9/2   09:59 마감. 마지막 제출은 여유를 두고
```

**격차 +13.61 이고 `tmw5` 하나로 닿을 수도 있다.** 못 닿아도 8/30 아침 +41.98 에서
여기까지 왔다는 것은 기록해둘 만하다.
