# CLAUDE.md — KBO 제구 성공 예측 프로젝트

이 문서는 Claude Code가 이 프로젝트에서 작업할 때 참고할 컨텍스트입니다.
데이콘 해커톤: 투구별 제구 성공 확률(`control_success`) 예측, 평가지표는 Brier Skill Score.

```
Score = max(0, 100000 × (1 - Brier / (r(1-r))))   # r = 평가 데이터 실제 평균 성공률
```

**현재 상태**: 리더보드 최고점 **1121.8905** (`v10wzc` = v10wz + **범주형 조합 2개**(cnt12 12칸 카운트 + hand4), GPU, 2026-08-27 확정).
1,089.81(8/24) -> 1,093.95 -> 1,103.31 -> 1,103.32 -> **1,116.17**. 이틀 만에 +26.4.
932.96 → 959.43(Optuna+fold10+seed3) → 987.39(조건부통계+재중심화) → 990.95(트랙맨 매핑 v2)
→ 996.998(월/요일 제거) → **1002.171(CPU 학습)**.
기각: **ptc6(타겟 x 구종) 1,033.84 = -82.3** — 잡음 축으로 쪼개면 죽는다.
기각: **v10wpn(prev-game 분모 역산) 1,113.17 = -3.00** — 17% 약분 오차가 신호와 상관.
기각: v6(릴리스 동역학) 988.47, v7(반복수 700/500) 986.21/967.89, v8(네 가지 동시) 968.60.
목표: 최소 1100점 (100인 컷은 현재 **1,119.20**). **마감 9/2.**

> 2026-08-22 **v9m 996.9977 (+6.04) → local_cpu 1002.1710 (+5.17).** 이틀 만에 두 번 전진.
> 다만 CPU 는 홀드아웃 +26 을 근거로 **+33 을 기대했는데 실측 +5.17** 이었다.
> ⛔ 이 실패로 '증분 전달률 0.3' 을 확정했는데, **2026-08-23 에 그 일반화가 깨졌다.**
>
> 2026-08-23 ⭐⭐⭐ **`v10w_fixed` 1058.7184 = v9m +61.72.** 역대 최대 도약이다.
> 스크리너 +72.2 -> 리더보드 +61.72, **전달률 0.855.** 3장 '전달률은 하나가 아니다' 참고.
> **100인 컷(1,119.20)까지 60.5 남았고, 새 정보 부류로 환산하면 홀드아웃 +71 이다.**

> 2026-08-21 **v8 절개 실험으로 -22.35 를 네 항목에 완전히 분해했다.**
> 넷 중 하나(월/요일 제거 +4.52)만 양수이고 나머지 셋은 전부 음수다.
> 그 하나를 30모델에 반영한 것이 위 v9m 이다 (예측 995.5, 실측 **996.998**).
> 3장 '절개 실험' 절에 다섯 건의 리더보드 실측이 있다. 새 시도 전에 그것부터 읽을 것.
>
> 2026-08-21 저녁 **CPU 와 GPU 가 서로 다른 알고리즘을 돌리고 있었다는 것을 발견했다.**
> `border_count`(254/128)와 `bootstrap_type`(MVS/Bayesian) 기본값이 달라 홀드아웃 26점
> 차이가 났고, 절개로 -12 / -10 으로 완전히 분해됐다. **MVS 는 CPU 전용**이라 GPU 로는
> 절반만 회수된다. 3장 'CPU 와 GPU 는 같은 CatBoost 가 아니다' 를 읽을 것.

---

## 1. 데이터 구조

```
data/
  train.csv                 # 1,475,092행 x 49컬럼. control_success 포함
  test.csv                  # 평가용 (배포본엔 5행 샘플만, 실제 평가 시 서버가 교체)
  sample_submission.csv     # row_id, control_success 2컬럼
  data/data_description.md  # 주최측에서 제공한 데이터 설명서
  trackman_history.csv      # 2019~2024년 투구 로그, 1,793,078행 x 30컬럼
                            # train/test와 1:1 결합 안 됨. pitcher_trackman_id 기반 매핑 필요
                            # 2025년(=test 시즌) 데이터는 없음 — 중요, 아래 4-4 참고
                            # ⚠️ 1군 전용이 아니다. 2024 기준 38%가 2군(퓨처스, MIN_* 팀코드)이고
                            #    올스타(KBO_*)/기타(ACE_*)도 섞여 있다. 2장 '트랙맨 매핑' 참고


                            # 이하 항목은 사용자가 만든 것으로 주최측 공식 제공은 아님.
pitcher_id_mapping.csv      # pitcher_id <-> pitcher_trackman_id  ⚠️ 약 91% 틀림 — 2장 참고
pitcher_id_mapping_v2.csv   # 재구축본(1,686행). 2026-08-19 생성, 이걸 쓸 것
batter_id_mapping.csv       # batter_id <-> batter_trackman_id  (타자측 트랙맨 피처용)
```

### train.csv 핵심 컬럼

- 식별자/상황: `row_id, season, game_month, game_dayofweek, inning, top_bottom, game_type`
- 카운트/점수: `balls_before, strikes_before, outs_before, score_diff_pitcher_team` 등
- 주자: `runner_on_1b/2b/3b, base_state(___/1__/_2_/__3/12_/1_3/_23/123), li`
- 선수: `pitcher_id, batter_id, pitcher_hand, batter_hand, pitcher_team_id, batter_team_id`
- **`asof_*` 컬럼**: 해당 투구 **직전까지**의 누적 통계 (leak-free, 운영 측 공식 제공).
  `asof_pitcher_success_rate`, `asof_pitcher_n`, `asof_pitcher_prev{1,3,5}_game_success_rate`,
  `asof_batter_success_rate`, `asof_pitcher_{fastball,breaking,offspeed}_rate` 등
- 타겟: `control_success` (0/1). 가운데로 몰림 / 크게 벗어남 / 포수 요구 반대방향 = 실패

### ⚠️ 데이터의 두 가지 핵심 성질 (2026-08-18 분석)

**1) train은 2019~2024만, test는 2025.** 시즌이 완전히 분리돼 있다 →
train으로 만든 통계를 test에 쓰는 건 전부 "과거 정보"라 규정상 안전하다.
동시에 **평가는 항상 "미래 시즌 예측"**이므로, 피처 선별은 반드시 **2024 시즌 홀드아웃**
(train=2019~2023 → valid=2024)으로 할 것. 랜덤 shuffle CV는 이 난이도를 반영하지 못한다.

**2) 성공률이 매 시즌 단조 하락한다 (드리프트).**

| 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---|---|---|---|---|
| .5647 | .5327 | .5328 | .5289 | .5000 | .4861 |

- **선수 구성 변화가 아니라 리그 전체 현상**: 양 시즌 500구+ 던진 동일 투수도 2023→24에 평균
  -0.0107(60%가 하락). Oaxaca 분해로 2022→23은 내부효과 102%, 2023→24는 내부 52%/구성 48%.
- 등판량 티어별로는 **핵심 1군(2000구+)이 -0.0051로 덜 떨어지고** 나머지가 -0.010~-0.013.
- **운영측 `asof_pitcher_success_rate`는 커리어 누적이라 이 하락을 못 따라가서 2024에 +0.0253
  과대평가** 중이다. 가장 중요한 피처가 최근 시즌일수록 위로 편향돼 있다.
- 추세 외삽은 신뢰할 만함: 2019~23만으로 2024를 예측하면 오차 0.0016(최근3년 선형).
  같은 방식으로 **2025 예상 베이스레이트는 0.462~0.475.**
- 단, 전체 피처를 쓰면 모델이 스스로 상당 부분 따라간다(2024 평균예측 0.4940 vs 실제 0.4861).
  그래서 재중심화 이득은 크지 않다(+18점). 자세한 건 4-11 참고.

**3) `asof_pitcher_n`은 경기내가 아니라 커리어 누적** (중앙값 1,776 / 최대 15,449).
이걸 경기내 투구수로 오해해서 만든 피처들이 죽어 있다 — 아래 2장 참고.
train/test 에 경기 ID도 날짜도 없고 test 내부 행 순서 사용은 금지라 **경기내 피로도는
복원 불가능**하다. ⚠️ 단 **트랙맨에는 `game_date` 와 `trackman_game_id` 가 있다** —
불가능한 것은 '경기 안의' 피로도지 '등판 사이의 휴식' 이 아니다 (v8r 에서 검증 중).

### 규정상 금지 사항 (중요)

- test.csv의 다른 행을 이용한 통계(누적/빈도/target encoding/rolling) 금지
- 현재 투구 이후 확정 정보, 실제 위치/판정/구종 사용 금지
- trackman_history.csv에 2025년 데이터 없음 → test 시점 투수의 최신 트랙맨 값은 **2024년 값으로 근사**할 수밖에 없음

---

## 2. 전처리 파이프라인 (`step1` ~ `step21`)

**설계 원칙**: 학습 노트북과 제출용 `script.py`가 **완전히 동일한 소스**를 써야 한다.
`STEPS_SRC`라는 문자열 하나에 `step1~14`를 정의하고, 노트북은 `exec(STEPS_SRC)`로 로드,
`script.py`는 이 문자열을 그대로 텍스트 삽입한다. (이 원칙이 깨져서 대형 버그가 여러 번
발생했음 — 4장 참고.)

| 함수 | 내용 |
|---|---|
| `step1_basic_features` | 주말 낮경기, 한여름 폭염경기 플래그 |
| `step2_pitcher_role_features` | 순수선발/롱릴리프/구원 플래그 |
| `step3_matchup_features` | 투타 손 일치 여부 |
| `step4_refined_count_features` | 볼카운트 유불리(`count_advantage`: Pitcher/Batter/Neutral/**None**), 풀카운트/초구 플래그 |
| `step5_pitches_per_inning` | 이닝당 투구수 |
| `step6_combined_runner_features` | RISP, 승계주자, 도루위협 등 |
| `step7_bayesian_smoothing` | `smoothed_pitcher_success_rate = (n*curr + C*prior_mean)/(n+C)`, C=50 |
| `step8_batter_toughness_features` | 타자 까다로움 지수 |
| `step9_garbage_time_features` | 가비지타임 플래그/지수 |
| `step10_recent_form_momentum` | 최근 1/3/5경기 폼 변화량 |
| `step11_veteran_and_pressure_features` | 루키/베테랑 + 압박지수(li) 결합 |
| `step12_first_pitch_tendency` | 초구 직구 스트라이크 확률 지수 |
| `step13_sac_fly_threat` | 희생플라이 위협 상황 |
| `step14_convert_to_category` | 범주형 컬럼 dtype 변환 (CatBoost/LightGBM용) |
| `step15_prep_trackman_data` | 트랙맨 원본 + `pitcher_id_mapping` 병합, `count_advantage` 부여 |
| `step16_calc_expected_difficulty` | 투수가 상황별로 상대해온 구질 비율 × 릴리스포인트 변동성 = `expected_control_difficulty` |
| `step17_calc_pitch_speed` | 투수의 과거 평균 패스트볼 구속(`past_fb_speed_mean`) + **체감구속**(`past_fb_zone_speed_mean`, `zone_speed` 기반) |
| `step18_calc_pitch_consistency_by_group` | 구종군별 릴리스 지표 표준편차 (18개 `past_*_std` 피처) |
| **조건부 투수통계** (`attach_cond_features`) | `cond_p/pc/ph/phc` — pitcher, ×카운트, ×타자좌우, ×둘다. **시즌 디트렌드 + 0으로 shrink** |

**현재 제출 파이프라인은 `step1~18` + 조건부 투수통계를 쓴다. `zone_speed` 기반 체감구속
피처는 포함되지 않는다 — 아래 참고.**

### 죽은 피처 (제거됨 — `DEAD_FEATURES`)

`asof_pitcher_n`이 커리어 누적인데 경기내 투구수로 가정해서 만들어져 의도대로 동작하지 않는다:

| 피처 | 실제 동작 |
|---|---|
| `is_long_relief` | 전체의 **86%**(이닝>1 중 97%) → 사실상 `inning>1`과 동일 |
| `is_short_relief` | 2.4% |
| `is_strict_inherited_runner` | **0.05%** → 상수나 다름없음 |
| `pitches_per_inning` | 커리어투구수/이닝 (중앙값 383) → 의미 붕괴 |

제거 효과는 +4점(±2)으로 미미하지만 코드 정합성 차원에서 뺐다.
`is_rookie`/`is_veteran`(684/3725 기준)은 **커리어 기준이 맞으므로 유효**하다.

### 조건부 투수통계 설계 (2026-08-18 추가)

원시 성공률을 그대로 집계하면 과거 시즌의 높은 수준이 섞여 들어오므로,
**"그 시즌 리그평균 대비 편차"로 디트렌드**한 뒤 `sum/(count+C)`로 0(리그평균)에 shrink한다.
표본이 적은 조합일수록 자동으로 0에 수렴해 콜드스타트가 자연히 처리된다.

- 학습: 각 행을 **그 시즌보다 과거** 데이터로만 인코딩 (leak-free, 2019 행은 NaN)
- 추론: 전 시즌으로 만든 룩업 테이블(`model/cond_*.csv`)을 merge
- C값: pitcher 200 / ×카운트 100 / ×좌우 100 / ×셋 50
- **`count_advantage`의 `'None'`이 키에 들어가므로 4-3 라운드트립 버그 주의** (검증 통과 확인)
- ⚠️ 2024 투수의 **19.9%가 직전 5년에 없던 신규 투수** — 2025도 비슷할 것이므로 NaN 폴백 필수

검증(2024 홀드아웃, 3-seed 짝지어): 기준선 대비 **+20(원본)/+27(재중심화)**, 3 seed 전부 우세.
부수 효과로 seed 분산이 ±18 → ±6으로 감소.

### 재중심화 (2026-08-18 추가)

시즌 드리프트로 모델 평균예측이 위로 뜨는 걸 로짓 공간 상수 시프트로 보정한다.
**규정 주의**: 추론 시점에 "test 예측 평균 = 목표값"이 되도록 푸는 방식은 평가 데이터 전체를
보고 만든 사후 보정값에 해당할 소지가 있다. 그래서 **학습 시점에 홀드아웃
(~2023 학습 → 2024 예측 → 정답 아는 2024 평균에 맞춤)으로 오프셋을 측정해
`train_constants.json`의 `recenter_offset`에 상수로 박고**, 추론은 그 상수를 더하기만 한다
(Cell 6c). 구조가 "Y까지 학습 → Y+1 예측"으로 최종 모델과 동일하다. 효과 +18점.

> ✅ **원인 분리 완료 (2026-08-18): `zone_speed` 자체가 원인이었다, 타자 피처가 아니었다.**
> `past_fb_zone_speed_mean`을 타자 피처 없이 `step17`에만(=`rel_speed`와 완전히 동일한
> shift(1).expanding() 패턴) 단독으로 추가해 Kaggle GPU로 재학습·제출한 결과 **929.6060점**
> (932.96 대비 **-3.35점**). 이전에 타자 피처 6개 + zone_speed를 함께 넣었을 때가 930.00
> (-2.96점)이었던 것과 거의 같은 낙폭 — 타자 피처를 뺐는데도 낙폭이 줄지 않았으므로,
> **원흉은 zone_speed였다.** `step17`은 원상복구했고(`past_fb_speed_mean`만 유지),
> **zone_speed는 이 방식(월 단위 집계)으로는 다시 시도하지 말 것.**
> (추측: `rel_speed`와 상관이 높아 중복 신호이거나, 월 단위로 뭉개진 집계라 이미 노이즈가 커서
> 피처를 하나 더 얹을수록 과적합 위험만 커졌을 가능성 — `asof_*` 재설계(최근 N구 이동평균)로
> 넘어가면 이 문제 자체가 달라질 수 있음.)

> ⚠️ **`step19~21`(타자측 트랙맨: `step19_prep_batter_trackman`,
> `step20_calc_batter_faced_profile`, `step21_calc_batter_zone_speed_faced`)은 여전히 폐기 상태.**
> 코드 자체는 실험 기록으로 어딘가 남아있을 수 있지만, 현재 학습/제출 파이프라인에는 포함되지 않는다.
> 다시 시도하려면 실패 원인(타자측 표본 희소 추정)부터 재확인할 것 — zone_speed와는 무관하게
> 독립적으로 재검증 필요.

**모든 `step16~18`은 `shift(1).expanding().mean()` 패턴으로 leak-free 누적 계산.**

### 트랙맨 병합 방식 — `TRACKMAN_MODE` 설정

- **`asof`(채택, 933점)**: `time_idx = season*100+game_month` 기준 `pd.merge_asof(direction='backward')`.
  2025 test 행은 해당 투수의 **가장 최근(2024) 값**을 받는다. 학습/추론 규칙이 동일해서 원칙적으로 타당.
- **`exact`(897점)**: `(season, game_month)` 완전일치 + `fillna(0)`. 900점 원본 버전과 동일 동작.
  트랙맨에 2025가 없으므로 **test에서는 전부 0**이 된다.
- `asof`가 `exact`보다 +36점. ~~트랙맨에 신호가 있다는 증거~~ → **2026-08-19 정정**:
  `exact`는 test에서 트랙맨 20개 피처가 전부 `fillna(0)`이 되는데 모델은 실제값으로 학습된 상태다.
  즉 +36은 **학습/추론 분포 불일치를 없앤 효과**이지 트랙맨의 기여가 아니다.
  트랙맨 자체 기여는 매핑을 고쳐도 +9(±2) 수준이다 (바로 아래).

### 트랙맨 매핑 재구축 (2026-08-19) — 기존 매핑이 약 91% 틀려 있었다

**증상**: 트랙맨 21개 피처를 통째로 빼도 2024 홀드아웃 점수가 안 변했다
(3-seed 짝지어 +1±15). 신호가 없다는 뜻이다.

**원인 두 겹**

1. **`pitcher_id_mapping.csv`가 대부분 틀렸다.** 총 965행뿐이고(2022~24는 시즌당 ~100명,
   투구 커버리지 **28%**), `distance` 컬럼(0.015~0.029)이 말해주듯 **구종비율 하나로만**
   매칭돼 있었다. 순환되지 않는 지표로 재보면 무너진다:
   - 시즌 간 일관성 **1.9%** (같은 trackman_id가 시즌마다 다른 pitcher_id로 감)
   - train 투구량 증분 vs 트랙맨 투구수 상관 **0.20** (무작위 셔플은 0.02)
2. **트랙맨의 38%가 2군 경기**인데 구분 없이 한 투수 평균으로 뭉개지고 있었다
   (906명 중 430명이 1군·2군 혼재).

**재구축 방법 (팀 → 투수 2단계)**

- 팀: `(월 × 요일 × 공수)` 63차원 투구량 프로파일로 헝가리안 매칭.
  **10개 팀이 6시즌 내내 동일 프랜차이즈로 대응** → 사실상 정답.
  `12:두산 13:LG 14:키움 15:롯데 16:KIA 17:한화 18:삼성 19:NC 20:KT 21:SSG(SK)`
  (⚠️ 월 단위 9차원으로는 실패한다 — 팀별 월간 분포가 거의 동일해서 비용이 평평해지고
  헝가리안이 무작위 배정을 한다. 일치율 10%였다.)
- 투수: 팀-시즌 안에서 등판 프로파일 + 이닝 분포 + 구종배합 + 총투구량, 손은 하드제약.
  ⚠️ **손 코딩이 다르다**: train은 `1=Left, 2=Right` 정수, 트랙맨은 `'Left'/'Right'` 문자열.
- 마지막에 시즌 간 다수결로 교정 (1,686쌍 중 44쌍).

| | 시즌간 일관성 | 2024 커버리지 | 기존과 일치 |
|---|---|---|---|
| 기존 `pitcher_id_mapping.csv` | 1.9% | 28.2% | — |
| 신규 `pitcher_id_mapping_v2.csv` | **90.9%** (고신뢰 구간 100%) | **94.0%** | 9.1% |

**효과 (2024 홀드아웃, 3-seed 짝지어, 기준 = 트랙맨 없음)**

| 설정 | 원본 | 재중심화 |
|---|---|---|
| 현행 매핑 | +8(±13) | +8(±12) |
| **새 매핑, 2군 필터 없음** | **+9(±5)** | **+9(±2)** |
| 새 매핑, 1군만 | +9(±18) | +5(±20) |
| 새 매핑 + 2군 등판비율 피처 | +10(±19) | -0(±22) |

평균이 아니라 **편차**가 핵심이다. 현행 매핑의 +8은 ±12라 "0일 수도" 있는 반면
새 매핑은 3 seed 전부 일관되게 이긴다. 예상과 어긋난 것 두 가지:
- **2군을 걸러내면 오히려 나빠진다** (결측 9.7%→14.1%). 정확도보다 커버리지가 중요하다.
  과거 'season 매핑'(정확하지만 커버리지 38%) 실험이 실패한 것도 같은 이유.
- **2군 등판 비율 피처는 무효**. train에 없는 새 정보라 기대했으나 재중심화 기준 -0±22.

**주의**: 이 홀드아웃은 트랙맨에 유리하다. 2024 검증행은 최신 트랙맨을 받지만
실제 2025 test는 1년 묵은 2024 값을 받는다. 실전 이득은 +9보다 작을 수 있다.

**리더보드 실측 (v5, 2026-08-19): +3.56 (987.3936 → 990.9528).**
예측 +9의 약 40%다. 위 '주의'가 그대로 실현된 셈이니, **앞으로 트랙맨 관련 홀드아웃
수치는 절반쯤 할인해서 읽을 것.** 참고로 누수 없는 중간 지표(오프셋 측정 셀의
~2023 학습 → 2024 예측)는 v4 777 → v5 784 로 +7 이었고, OOF 는 Brier 0.24401 → 0.24395.
둘 다 방향은 맞았으나 리더보드보다 낙관적이었다.

**폐기**: 트랙맨 추가 파생(속도변화율, 구종 엔트로피, 무브먼트 일관성 추세 등)은
밑에 깔린 집계가 pitcher×month 평균이라 이미 뭉개져 있다. zone_speed가 실패한 것과 같은 이유.

---

## 3. 모델링

### 현재 채택 구성 (990.9528점, `aimers_tuned_ensemble.ipynb` v5)

```python
# Cell 6a: Optuna 40 trial (전체의 약 33% 서브샘플, 3-fold)로 하이퍼파라미터 탐색
# 찾은 최적값 (932.96 버전의 depth=6,lr=0.05 기본값보다 훨씬 깊고 느리게 학습):
#   learning_rate≈0.0247, depth=9, l2_leaf_reg≈9.23,
#   bagging_temperature≈0.79, random_strength≈0.78

# Cell 6b: 위 파라미터로 전체 데이터 학습
for seed in [42, 202, 2024]:
    StratifiedKFold(n_splits=10, shuffle=True, random_state=seed)
    CatBoostClassifier(iterations=1000, **optuna_best_params,
                        eval_metric='Logloss', task_type='GPU', early_stopping_rounds=50)
    # fold별 CalibratedClassifierCV(method='isotonic', cv='prefit')로 보정
# 10-fold x 3-seed = 30개 모델 평균 (cb_fold_{seed}_{fold}.cbm 네이밍)
```

- **피처 파이프라인은 932.96 버전과 완전히 동일** (`step1~18`, zone_speed 없음) — 차이는
  하이퍼파라미터 튜닝 + fold 5→10 + seed 1→3뿐. **"같은 정보를 더 안정적으로 뽑아내는" 개선이
  실제로 +26.47점**이라는 큰 폭으로 먹혔다. 새 피처 추가 시도(zone_speed 계열)가 전부 마이너스였던
  것과 대조적 — 지금 단계에서는 신규 피처보다 기존 신호를 잘 뽑아내는 쪽이 더 검증된 전략.
- **`TimeSeriesSplit`이 아니라 `StratifiedKFold`를 쓰는 이유**: `TimeSeriesSplit`은 fold마다
  학습 데이터량이 다르고(17%~83%), 초기 fold가 최근 시즌 예측에 취약해서(실측 skill -0.6~-2.0%)
  균등평균이 오히려 naive baseline보다 나빠짐(818점 사태). `StratifiedKFold`는 각 fold가
  전체의 80%~90%를 균등하게 학습해 fold 간 실력이 대등하고, 합집합이 데이터 100%를 커버한다.
- **CatBoost 단독** (LightGBM은 현재 미사용 — TimeSeriesSplit 시절 불리한 조건에서 비교했던 결과라
  StratifiedKFold 조건에서 재검토 여지 있음)
- **모델 저장은 네이티브 포맷**: `model.save_model("x.cbm")` (joblib pickle 금지 — 버전 의존성 문제로
  과거 크래시 발생). Isotonic 보정기만 `joblib.dump` (단순 배열 기반이라 안전).

### 시도했던 것들

| 시도 | 결과 |
|---|---|
| **v5: 트랙맨 투수 매핑 재구축** | **990.9528 (987.39 대비 +3.56점) → 채택, 현재 최고점** |
| **월/요일 제거 (`v9m`, 30모델)** | **996.9977 = v5 +6.04** → ✅ **채택, 현재 최고점** (10모델 `s1m` 은 +4.52) |
| v8: 위 네 가지 동시 | 968.5952 (-22.35) → ❌ 기각. 절개로 완전 분해됨 |
| cond_* 감쇠 0.25 (`s1d`) | 기준선 -11.05 → ❌ 기각. 정규화가 원인 |
| 휴식·등판밀도·파울 (`s1r`) | 기준선 -6.85 → ❌ 기각. 트랙맨 staleness |
| cond_pb 맞대결 (`s1b`) | 기준선 -3.51 → ❌ 기각 |
| depth 8 → 9 (다른 것 동일) | 990.3838 (**-0.57**) → ❌ 기각 |
| seed 3 → 10 (모델 30 → 100) | 989.6899 (**-1.26**) → ❌ 기각 |
| v7: 반복수 1000 → 700 | 986.2114 (**-4.74**) → ❌ 기각 |
| v7: 반복수 1000 → 500 | 967.8868 (**-23.07**) → ❌ 기각. 아래 '앙상블 규모' 참고 |
| v6: 릴리스 동역학 12개 (등판 단위 분해) | 988.4720 (**-2.48**) → ❌ 기각. `USE_RELEASE_DYNAMICS=False` |
| v4: 조건부 투수통계 + 재중심화 + 죽은 피처 제거 | 987.3936 (959.43 대비 +27.97점) |
| Optuna 튜닝 + fold10 + seed3 앙상블 | 959.4258 (932.96 대비 +26.47점) → v4의 기반 |
| `prior_mean` 3안 비교 (현행 0.5352 / `control_success.mean()` 0.5238 / 시즌별) | 전부 오차범위 안 → 현행 유지. 4-1 참고 |
| LightGBM 85:15 앙상블 | TimeSeriesSplit 시절 실험, 재검토 필요 |
| 타자측 트랙맨 피처 6개 + zone_speed 체감구속 (동시) | 930.00 (932.96 대비 -2.96점) → 롤백 |
| zone_speed 체감구속 **단독** (타자 피처 없이) | 929.6060 (932.96 대비 -3.35점) → 롤백. 원인이 zone_speed였음을 확정 — 두 실험의 낙폭이 거의 같아서 타자 피처는 결백 |

### 2024 홀드아웃 스크리닝 결과 모음 (3-seed 짝지어, 트랙맨 제외 300iter 단일모델 기준)

절대값이 아니라 **기준선 대비 차이**만 의미가 있다. 단일 seed는 ±32점 노이즈이므로 4-10 참고.

| 시도 | 기준선 대비 | 판정 |
|---|---|---|
| ~~손실함수 RMSE~~ (Logloss 대신) | 스크리닝 +15(±6) → **실제 조건에서 ±0** | ❌ **기각.** 2026-08-20 재측정에서 Logloss 와 정점이 795 로 완전 동일. 스크리닝의 +15 는 재현 안 됨 |
| 조건부 투수통계 | 원본 +20(±15) / 재중심화 +27(±17) | ✅ 채택 (v4 반영) |
| 재중심화 | 전 설정 +11~22 (평균 +18) | ✅ 채택 (v4 반영) |
| 죽은 피처 제거 | +4(±2) | ➖ 미미하나 코드 정합성 차원에서 채택 |
| `asof_*` 시즌 디트렌드 | 원본 **-13(±10)** / 재중심화 +5(±13) | ❌ 기각. 조건부 통계와 중복으로 추정 |
| **`asof_*` 재설계 (최근 N구 이동평균)** | **아래 별도 항목** | ❌ **기각** |
| 최근가중 ×1.5 / ×2 학습 | -33 / -118 | ❌ 기각 |
| 2021~23만 / 2022~23만 학습 | -62 / -128 | ❌ 기각. 데이터 양이 최신성을 이긴다 |

**RMSE 도입 시 필요한 변경**: `CatBoostClassifier` → `CatBoostRegressor(loss_function='RMSE')`,
`predict_proba`→`predict`+클리핑, **Optuna 재탐색 필수**(현 `best_params`는 Logloss 기준),
isotonic 보정이 회귀 출력에도 유효한지 재확인.

### ⛔⛔ 2024 홀드아웃은 판정 지표가 아니다 — 4전 1승 3패 (2026-08-21 갱신)

이 프로젝트에서 가장 비싼 교훈이다. 처음엔 "낙관 편향(할인율)" 문제로 봤는데,
표본이 넷으로 늘고 나니 **할인이 아니라 무상관에 가깝다.**

| 변경 | 스크리닝(300iter/depth6) | 누수없는 홀드아웃(실제 파라미터) | 리더보드 |
|---|---|---|---|
| 트랙맨 v2 매핑 | +9 | +7 | **+3.56** ✅ |
| 릴리스 동역학 12개 | +20 | +5 | **-2.48** ❌ |
| 반복수 1000 → 500 | +30 | +30 | **-23.07** ❌ |
| **v8 (네 가지 동시)** | **+45** | **+27** | **-22.35** ❌ |

- **성공한 유일한 한 건(트랙맨 매핑)은 새 피처도 파라미터 변경도 아니라 '버그 수정' 이었다.**
  91% 틀린 매핑을 고친 것이라 어떤 검증 지표를 봐도 좋아질 수밖에 없었다.
  **홀드아웃이 맞힌 적은 사실상 없다고 보는 게 맞다.**
- 폐기된 옛 규칙: ~~"누수 없는 홀드아웃에서 +10 미만이면 제출 가치 없다"~~ →
  **+27 과 +30 도 실측 -22, -23 이었다. 크기로 거르는 규칙 자체가 성립하지 않는다.**
- ⚠️ **홀드아웃 수치로 채택 여부를 결정하지 말 것.** 쓸 수 있는 용도는 두 가지뿐이다:
  (a) 파이프라인이 안 죽고 점수가 붕괴하지 않았는지 확인 (예: season 제거 -424),
  (b) **부호가 크게 음수인 것을 버리는** 필터. 양수는 아무 것도 보증하지 않는다.

**왜 반대로 나오는가 — 데이터량 가설 (미검증, 현재 최선의 설명)**

홀드아웃은 **~2023 = 1.22M 행**으로 학습하고, 실전은 **~2024 = 1.48M 행**으로 학습한다.
**18% 차이다.** 작은 모델일수록 규제가 이득이므로, 홀드아웃은 체계적으로
"용량을 줄여라 / 피처를 빼라 / 더 세게 shrink 해라" 쪽에 가산점을 준다.

이 가설로 세 번의 실패가 전부 설명된다 — 반복수 1000→500(용량 축소), v8 의 월/요일
제거(피처 축소)와 cond 감쇠(유효표본 축소)가 모두 **규제 방향**이다. 반대로 성공한
트랙맨 매핑은 규제와 무관한 정보 수정이었다.

**가설이 맞다면 예측**: v8 네 항목 중 **월/요일 제거와 감쇠는 음수**, **휴식·파울과
cond_pb(정보 추가)는 0 이상**이어야 한다. 아래 절개 실험이 이걸 직접 가른다.

### ⛔ v8 = 968.5952 (-22.35) — 네 가지를 동시에 넣어서 범인을 못 찾는다 (2026-08-21)

| 항목 | 결합검증(500회 3seed) | 오프셋셀(1000회) | 리더보드 |
|---|---|---|---|
| 월·요일 제거 | +28 | | |
| 휴식·등판밀도 5개 | +20 | | |
| 파울 2개 + cond_pb | +16 | | |
| cond_* 감쇠 0.25 | +13 | | |
| **네 가지 동시** | **+45** | **+27** (790→817) | **-22.35** |

**과정상의 잘못이 명확하다. 4-14 가 "변경 하나를 측정할 땐 하나만 바꿀 것" 이라고
적혀 있는데 네 개를 한 번에 넣었다.** 그래서 -22.35 를 넷 중 누구에게도 귀속시킬 수
없고, 그 안에 섞여 있을 양수 항목까지 같이 버려야 할 판이다. 앞으로 **리더보드에
올리는 변경은 항상 하나씩.** 결합은 각각 리더보드에서 양수로 확인된 뒤에만.

건진 관찰 하나: **v8 의 OOF 는 v5 보다 나빴다** (0.24399 vs 0.24395). 월/요일 제거가
in-distribution 성능을 깎기 때문이다. 즉 OOF·홀드아웃·리더보드 셋이 서로 다른 말을 했고,
**이번엔 OOF 가 맞았다.** depth 9 때는 OOF 만 좋고 리더보드가 나빴으니 정반대다.
어느 지표도 단독으로는 신뢰할 수 없다.

### ✅ 절개 실험 — v8 의 -22.35 를 완전히 분해했다 (2026-08-21)

코드는 v8 과 **완전히 동일**하게 두고 설정 플래그 하나씩만 켠 축소판(1 seed / 10모델)
다섯 개를 리더보드로 실측했다. 빌더 `tools/mk_v8ab.py` (`S1=1` 로 축소판).

| 변형 | 리더보드 | 기준선 대비 | 2024 홀드아웃 |
|---|---|---|---|
| `s1o` v5 기준선 (전부 off) | 987.8724 | — | 793 |
| **`s1m` 월·요일 제거** | **992.3950** | **+4.52** ✅ | +15 |
| `s1b` cond_pb 맞대결 | 984.3595 | -3.51 ❌ | +13 |
| `s1r` 휴식·등판밀도·파울 | 981.0210 | -6.85 ❌ | +19 |
| `s1d` cond_* 감쇠 0.25 | 976.8195 | **-11.05** ❌ | +4 |

넷의 합 -16.89 + 상호작용 -5.46 = **-22.36 = v8 의 낙폭**. 완전히 설명된다.

**축소판이 유효하다는 확인 두 가지**
- `s1o` 987.87 vs v5(30모델) 990.95 → **모델 10→30 은 +3.08 이 전부다.** 앙상블 규모
  포화가 다시 확인됐다 (seed 3→10 이 -1.26 이었던 것과 일관).
- `s1r` 의 `cond_p.csv` 가 `s1o` 와 **비트 단위로 동일**(동일비율 1.000, 최대차 0.00000).
  `COND_DECAY = 1.0` 이 원래 식을 정확히 재현하므로 코드 경로 변경에 의한 오염이 없다.
  `s1d` 는 상관 0.69 로 확실히 다르다 — 감쇠가 실제로 적용됐다.

### ⛔ 최대 범인은 감쇠 **자체**였다 — 정규화는 악화 요인일 뿐 (2026-08-22 정정)

> ⚠️ **2026-08-21 에 이 절은 "원인은 정규화였다" 고 단정했다. 틀렸다.**
> `s1n`(정규화 없는 감쇠) 실측이 **979.9262 = -7.95** 로 나왔다.
> 정규화가 설명하는 것은 11.05 중 **3.11 (28%)** 뿐이고 나머지는 감쇠 자체다.
> 메커니즘 논증이 그럴듯하다는 이유로 확신했는데, 논증은 4분의 1만 설명했다.
> **메커니즘으로 원인을 지목했으면 반드시 실측으로 크기를 확인할 것.**

| | 리더보드 | 기준선(`s1o` 987.87) 대비 |
|---|---|---|
| `s1d` 감쇠 0.25 + 정규화 | 976.8195 | **-11.05** |
| `s1n` 감쇠 0.25, 정규화 없음 | 979.9262 | **-7.95** |

정규화(`w *= len(src)/w.sum()`)가 나쁜 것은 맞다. 분모를 행 수에 맞추면
`sum/(count+C)` 의 "표본이 적으면 0 으로 shrink" 라는 안전장치가 죽는다
(6시즌 x 300구 인 투수가 감쇠 0.25 면 유효표본은 400 인데 분모는 1800 을 쓴다).
하지만 그건 -3.11 이었다.

### ✅ 예측 상관은 제출 없이 3분에 잰다 — `s1lr` 확정 사망 (2026-08-22)

`tools/pred_corr.py` + `tools/mk_kcorr.py`. 제출 zip 의 `script.py` 를 train 한 조각
(test 로 위장)에 돌려 `submission.csv` 를 받고 상관 행렬을 찍는다. 학습 0회, 제출 0장,
캐글 CPU 커널에서 **3분**.

```
                 submit_v9m   submit_s1lr
submit_v9m           1.0000        0.9704
```

**0.9704 — 트리 계열 범위(0.92~0.97)의 꼭대기다.** 선형 baseline 을 깔아도 예측이
순수 트리와 사실상 같다. 점수(스크리너 +7 = 노이즈)와 다양성 양쪽에서 닫혔다.
`s1lr` 은 제출하지 않는다.

⚠️ 캐글 함정 둘 (여기서 커널 3번 죽었다):
- 이 계정은 `/kaggle/input/datasets/<owner>/<slug>/` 로 **한 단계 더 깊이** 마운트된다.
- 캐글은 데이터셋에 올린 zip 을 **자동으로 풀어** 디렉터리로 준다 (`submit_v9m/`).
- `kaggle datasets create -p` 의 경로에 `/` 가 있으면 Windows 에서 업로드가 깨진다.
  반드시 단일 이름 폴더를 쓸 것.

### 📌 모델은 외우고 있지 않다 — 전이 손실은 과적합이 아니다 (2026-08-22)

위 상관 측정의 부산물. 2024 행 15만 개는 두 모델 **모두의 학습에 들어가 있는데**:

```
in-sample 스킬(학습에 쓴 행)   2.232%
OOF 스킬                       2.21%
```

**거의 같다.** 122만 행에 1000트리/depth 8 인데 학습한 행과 안 한 행의 성능 차이가 없다.
즉 우리가 미래 시즌에서 잃는 64% 는 **과적합이 아니라 진짜 분포 이동**이다.

**따라서 "규제를 더 세게" 는 처방이 아니다.** 반복수 500(홀드아웃 +30 / 리더보드 -23),
최근성 가중 4연패, 홀드아웃의 규제 편향이 전부 같은 이야기다. 용량을 줄이는 방향의
후보는 홀드아웃이 뭐라 하든 기대값을 0 이하로 잡을 것.

### ❌ 단조 제약 — 기각 (2026-08-22)

트리는 학습 범위 안에서도 축을 아무 모양으로나 쪼갠다. 2025 는 투수 통계 분포가
왼쪽으로 밀리므로 지금 안 쓰이는 저구간의 **우연한 굴곡**이 그때 대량으로 쓰인다 —
그걸 금지하면 전이가 좋아질 것이라는 가설. 선형 baseline(항을 더함, -183)과 반대로
**자유도를 빼는** 접근이라 그쪽의 실패 모드가 없다는 것이 근거였다.

| 후보 | 점수 | base 대비 |
|---|---|---|
| `base` | 813.4 | (기준) |
| `plain` (`boosting_type='Plain'` 만) | 813.4 | **+0** — 대조군 정상 |
| `mono` (성공률 계열 10개, +1) | 810.9 | **-2** |
| `mono2` (+ 실패유형·릴리스std 25개, -1) | 769.1 | **-44** |

- **`plain` 이 정확히 base 와 같다.** 따라서 `mono` 의 -2 는 순수하게 제약의 효과다.
- **`mono` 가 0 이라는 것은 트리가 애초에 해로운 비단조 분할을 하고 있지 않았다는 뜻이다.**
- **`mono2` 의 -44 는 예고된 실패다.** 단조 제약은 **편(partial)** 단조성인데,
  `middle_rate` 를 음수로 묶는 것은 "성공률을 고정했을 때" 성립하지 않는다
  (같은 성공률에서 실패가 '몰림' 쪽에 몰려 있다고 더 나쁠 이유가 없다).
  **부호가 확실한 것은 주변 효과이지 편 효과가 아니다.**

### 📌 전이 손실은 모델이 오작동해서 생기는 게 아니다 (2026-08-22)

세 가지가 같은 곳을 가리킨다.

| 관찰 | 뜻 |
|---|---|
| in-sample 스킬 2.232% ≈ OOF 2.21% | 외우고 있지 않다 |
| 단조 제약 +0 | 이동 구간에서 이상한 모양을 만들고 있지 않다 |
| 용량 축소·최근성 가중 전부 음수 | 규제가 부족한 게 아니다 |

**모델은 멀쩡히 행동하고 있다.** 그렇다면 미래 시즌에서 잃는 64% 는
**피처와 결과의 관계 자체가 시즌마다 달라지기 때문**이다. 이건 훨씬 어려운 문제이고,
"규제/용량/제약" 계열 후보는 전부 기대값 0 으로 잡아야 한다는 뜻이다.

### ❌ MVS vs Bayesian 은 예측을 다르게 만들지 않는다 (2026-08-22)

CPU(MVS) 와 GPU(Bayesian) 는 **다른 부트스트랩 알고리즘**이라 예측이 갈릴 수 있다고
보고 블렌드 파트너 후보로 쟀다. 학습 비용 0 (`tools/pred_corr.py`).

```
                  local_cpu(MVS)   v9m(Bayesian)   s1lr(선형+잔차)
local_cpu               1.0000          0.9964          0.9694
v9m                     0.9964          1.0000          0.9704
```

**0.9964 — 사실상 같은 모델이다.** 샘플링 방식 차이는 다양성을 만들지 않는다.

**건진 것 하나**: in-sample 스킬이 `local_cpu` 2,100 / `v9m` 2,232 로
**CPU 쪽이 학습 데이터를 덜 맞힌다.** 그런데 홀드아웃은 CPU 가 822 / GPU 가 784~797 로
높다. **학습을 덜 맞히고 미래를 더 맞히는 것** — 진짜 일반화 개선의 서명이다.
홀드아웃(+26, +25~38)과 독립적인 세 번째 증거다.

### ⛔⛔ 최근성 가중은 이 문제에서 **항상** 진다 (2026-08-22, 4번째 확인)

| 시도 | 결과 |
|---|---|
| 최근 가중 학습 x1.5 / x2 | -33 / -118 |
| 2021~23만 / 2022~23만 학습 | -62 / -128 |
| `asof_*` 이동평균 (roll100/300/1000/ewm) | -5 ~ -27 |
| **`cond_*` 시즌 감쇠** | **-7.95** |

드리프트가 실재하고 단조적인데도 그렇다. 이유는 이제 분명하다 —
**드리프트는 이미 `season` 과 재중심화가 처리하고 있다** (`season` 제거 = **-424**).
그래서 피처 안에 최근성을 또 넣으면 새 정보가 아니라 **유효 표본만 깎인다.**
`cond_*` 는 표본 크기로 버는 피처(C=200/100/100/50 으로 shrink)인데
옛 시즌을 버리면 그 근거가 사라진다.

**이 축은 완전히 닫혔다. 어떤 형태의 최근성 가중도 다시 시도하지 말 것.**

### 📐 노이즈 바닥은 ±4 가 아니라 ±1.5 안팎이다 (2026-08-21 수정)

`s1o` 가 990.95 → 987.87 로 **10→30 모델 차이 +3.08 을 깨끗하게 재현**했고,
seed 3→10(30→100모델)은 순수 분산 감소인데 **-1.26** 이었다. 후자가 노이즈의 크기다.
어제 "±4" 로 추정하며 한 자릿수 판정들을 전부 폐기했는데, **그건 과도했다.**

| | 값 | 재판정 |
|---|---|---|
| depth 9 | -0.57 | 노이즈 |
| seed 10 | -1.26 | 노이즈 |
| v6 릴리스 동역학 | -2.48 | **진짜 음수** |
| zone_speed | -3.35 | **진짜 음수** |
| 트랙맨 v2 매핑 | +3.56 | **진짜 양수** — v5 채택은 정당했다 |

### ⛔ 트랙맨 파생 피처는 2024 홀드아웃으로 잴 수 없다 (2026-08-21 확정)

| 트랙맨 파생 | 홀드아웃 | 리더보드 |
|---|---|---|
| 매핑 재구축 (버그 수정) | +7 | +3.56 |
| 릴리스 동역학 | +5 | -2.48 |
| **휴식·등판밀도·파울** | **+19** | **-6.85** |

이유는 1장에 이미 적혀 있었는데 고치지 않았다: **2024 검증행은 그 해의 트랙맨을 받고,
실제 2025 test 행은 1년 묵은 2024 값을 받는다.** 릴리스 메커니즘은 해가 바뀌어도
비슷하지만 **휴식 패턴·등판 밀도는 보직이 바뀌면 통째로 달라진다** — 그래서 가장 크게 무너졌다.

**고치는 법(미적용)**: 홀드아웃 검증행의 트랙맨 merge 를 한 시즌 늦춰 배포 조건과 맞출 것.

### 📊 홀드아웃은 크기를 못 맞히지만 순위는 맞혔다 (2026-08-21)

트랙맨 파생(`r`)을 빼면 나머지 셋의 **순서가 정확히 일치한다**:
홀드아웃 m(+15) > b(+13) > d(+4) → 리더보드 m(+4.52) > b(-3.51) > d(-11.05).

즉 **후보들 사이의 상대 순위는 살아남을 수 있다.** 다만 (a) 트랙맨 파생은 예외이고
(b) 부호가 어디서 바뀌는지는 알 수 없다(홀드아웃 +13 이 리더보드 -3.51 이다).
**여러 후보 중 하나를 고르는 데는 쓰되, 채택 여부는 여전히 리더보드로만 결정할 것.**

### ⛔ CatBoost 용량 축은 소진됐다 (2026-08-21) — 더 돌리지 말 것

v5 에서 **한 가지씩만** 바꿔 리더보드로 실측한 결과 전부 0 이하다.

| 변경 | 리더보드 | v5 대비 |
|---|---|---|
| 반복수 1000 → 500 | 967.89 | **-23.07** |
| 반복수 1000 → 700 | 986.21 | **-4.74** |
| **v5 (1000회 / depth 8 / seed 3)** | **990.95** | (기준) |
| depth 8 → 9 | 990.38 | **-0.57** |
| seed 3 → 10 (모델 100개) | 989.69 | **-1.26** |

**세 축(반복수·깊이·앙상블 크기)이 전부 평평한 꼭대기에 있다.** v5 는 국소 최적이고,
같은 모델 안에서 용량만 조절하는 시도는 여기서 끝이다. 남은 방향은 **다른 모델 계열**뿐.

- **seed 30 → 100 이 0 이라는 게 특히 결정적이다.** 순수 분산 감소인데도 안 오른다는 건
  30개 앙상블이 이미 분산을 뽑을 만큼 뽑았다는 뜻이다. 모델들이 90% 겹치는 데이터로
  학습돼 서로 상관이 높아서, 개수를 늘려도 독립적인 정보가 안 늘어난다.
  (기존 로드맵의 "seed 3→10 +5~11, 무위험" 추정은 **틀렸다**. 무위험은 맞았고 이득이 0이다.)
- depth 9 는 **OOF 는 개선(0.24395 → 0.24374)인데 리더보드는 -0.57** 이다. 용량을 키우면
  학습 시즌은 더 잘 맞히지만 미래 시즌 전이가 안 되는 전형적 패턴. **OOF 개선을 근거로
  용량을 늘리지 말 것.**

### ⚠️ 오프셋 측정 셀에 ±13 노이즈가 있다 (2026-08-21)

`seed10` 실행에서 발견했다. 오프셋 셀은 `random_state=SEEDS[0]` 3-fold 고정이라
**seed 가 3개든 10개든 계산이 완전히 동일**한데, v5 는 784, seed10 실행은 797 이 나왔다.
같은 코드·같은 seed·같은 fold 다. 차이는 전부 **CatBoost GPU 학습의 실행 간 비결정성**이다.

**한 번 돌려서 ±26(2σ) 안쪽이면 아무것도 못 읽는다.** 어제 v7 의 "+30" 도 이 노이즈를
빼지 않고 읽은 값이었다. 이 지표로 판정하려면 최소 3회 반복하고 평균·편차를 함께 볼 것.

### ⚠️⚠️ 2024 홀드아웃과 리더보드가 반복수에서 반대를 가리킨다 — 원인 미상 (2026-08-21 갱신)

**증상 (사실, 확정)**: 반복수 1000 → 500 이 홀드아웃에서 +30 이었는데 리더보드에서 **-23**.
리더보드는 트리가 **많을수록** 좋다고 말한다 (500: 967.89 / 700: 986.21 / 1000: 990.95).
v7 안의 n500 vs n700 은 같은 모델·같은 코드에 `ntree_end` 만 다른 깨끗한 비교라
리더보드 쪽은 의심할 여지가 없다. 2024 홀드아웃 쪽도 여러 커널에서 500 정점이 재현된다.

> **~~원인: 앙상블 규모~~ — 2026-08-21 기각.**
> "홀드아웃은 모델 3개, 실운영은 30개. 후반 반복의 과대적합은 분산 성분이라 평균으로
> 상쇄되므로 앙상블이 크면 최적 반복수가 오른쪽으로 밀린다"고 적었으나, `aimers-fam3`
> 에서 seed 5개로 직접 재보니 **`cb_sym` 의 최적점이 k=1,2,3,5 내내 500 에 고정**이었다
> (점수만 794 → 801 → 803 → 803 으로 수렴). leaf-wise 계열(cb_loss 200→350,
> xgb 100→200)은 한 칸 밀렸다가 k=2~3 에서 멈춘다.
> **분산 상쇄 효과는 실재하지만 작고 금방 포화한다.** 500 을 1000 너머로 옮길 크기가 아니다.

**남은 후보**: (a) 학습 데이터가 9% 더 많다(1.22M → 1.33M), (b) **2025 시즌이 2024 와
다르게 반응한다**. 둘 다 검증할 방법이 없다. isotonic 유무도 아니다 — v7 오프셋 셀은
isotonic 을 붙이고도 500/700 이 1000 보다 높았다.

**실무 규칙 (원인과 무관하게 유효)**:
1. **반복수는 v5 의 1000 을 유지한다.** 2024 홀드아웃이 반복수 축에서는 방향조차 못 맞히므로
   이 축의 판정은 리더보드로만 한다. 그리고 리더보드에서 이미 포화했다(3장 '용량 축 소진').
2. **지금까지의 스크리닝은 전부 300회 / depth 6 / 모델 1~3개였다.** 그 조건은 체계적으로
   "용량을 줄여라"는 답을 낸다. 그 조건에서 기각한 것들은 재검증 여지가 있다.
3. 블렌드 가중치처럼 **같은 실행 안에서 짝지어 빼는 델타**는 ±13 노이즈와 무관하고
   앙상블 규모 의존성도 낮다 (k=1~5 에서 +9 → +7 로 거의 불변). 이런 지표는 믿을 수 있다.

**부수 확인**: isotonic 구현을 `CalibratedClassifierCV` → `IsotonicRegression` 직접 적합으로
바꾼 것은 **중립**이다. n700(986.21)과 v5(990.95)의 차이 4.74 가 트리 300개 몫과 맞아떨어진다.

### 📏 공식 베이스라인이라는 기준점 (2026-08-21 실측)

데이콘 공식 베이스라인(코드공유 14146/14147, RandomForest)을 **우리와 같은 2024 홀드아웃**
에서 직접 돌렸다. 설정은 배포본 그대로: `test.csv` 의 원본 47컬럼만,
`RandomForestClassifier(n_estimators=100, max_depth=10, min_samples_leaf=200)`,
트랙맨·파생피처·재중심화 전부 없음.

| | 2024 홀드아웃 | 평균예측 편차 | 재중심화 여지 |
|---|---|---|---|
| 공식 베이스라인 (RF, 원본 47컬럼) | **415.6** | +0.0151 | +91 |
| CatBoost 튜닝, 원본 컬럼만 (A 설정) | **760** | — | — |
| **우리 v5 (전체 파이프라인)** | **784~803** | +0.008 | +15~24 |

읽어낼 것 세 가지:
1. **우리 파이프라인은 베이스라인의 2배다.** "우리가 뭔가 망가뜨렸다"는 의심은 여기서 끝.
2. **모델을 제대로 쓰는 것이 345점(RF→CatBoost), 우리 피처 엔지니어링 전체가 35점.**
   같은 원본 47컬럼인데 이만큼 벌어진다. 가치 배분이 이렇다는 걸 기억할 것.
3. **평균 보정은 우리 구멍이 아니다.** 베이스라인은 편차 +0.0151 로 91점을 흘리는데
   우리는 +0.008 이고 그마저 재중심화로 줍고 있다.

**홀드아웃 → 리더보드 환산**: v5 가 784 → 990.95 이므로 2025 는 2024 보다 약 **+207** 후하다.
이 오프셋으로 역산하면 리더보드 100위(1,107)는 2024 홀드아웃 **~900**, 1위(1,243)는 **~1,036**.
즉 **상위권은 미래 시즌 전이에서 우리보다 16% 더 뽑아낸다.** 우리 파생피처 51개가 0 이었으니
그쪽도 피처로 번 것은 아닐 가능성이 높다. 무엇으로 버는지는 아직 모른다.

### NN 엔티티 임베딩 1차 — 실패, 그러나 상관은 유망 (2026-08-21)

`aimers-nn`. 범주형 27개(+`pitcher_id`/`batter_id`)를 임베딩, 수치 103개(결측 지시자 32 포함),
MLP 512→256→1, 파라미터 29.6만, lr 2e-3 OneCycle, 20 에폭, 2 seed.

| 에폭 | 1 | 3 | 7 | 10 | 20 |
|---|---|---|---|---|---|
| 단독(재중심화) | 333 | **365** | 234 | -404 | -2,435 |
| cb_sym 예측 상관 | 0.67 | 0.71 | 0.67 | 0.55 | **0.35** |
| 블렌드 이득 | +0 | +1 | **+5** | +1 | +0 |

- **상관 0.67 은 진짜 성과다.** 트리 계열은 최적점에서 0.92~0.97 을 못 벗어났다.
  함수 계열이 다르면 실제로 다르게 틀린다는 게 확인됐다.
- **단독 365 라 섞을 값어치가 없었을 뿐이다.** 그리고 **에폭 1부터 내리막**이라
  과소적합 구간을 아예 못 봤다 — 설정이 틀린 것이지 방향이 틀린 게 아니다.
  신호가 1% 뿐인 문제에 파라미터 29.6만 + lr 2e-3 은 과하다.
- 다음(v3) 방향: 임베딩 차원 축소, 은닉 128→64, dropout 0.4, 임베딩에 별도 weight decay,
  lr 1e-3, 에폭 8, **0.2 에폭 단위 평가**. 그리고 ID 임베딩 유무를 A/B 로 갈라 본다.
- 목표: **단독 600~700 + 상관 0.75 → 블렌드 +40~80.** 두 자릿수를 넘길 유일한 후보다.

### ⚠️ 캐글 기본 GPU(P100)에서 torch 가 안 돈다 (2026-08-21)

```
AcceleratorError: CUDA error: no kernel image is available for execution on the device
```

캐글 설치본이 **torch 2.10.0+cu128** 인데 최신 빌드가 **Pascal(sm_60) 커널을 뺐다.**
P100 이 sm_60 이다. CatBoost 는 자체 커널이라 P100 에서 멀쩡히 돈다 — 그래서 지금까지 몰랐다.

- **`torch.cuda.is_available()` 는 True 를 반환한다.** 디바이스는 보이는데 실행할 이미지가
  없어서 첫 연산에서야 터진다. 폴백을 넣으려면 **실제로 `mm` 과 `randperm` 을 시켜보고**
  판단해야 한다.
- T4 x2 는 sm_75 라 정상 동작하지만 **API 로는 선택 불가**(1.7.4.5 에 `machine_shape` 미구현).
  웹 에디터에서 Accelerator 를 바꿔 Save & Run All 해야 한다.
- CPU 폴백 실측: 파라미터 29.6만, 1.22M 행, 배치 8192 → **에폭당 약 33초**. 견딜 만하다.

### ⛔ 보정(calibration) 축 완전 종료 — 천장 51점, 접근 수단은 전부 마이너스 (2026-08-21)

지금까지 모든 시도가 **분해능(discrimination)** 축이었다. **신뢰도(calibration)** 축은
재중심화 상수 하나로만 건드리고 있었으므로, 그 축의 **천장을 직접 쟀다**
(`experiments/features/aimers_oracle.ipynb`).

**Murphy 분해 (2024 홀드아웃, 50분위)**

```
Brier = 신뢰도 − 분해능 + 불확실성
신뢰도 0.000128   분해능 0.002098   불확실성 0.249807
점수 환산:  분해능 840  −  신뢰도 51  =  788
★ 완벽 보정으로 회수 가능한 최대치 = 51점 (그때 도달 840)
```

**오라클 상한** (2024 정답을 봤을 때. 실제로는 못 쓴다)

| 설정 | 점수 | 현행 대비 |
|---|---|---|
| 현행 (외삽 목표로 상수 시프트) | 805 | — |
| 오라클 평균 시프트 | 818 | +13 |
| 오라클 온도 (2-파라미터 Platt) | 820 | +15 — **a=1.052** |
| 오라클 완전 isotonic | 856 | **+51** (Murphy 상한과 일치) |

**실행 가능한 버전** (배포와 같은 '한 시즌 앞' 구조: 2023 에서 적합 → 2024 에 적용)

| | 점수 | 현행 대비 |
|---|---|---|
| 평균 시프트 | 757 | **-48** |
| 온도 | 239 | **-566** |
| 완전 isotonic | 620 | **-185** |

- **천장이 51 인데 접근 수단이 전부 큰 마이너스다.** 이 축은 끝났다. 다시 파지 말 것.
- 실행 가능 버전이 무너지는 이유: **모델의 과대예측 폭 b 가 시즌마다 다르다**
  (b_2023 +0.0192 / b_2024 +0.0072). 한 시즌에서 잰 보정을 다음 시즌에 옮기면 과보정한다.
- **`a = 1.052` 는 과신이 아니라 미세한 과소신이다.** 아래 '수축 가설' 참고.

### `season` 피처 제거 — -424 (2026-08-21)

백분위 변환이 0 이었던 이유가 "트리가 이미 시즌별 매핑을 배운다"였으므로, 그 학습이
전이에는 해로울 수 있다고 보고 `season` 을 빼봤다. **805 → 380.** 즉사다.
그 학습은 해로운 게 아니라 **필수**다.

### 편향 시계열 외삽 — 방법론은 옳고, 현행이 이미 최적 (2026-08-21)

재중심화 상수를 b_2024 한 점이 아니라 여러 점의 추세로 정하자는 발상
(`experiments/features/aimers_bias.ipynb`). Y=2021~2024 각각 `~Y-1 학습 → Y 예측` 을
현행 오프셋 셀과 **동일한 구조**(3-fold + isotonic + 1000회)로 돌려 b 를 네 점 실측했다.

| Y | b | 그 시즌 하락폭 |
|---|---|---|
| 2021 | **-0.0062** | +0.0000 |
| 2022 | +0.0033 | -0.0038 |
| 2023 | **+0.0192** | -0.0290 |
| 2024 | +0.0072 | -0.0139 |

**b 는 시간의 함수가 아니라 그 시즌 하락폭의 함수다.** 하락이 0 이던 2021 엔 b 도 음수다.

**검증 (2021~2023 으로 b_2024 를 맞히기)** — 이 셀이 결정적이었다.

| 모형 | 예측 b_2024 | 오차 | 손실 |
|---|---|---|---|
| A: b ~ 연도 | +0.0309 | 0.0236 | **224점** |
| **B: b ~ 하락폭** | **+0.0078** | **0.0006** | **0점** |
| C: b = k×하락폭 | +0.0093 | 0.0020 | 2점 |

**2025 외삽**: 예상 하락폭 -0.0114 (리그평균 0.4747) → B 모형 b_2025 = **+0.0057**.
현행(b_2024 재사용) +0.0072 와 차이 0.0015 = **1점. 바꿀 값어치 없음, 현행 유지.**

- **A(연도 외삽)를 그냥 반영했다면 224점을 잃었을 것이다.** 검증 셀이 잡았다.
  외삽 모형을 세울 땐 **반드시 한 점을 빼고 맞혀보게 할 것.**
- 건진 것: **하락폭 → b 관계식을 확보했다.** 만약 2025 하락폭이 2023 처럼 -0.029 로
  커지면 b_2025 ≈ +0.0157 이고 현행은 29점을 잃는다. 전망이 바뀌면 상수를 계산할 수 있다.

### ❌ 수축(과신) 가설 — 기각 (2026-08-21)

"OOF 스킬 2.20% vs 실전 0.99% 이므로 예측이 과신 상태다. `p' = r̂ + λ(p−r̂)` 로 수축하면
λ*=0.72, 스킬 1.156%(+165)" 라는 가설. 유도식(λ*=Cov/Var, skill*=Cov²/(Var·U))은 전부 맞다.
**입력값 `Var(p)` 가 틀렸다.**

Murphy 숫자로 직접 풀면:
```
2c − v = (res−rel)/U = 0.007886      (c=Cov/U, v=Var/U)
isotonic 오라클 = res/U = 0.008398  →  v ≈ 0.0086,  c ≈ 0.008243
λ* = c/v ≈ 0.96          skill* = c²/v = 0.0079 → 790점 (현재 788, 이득 +2)
```

- **λ* 는 0.72 가 아니라 0.96 이다.** 가설은 `Var(p)=0.0220·U` (OOF 스킬에서 유도)를 썼는데
  **홀드아웃 실측은 0.0086·U 로 2.5배 작다.**
- OOF 의 퍼짐이 부풀려진 이유 두 가지: (1) **시즌 간 퍼짐**이 섞여 있다(모델이 2019 행엔
  ~0.55, 2024 행엔 ~0.49 를 낸다. 그 분산 ≈0.00064 는 단일 시즌인 test 안엔 없다),
  (2) **무작위 fold 누수** — 같은 투수·시즌의 인접 투구가 train/valid 로 갈린다.
- **λ=0.72 를 실제로 적용하면 741점, 약 -47 이다.** 과신이 아닌 것을 수축하면 신호를 깎는다.
- 논리적으로도 닫힌다: `p'=r̂+λ(p−r̂)` 는 λ>0 이면 단조변환이고 **isotonic 이 최적 단조변환**
  이므로, 수축의 상한은 isotonic 오라클 = **+51** 을 넘을 수 없다.

**교훈**: in-distribution OOF 로 out-of-distribution 의 분산·공분산을 추정하지 말 것.
필요하면 홀드아웃 예측값에서 직접 재라 (Murphy 분해 두 줄이면 된다).

### 전이 손실 직접 공격 — 두 건 모두 무효 (2026-08-21)

in-distribution 2.21% → 미래 시즌 0.80%. **64%를 전이에서 잃는다.** 여기가 유일하게
세 자릿수가 남은 곳이라 보고 두 방향을 쟀다. 둘 다 0 이다.

| 시도 | 잰 것 | 결과 (2024 홀드아웃, 3seed 짝지어, 500회) |
|---|---|---|
| **시즌 내 백분위 변환** (`experiments/features/aimers_pct.ipynb`) | 수준 피처 14개를 그 시즌 안에서의 백분위로 교체/추가 | **+0(±9) / -1(±1)** |
| **타자측 조건부통계** (`experiments/features/aimers_bat.ipynb`) | `cond_b`/`cond_bc`/`cond_bp` + 타자 스무딩 | **+1(±3)** |
| ↑ 같은 커널: 투수팀(포수 대리) | `cond_ptm` | +3(±9) — 편차가 평균의 3배 |
| ↑ 같은 커널: 투수×타자 맞대결 | `cond_pb` (C=20) | **+6(±4)** — 유일하게 부호 일관 |

- **백분위가 0 인 이유**: 트리는 `season` 으로 분기할 수 있으므로 시즌별 매핑을 **이미
  암묵적으로 학습하고 있었다.** 명시화해줘도 새 정보가 아니다. `step1~13` 이 0 이었던 것과
  같은 원리 — 그 행 안에서 계산되는 변환은 트리가 스스로 만든다.
  (구현은 정상이었다. 2019 원값중앙 0.5574→백분위 0.500, 2024 는 2023 격자로 0.5106→0.390
  으로 배포 시점의 낡음까지 재현했다.)
- **`cond_p`(투수) +27 vs `cond_b`(타자) +1 — 27배 비대칭.** 똑같은 코드에 키만 바꿨는데
  이렇다. **제구 성공에 타자는 거의 기여하지 않는다.** 정의 자체가 "포수 요구 지점에
  던졌는가"라 타자는 간접적으로만 영향을 준다.
  **주최측이 투수 16컬럼 / 타자 3컬럼을 준 것은 편의가 아니라 정보였다.**
- 살릴 만한 건 `cond_pb` 하나(+6)뿐이고, 그것도 `+전부` 설정에서 +2 로 줄어든다(중복).

**남은 미검증 카드**: LR 을 baseline 으로 깔고 트리로 잔차를 부스팅하는 것
(`Pool(baseline=...)`). 트리는 구조적으로 외삽을 못 하는데(2025 를 2024 로 취급) 선형항은
할 수 있다. 재중심화(+18)가 그것을 상수 하나로 때우는 crude 버전이다. 다만 백분위가 0 으로
나온 이상 기대값은 낮춰 잡아야 한다 (+5~15).

### 트리 계열 다양화 — 종료 (2026-08-21)

CatBoost 용량 축이 소진돼서 "다르게 틀리는 모델"을 찾아 세 계열을 붙여봤다.
`aimers-fam` → `fam2` → `fam3` 세 커널. 전부 2024 홀드아웃, 각 계열 최적 반복수에서 비교.

| 계열 | 최적점 (k=5) | 최적 반복 | cb_sym 과 예측 상관 |
|---|---|---|---|
| **cb_sym** (대칭트리, 현행) | **803** | 500 | — |
| cb_loss (CatBoost Lossguide) | 731 | 350 | 0.935 |
| lgbm (LightGBM) | 739 | 200 | 0.916 |
| xgb (XGBoost) | 724 | 200 | 0.919 |

**블렌드 이득은 +7 이고 앙상블이 커져도 안 커진다** (k=1 +9 / k=2 +8 / k=3 +7 / k=5 +7).
오히려 줄어든다 — CatBoost 자체가 좋아지면 남의 도움이 덜 필요해진다.
최적 가중치는 `cb_sym 0.75 + lgbm 0.25`.

- **원인은 상관이다.** 최적점에서 세 계열의 예측 상관이 0.92~0.97 이다(lgbm↔xgb 0.973).
  트리 구조만 다를 뿐 같은 데이터에서 같은 것을 학습한다. 평균해도 지워질 게 없다.
- **블렌드가 의미 있으려면 상관 0.85 아래가 필요하고, 트리 계열 안에서는 안 나온다.**
  다음은 함수 계열 자체가 다른 NN 엔티티 임베딩.
- ⚠️ **fam v1 의 함정**: 네 계열을 전부 1000회 고정으로 돌려 lgbm 506 / xgb 452 가 나왔다.
  계열의 실력이 아니라 과적합 지점의 점수였다(최적점에서는 739 / 724). 계열 비교는
  **반드시 각자의 최적 반복수에서** 할 것. lr × 트리수로 총 학습량을 맞춰보면 예측된다
  (CatBoost 0.0228×1000=22.8 vs lgbm/xgb 0.03×1000=30 — 이미 30% 초과였다).
- ⚠️ **잔차 상관은 쓰지 말 것.** `p - y` 는 분산의 99%가 공유된 `y` 라 어떤 두 모델을 넣어도
  0.99 가 나온다. fam v1 에서 이 무의미한 표를 찍었다. **예측 상관을 볼 것.**

### 🔧 큰 스윙 전용 스크리너 (2026-08-21) — 후보를 25분에 죽인다

`tools/make_cache.py` 가 전처리를 한 번만 돌려 `cache/` 에 X/y/season 을 저장하고,
`tools/screen.py` 가 후보 하나를 **제출 없이 25~30분**에 판정한다. 구조는 오프셋 셀과
같다 (~2023 학습 -> 2024 예측, 3-fold, isotonic, 재중심화). 로컬 CPU 로 무인 실행된다.

**왜 못 믿는 지표를 다시 쓰는가**: 이 지표는 한 자릿수 차이의 부호를 못 맞힌다
(홀드아웃 +13 이 리더보드 -3.51 이었다). 하지만 우리가 찾는 건 +3 이 아니라 **+80** 이다.
±15 노이즈로 그 크기를 놓칠 수는 없다. **+50 미만은 전부 노이즈로 버린다** 를 코드에 박았다.

**첫날 결과 (base 816)** — 전부 노이즈이거나 음수다.

| 후보 | 점수 | base 대비 | |
|---|---|---|---|
| `lrbase` 선형 baseline + 트리 잔차 | 823 | +7 | 노이즈 |
| `optbest` GPU Optuna 최적 조합 | 822 | +6 | 노이즈 |
| `bc512` border_count 512 | 818 | +2 | 노이즈 (254 에서 포화) |
| `rsm40` 랜덤 부분공간 40% | 817 | +1 | 노이즈 |
| `ctr3` max_ctr_complexity 3 | 816 | +0 | 노이즈 |
| `blend_recent` 전시즌+최근 블렌드 | 814 | -2 | 노이즈 (가중 0~0.5 전부 훑음) |
| `ctr2` max_ctr_complexity 2 | 811 | -5 | 노이즈 |
| `rsm70` 랜덤 부분공간 70% | 808 | -8 | 노이즈 |
| `bayes` bootstrap_type=Bayesian | 806 | -10 | 아래 참고 |
| `bc128` border_count=128 | 804 | -12 | 아래 참고 |
| **`lrbase2` 선형항에 시즌x피처 상호작용** | **633** | **-183** | **재앙** |

**`lrbase2` 가 이 도구의 값어치를 증명했다.** baseline 로짓 표준편차는 0.229 로 멀쩡했고
(내가 안전망으로 잡은 기준은 3), 2024 범위도 학습 범위와 비슷했다. **크기는 정상인데
방향이 틀린** 유형이라 크기 기준 점검으로는 절대 못 걸렀다. 스크리너가 없었으면 코랩
45분 + 제출 한 장을 쓰고 다음 날 알았을 일이다.

**닫힌 축**: 전이 손실 공격(선형 baseline 계열), 랜덤 부분공간, 최근 시즌 블렌드,
범주형 조합 CTR(CPU 기준).

### ✅ 스크리너는 캐글 CPU 커널에서도 같은 값을 낸다 (2026-08-21)

`tools/mk_kscreen.py` 로 s1o 노트북 전처리 + `tools/screen.py` 를 캐글 **CPU** 커널
(`enable_gpu: false`) 에 올렸다. 캐글 CPU 세션은 **GPU 쿼터를 쓰지 않는다** (12시간/세션).

| | base 점수 |
|---|---|
| 로컬 16코어 | 816 |
| **캐글 CPU 4코어** | **813** |

**차이 3점 — 노이즈 안이다.** 둘 다 CPU 라 `bootstrap_type=MVS` / `border_count=254`
로 같은 알고리즘이 돈다. 코랩 GPU 로 돌리면 기본값이 달라 절대값을 섞어 읽을 수 없다.
**이제 로컬이 본 학습에 묶여 있어도 캐글에서 후보를 병렬로 돌릴 수 있다.**

### ⛔ 한 행 안에서 계산되는 파생피처는 구조적으로 죽어 있다 (2026-08-21 확정)

세 번 독립적으로 같은 결론이 나왔다.

| 시도 | 결과 |
|---|---|
| `step1~13` 파생 51개 (피처 구성 감사) | ~0 |
| 시즌 내 백분위 변환 | +0 |
| **차이 피처 6개** (`d_form{1,3,5}`, `d_middle`, `d_pb`, `d_smooth`) | **+7 = 노이즈** |

`diff` 는 "차이는 시즌 드리프트에 불변이므로 전이에 강하다"는 논거로 만들었는데
(리그가 1%p 내려가면 두 항이 같이 내려가고 차이는 그대로다) **역시 0 이었다.**

**공통 원리: 그 행 안의 기존 컬럼만으로 계산되는 값은 트리가 스스로 만든다.**
새 정보가 아니라 재배열일 뿐이다. 이득이 났던 것들(`cond_*` +27, 트랙맨 +9)은 전부
**다른 행/다른 테이블에서 정보를 끌어온** 것이다.
**앞으로 이 부류의 후보에는 시간을 쓰지 말 것.**

### ✅ 학습/추론 피처 대조 감사 — 불일치 없음 (2026-08-21)

`tools/audit_parity.py`. train 한 조각을 test 로 위장해 제출 zip 의 `script.py` 를 돌리고,
그때 만들어진 `df_features` 를 `cache/X.pkl` 의 **같은 `row_id`** 와 컬럼별로 비교한다.
`script.py` 는 학습과 완전히 다른 코드 경로인데 값이 같은지는 한 번도 안 봤었다
(트랙맨 exact 897 vs asof 933 의 36점이 통째로 이 유형이었다).

**결과: 공통 94개 중 90개가 완전 일치.** 트랙맨 `merge_asof` + `__orig` 순서 복원,
`step1~18`, `count_advantage` 의 `'None'` 라운드트립, `asof_*` 45개 전부 통과.
**지금 제출본이 조용히 흘리고 있던 점수는 없다.**

남은 4개(`cond_p/pc/ph/phc`)는 **설계상 당연한 차이**다. 학습은 leak-free 라 2024 행을
2019~2023 으로만 인코딩하고(신규 투수 18.2% 가 NaN), 배포 룩업 테이블은 2024 까지
포함해 만들어진다(결측 0%, 상관 0.90). 실제 2025 test 는 양쪽 다 "직전 전 시즌" 이라
일관된다. **즉 이 방법으로는 `cond_*` 를 검증할 수 없다 — 미감사 상태로 남는다.**

⚠️ 감사 중 눈에 걸린 것: `cond_*` 의 **의미가 학습 시즌마다 다르다.**
2019 행은 과거 시즌 0개라 전부 NaN, 2024 행은 5개, test 는 6개다. 모델은 "1시즌으로 만든
`cond_p`" 와 "5시즌으로 만든 `cond_p`" 를 같은 축에 놓는다. 고치려면 고정 윈도우가
필요한데 초기 시즌을 버려야 하고("데이터 양이 최신성을 이긴다" -62/-128 과 충돌),
'과거 시즌 개수' 를 피처로 주면 test 에서 학습에 없던 값(6)이 나온다
(`asof_*` fresh 가 **-217** 로 무너진 것과 같은 함정). **미해결로 남겨둔다.**

### ⛔ 전이 손실 공격 종료 (2026-08-21)

| | 홀드아웃 |
|---|---|
| 선형 baseline + 트리 잔차 | +7 |
| 위 + (시즌 x 피처) 상호작용 | **-183** |

재중심화(+18)가 이 방향의 유일한 성공이었는데, 그건 **상수 하나**였기 때문에 통했다.
선형항을 늘릴수록 외삽이 망가진다. 계수를 보면 이유가 보인다 —
`smoothed_pitcher_success_rate +0.120` 과 `cond_p -0.118` 이 거의 상쇄하는 공선 쌍이고,
그런 쌍을 외삽하는 것은 구조적으로 불안정하다 (`C=1.0` 은 120만 행에서 사실상 무규제).

### ❌ CPU 하네스에서도 용량 축은 닫혀 있다 — optbest 절개 (2026-08-22)

"닫힌 축들은 전부 bc128 + Bayesian 인 -22점짜리 CatBoost 에서 닫혔으니 CPU 에서
재개봉해야 한다" 는 제안. 그리고 `optbest`(822)가 `bc128`(-12)과 `ctr2`(-5)를 달고
있으니 벗기면 **839** 가 될 것이라는 가법 추정이 근거였다.

| 구성 | 점수 | 예측 | 실측 |
|---|---|---|---|
| `base` (d8 / lr0.0228 / it1000 / bc254 / ctr1) | 813.4 | — | — |
| `optbest` (d6 / lr0.0135 / it1400 / **bc128 / ctr2**) | 822 | — | — |
| `opt254` (bc128 -> **bc254**) | 821.3 | +12 | **+0** |
| `opt254c1` (+ ctr2 -> **ctr1**) | 805.9 | +5 | **-15** |

**두 항목 모두 예측과 반대다. 839 는커녕 806 이다.**

메커니즘은 깨끗하다 — **얕은 트리(d6)는 촘촘한 분할 경계가 필요 없고**(bc254 무의미),
**깊이로 상호작용을 못 만드니 범주형 조합 CTR 에 의존한다**(ctr2 필수).
`-12` 와 `-5` 는 **d8 에서 잰 값이라 d6 로 옮겨지지 않는다.**

**교훈: 다른 파라미터 영역에서 잰 효과를 더하지 말 것.** v8 절개에서 상호작용이
전체의 24% 였던 것과 같은 함정이고, 이번엔 부호까지 뒤집혔다.
그리고 Optuna 가 `bc254` 를 탐색 공간에 넣고도 `bc128` 을 고른 것이
**이미 반대 증거였다** — 탐색 결과를 읽을 때 무엇이 기각됐는지도 볼 것.

`opt254` 의 +7.9 는 전달률 0.3 을 적용하면 리더보드 **+2.4** 로 노이즈 바닥(±1.5)
언저리다. **용량 축은 CPU 하네스에서도 닫혀 있다.**

### ❌ 시즌 절편 baseline — 기각 (2026-08-22)

`season` 제거가 -424 라는 것은 트리가 **시즌 수준을 배우는 데 상당한 용량을 쓴다**는
뜻이니, `Pool(baseline=logit(리그평균_시즌))` 로 수준을 미리 빼주면 그 용량이 풀릴
것이라는 가설. `lrbase2`(-183)와 달리 선형항이 **시즌당 절편 하나**뿐이라 공선 쌍이
생길 수 없다는 것이 안전 근거였다. 검증 시즌 절편은 학습 시즌만으로 외삽했다
(2024 외삽 0.4877 / 실제 0.4861, 오차 0.0016).

**결과 808.1 = base 대비 -5.3.**

`season` 이 다른 피처와 워낙 얽혀 있어(제거 시 -424) **수준만 따로 떼어내는 것이
성립하지 않는 것으로 보인다.** 트리는 시즌 수준을 상수로 배우는 게 아니라
'시즌 x 피처' 로 배우고 있고, 절편을 빼주면 그 결합 학습을 방해한다.

### ⭐⭐⭐ '전이 손실' 은 시즌 드리프트가 아니었다 — 당해 시즌 폼을 모르는 것이었다 (2026-08-22)

열흘 동안 "in-distribution 2.21% vs 미래 시즌 0.80%, 64% 를 전이에서 잃는다" 를 전제로
규제·용량·단조제약·최근성가중·선형외삽·보정을 전부 시도했고 전부 0 이었다.
**전제가 틀렸다.** `tools/group_oof.py` 로 같은 조건에서 세 가지 분할을 쟀다.

| 분할 | 전체 | **2024 행만** |
|---|---|---|
| A. 무작위 fold (현행 OOF) | 2020.7 | **1092.3** |
| B. 투수 x 시즌 그룹 | 1740.0 | **791.1** |
| C. 투수 그룹 (커리어째) | 1703.5 | 774.6 |

**B 의 2024 행 점수 791.1 이 우리 시즌 홀드아웃(~790)과 사실상 같다.**
B 는 **시즌 드리프트가 전혀 없는 설정**이다 — 2024 데이터가 학습에 들어가 있고
단지 *그 투수의* 2024 만 빠졌다. 그런데도 홀드아웃과 같은 점수가 나온다.

```
A (그 투수의 당해 시즌을 봄)   1092
B (못 봄)                      791
차이 301  =  '당해 시즌 폼을 아는 것' 의 값어치
```

**즉 우리가 잃던 것은 미래 시즌으로 옮겨가는 비용이 아니라 '그 투수가 올해 어떤지'
를 모르는 비용이었다.** 분포 이동을 고치려던 처방이 전부 0 이었던 이유다.

C 가 B 와 거의 같다(1703 vs 1740)는 것도 같은 이야기다 — **과거 시즌 이력은 당해
시즌 폼에 비하면 거의 값어치가 없다.** eda9 에서 `past`(cond_p 대역)가 `wcur` 옆에서
기여 0 이었던 것과 정확히 일치한다.

**따라서 남은 방향은 하나다: 당해 시즌 정보를 합법적으로 최대한 끌어내는 것.**

### 📖 주최측 Q&A 에서 확인된 것들 (2026-08-22, `aimers_qna.md`)

**1) `game_type` 의 정체 — R = Regular(1군 정규시즌), F = Futures(퓨처스/2군)**
train 의 10.9% 가 **2군 경기**다. 우리가 찾은 F 의 2023 붕괴(.7087 -> .4729)는
판정 체제 변화였다 (참가자 질문에 ABS 도입 시기가 언급되나 **외부 데이터는 사용 불가**
라는 것이 주최측 답변이다. 우리는 train.csv 자체에서 이 변화를 찾았고 그건 합법이다).

**2) ⚠️ `wseason` 은 우리 고유 발견이 아니다 — 9일 전 공개 확인됐다**
차니찬(2026-08-13): "`asof_pitcher_n` 과 `asof_pitcher_success_rate` 가 통산 누적임을
확인했습니다. 각 행의 값에서 학습 기간까지의 투수별 누적(동봉한 상수표)을 빼서
**현 시즌 진행분을 복원**해 피처로 쓰는 것이 허용되는지" -> 주최측 **"네. 가능합니다."**
**우리가 오늘 찾은 것과 정확히 같다.** 합법성은 확정이지만 **독점적 우위가 아니다** —
상위권 상당수가 이미 9일째 쓰고 있을 것이다. 기대 순위 상승을 낮춰 잡을 것.

**3) test.csv 에는 연속 행의 asof 차분으로 정답이 복원되는 구조가 있고, 그건 금지다**
2jin1(08-17)이 지적했고 주최측이 **"규칙 위반"** 이라고 명시했다.
8/18 에 리더보드에 스킬 **99% 후반대** 점수가 올라왔던 것(talkboard/417157, 추천 56)이
이것으로 보인다. 현재는 지워졌다. **우리 `wseason` 은 자기 행의 asof + train 룩업만
쓰므로 이에 해당하지 않는다** (2번 답변으로 확정).

**4) LUPI/증류는 허용된다 — 그러나 얻을 게 없다 (아래 별도 절)**

### ❌ 투구 1:1 정렬 + LUPI/증류 — 문은 열렸는데 방이 비었다 (2026-08-22)

주최측이 seopseopi(08-17)/손석길(08-20) 질문에 **"모두 가능합니다"** 라고 답했다:
train x trackman 매칭 부분집합에서 **현재 투구의 구종·트랙맨을 입력으로 받는 teacher**
를 학습하고 student 는 pre-pitch 만 받아 증류받는 방식. 제출은 student 만.

**투구 1:1 정렬은 실제로 된다.** 키
`(season, game_month, pitcher_id, inning, top_bottom, balls, strikes, outs, dayofweek)`
로 양쪽 다 유일한 조합이 **344,018개 = train 의 23.3%**.

정렬된 부분집합에서 잰 결과:

| 구성 | 스킬 |
|---|---|
| A. 투구 물리량 원본 + 구종 | 843 |
| B. 투수 대리 (asof + 투수평균 물리량) | 906 |
| **D. B + 실제 구종** (privileged, 추론 불가) | **1,384** (+478) |
| C. D + 이 투구의 물리량 편차 | 1,441 (+57) |
| **E. B + 구종 성향** P(구종\|투수,카운트,타자손) — **합법 대체재** | **894 (-12)** |

**privileged 정보 중 값어치 있는 것은 물리량이 아니라 '무슨 구종을 던졌나' 다** (+478).
물리량 편차는 +57 로 거의 없다 — 트랙맨에 **위치 정보가 없어서** 제구 성공을 직접
말해주지 못한다.

**그런데 합법 대체재인 '구종 성향' 은 정확히 0 이다.** 성향은 투수 수준의 상수라
이미 base 에 들어 있다. +478 은 **그 무작위 추첨의 실현값을 아는 것**에서 나오고
추론 시점에는 원리적으로 없다.

**따라서 증류도 닫힌다.** 증류가 student 에게 주는 것은 `E[y | pre-pitch]` 인데
student 는 이미 그것을 직접 추정한다. 증류의 이득은 새 정보가 아니라 **분산 감소**
뿐이고, 우리는 **in-sample 2.232% ~= OOF 2.21%** 로 분산 제약 상태가 아님을 확인했다.
**합법이지만 얻을 것이 없다.**

### ⚠️ 주최측 '평가 데이터 독립 예측 원칙' — 제출 전 필수 검증 (2026-08-22)

공지 `talkboard/417123` (2026-08-13): **"실격 사례도 지속적으로 발생하고 있습니다."**

**행 A 의 예측에 쓸 수 있는 것은 넷뿐이다**
1. 행 A 에 포함된 입력 변수
2. 행 A 의 입력 변수만으로 만든 파생변수
3. 주최 측 공식 학습 데이터
4. 공식 학습 데이터만으로 만든 통계·모델·파생변수

**금지 예시**: test 다른 행으로 누적/rolling/lag, test 전체의 평균·분포·빈도·순위로
예측값 보정, 같은 선수·팀·월·경기 단위로 test 안에서 집계, **평가 데이터상 더 과거로
보이는 행을 이용해 현재 행의 피처 생성**.

**주최측이 제시한 판정 시험** — 이대로 돌려보면 된다:
> 어떤 행의 예측값은 (a) test 에 그 행 1개만 있을 때와 (b) 전체가 함께 있을 때
> **동일해야 한다.** 달라지면 다른 행이 추론에 영향을 준 것이다.

`tools/audit_independence.py` 가 이 시험을 자동화한다. **v10w 실측 통과**:

```
3,000행 배치 예측  vs  같은 행을 1행 단독으로 예측
최대 차이 1.11e-16  (부동소수 수준)
```

의심했던 곳은 `step14_convert_to_category` 의 `astype(str).astype('category')` 였다 —
카테고리 목록이 배치 내용에 의존하므로 CatBoost 가 코드를 쓰면 배치 크기에 따라
예측이 달라진다. 실측 결과 문자열 값을 쓰므로 문제없다.

**`wseason` 이 규칙상 안전한 이유**: 행 A 의 `asof_pitcher_n` / `asof_pitcher_*_rate`
(=1번) 에서 **train 으로만 만든 투수별 룩업**(=4번) 을 뺀다. test 다른 행을 보지 않는다.

**앞으로 모든 제출 전에 이 검증을 돌릴 것.**

### 📌 리더보드 정황 — '99% 후반대' 점수가 있었다 (2026-08-22 확인)

`talkboard/417157` (2026-08-18, 추천 56): 리더보드에 **스킬 99% 후반대**의 점수가
올라왔고, 외부에서 2025 실제 결과를 구해 넣은 것으로 의심된다는 문제 제기.
현재는 지워져 1위가 1,218.66(스킬 1.219%)이다.

⚠️ 그 글의 핵심 우려는 **"정답 전체가 아니라 일부만 교묘하게 쓰면 코드만으로 못 잡는다"**
이고 주최측 답변은 아직 없다. 통제할 수 없는 변수이므로 참고만 한다.
다만 **컷(100인 = 팀 순위 약 96위, ~1,119.4)이 부풀려져 있을 가능성**은 염두에 둘 것.

### ❌ 당해 시즌 비율의 디트렌드 기준 '정정' — 오히려 -13 (2026-08-22)

`wseason5` 는 당해 시즌 비율을 **커리어 컬럼(`asof_pitcher_*_rate`)의 시즌 평균**으로
뺀다. 그런데 둘의 평균 차이가 시즌마다 0.000~0.029 로 흔들려서 디트렌드가 절반만 된다.
'복원값 자체의 시즌 평균' 으로 빼는 것이 이론적으로 옳다 (`wseason5b`).

| | 점수 |
|---|---|
| `wseason5` (원래) | **885.6** |
| `wseason5b` (이론적으로 옳은 디트렌드) | 872.6 |

**-13.0 으로 오히려 나쁘다** (스크리너 노이즈 ±15 안이므로 '같거나 조금 나쁨').
가설: 원래 판이 남기는 시즌별 잔차 오프셋이 **드리프트 신호를 담고 있어서**,
완전히 0 으로 중심을 맞추면 그걸 지운다. 단정은 못 한다.
**원래 판(`wseason5`)을 유지한다 — 이미 학습·검증이 끝난 v10w 가 그것이다.**

### 📐 트랙맨은 신호가 있다 — 다만 1년 묵으면 77% 가 사라진다 (2026-08-22)

지금까지 트랙맨은 '피처를 만들어 시도했다 실패했다' 는 기록뿐이었고(매핑 +3.56 /
릴리스 동역학 -2.48 / 휴식·등판밀도 -6.85 / zone_speed -3.35) **데이터를 들여다본
적이 없었다.** 투수-시즌 단위로 트랙맨 집계 -> 그 투수-시즌의 제구 편차를 예측해봤다.

⚠️ **먼저 방법론**: `cross_val_score` 를 shuffle 없이 쓰면 R2 가 **-0.0098** 로 나온다.
   fold 가 투수 id / 시즌 순으로 갈려서 생기는 인공물이다. `KFold(shuffle=True)` 로
   바꾸면 **+0.100** 이다. 부호가 뒤집힌다 — 이걸로 축을 닫을 뻔했다.

| 구성 | R2 (투수-시즌 제구 편차) |
|---|---|
| 트랙맨 `_mean` 8개 | +0.053 |
| 트랙맨 `_std` 8개 | +0.025 |
| 둘 다 16개 | **+0.093** |
| 현행 파이프라인 근사 (fb속도 mean + std 8개) | +0.042 |
| (비교) 그 투수의 직전 시즌 제구 편차 하나 | +0.112 |

**평균이 표준편차의 2배를 설명하는데 우리 트랙맨 피처 20개 중 18개가 표준편차다.**
회전수·무브먼트·릴리스 위치의 **수준(level)** 은 한 번도 안 썼다.

**★ 결정적 검증 — 배포 조건 (트랙맨에 2025 가 없으므로 1년 묵은 값을 쓴다)**

| | 직전시즌 기록 위 증분 R2 |
|---|---|
| 같은 시즌 트랙맨 (낙관, 배포 불가) | +0.047 |
| **직전 시즌 트랙맨 (배포 조건)** | **+0.011** |

**77% 가 staleness 로 사라진다.** 트랙맨 축이 닫혀 있던 이유가 피처 설계가 아니라
**데이터에 2025 가 없다는 것**임이 처음으로 정량화됐다. +0.011 을 스킬로 환산하면
홀드아웃 ~9점, 리더보드 **~+3** 이고 이는 매핑 재구축 실측(+3.56)과 정확히 맞는다.

- `zone_speed` 실패(-3.35)도 설명된다 — `rel_speed` 와 같은 신호라 **중복**이었다
  (상관 -0.170 vs -0.165). 반면 회전수·무브먼트·릴리스 위치는 물리적으로 다른 차원이다.
- 트랙맨의 **32% 가 2군(MIN)** 이고 2019 63,869 -> 2024 127,452 로 비중이 커지고 있다.
  1군만 쓰면 설명력이 오히려 오른다 (+0.100 -> +0.127) — 다만 커버리지가 준다.
- **남은 후보**: `_mean` 계열 추가. 배포 조건 증분이 +0.0038 이라 기대는 리더보드 +1
  수준이지만 비용이 0 에 가까워 다른 것 위에 얹는 용도로는 남는다.

### ❌ 당해 시즌 비율의 shrink 기준을 '그 투수 과거' 로 바꾸기 — 기각 (2026-08-22)

현행은 `(ws + 리그평균 x C)/(wn + C)` 로 **리그 평균**에 당긴다. 5시즌 이력이 있는
투수라면 그 투수 자신의 과거 수준이 더 나은 사전값일 것 — 경험적 베이즈의 상식.

| shrink 기준 | 단독 스킬 (2024, C=100) |
|---|---|
| 리그 평균 (현행) | **600** |
| 그 투수 과거 수준 | 580 |

**표본이 적을수록 더 나쁘다** (당해 0~50구 구간에서 -115). 상식과 반대인 이유는
**투수의 과거 이력이 당해 시즌을 예측하는 데 거의 쓸모없기 때문**이다. 같은 결론이
네 번 독립적으로 나왔다:

| 측정 | 결과 |
|---|---|
| eda7 '직전 시즌까지만' 단독 | -2 |
| eda9 `past` leave-one-out (wcur 있을 때) | -0 |
| group_oof C(커리어째 제거) vs B(당해만 제거) | 1703 vs 1740 |
| eda11 과거 수준으로 shrink | -20 |

편향되고 노이즈 큰 표적으로 당기느니 비편향인 리그 평균이 낫다.
**함의: `cond_*` 네 개는 당해 시즌 복원이 들어간 뒤로 거의 잉여일 수 있다** (미검증).

### ❌ prev-game 지표의 시즌 경계 보정 — +6, 기각 (2026-08-22)

`asof_pitcher_prev{1,3,5}_game_*` 는 **시즌 경계를 넘는다.** 이력 있는 투수의
시즌 첫 등판(당해 0~30구) 9,002행에서 `prev1` 결측이 **0.0%** 다 — 작년 마지막
등판을 가리킨다는 뜻이다. 그리고 그 구간에서 해롭다:

| 당해 시즌 투구수 | prev5 단독 스킬 |
|---|---|
| 0~60 (7.4%) | **-459** |
| 60~300 | 175 |
| 300+ | 424 |

'그 값이 가리키는 시즌' 의 리그평균으로 디트렌드해봤다 (당해 비중으로 두 시즌 평균을
혼합). 시즌 초 구간 -459 -> -383 (+76) 이지만 **전체로는 +6** 이라 노이즈다.
경계 넘김이 원인의 일부일 뿐이고, 시즌 초에는 애초에 경기 수가 적어 prev5 자체가
표본 부족이다. 부수 관찰: `prev1_game` 은 전 구간에서 음수다 (한 경기 ~20구 = 노이즈).

### ❌ game_type 제거 / 구체제 F 행 제외 — 둘 다 기각 (2026-08-22)

| 후보 | 점수 | base(813.4) 대비 |
|---|---|---|
| `nogt` (game_type 제거) | 766.7 | **-46.7** |
| `dropoldF` (2019~2022 F 행 학습 제외) | 773.5 | **-39.9** |

순열 중요도가 -554 로 96개 중 최악이었는데 빼면 -47 이다.
**'모델이 쓰는 방식이 해롭다' 와 '빼면 좋아진다' 는 다른 이야기다** — 재학습하면
`season` 과 결합해 보완한다. 팀 ID(홀드아웃 -77)와 같은 패턴이다.
체제 변화 자체는 실재하지만(2022 .7087 -> 2023 .4729) **처방이 나오지 않는다.**

### 📊 분해 가능한 asof 컬럼은 열 개, 그중 쓸 것은 일곱 개 (2026-08-22)

같은 구조(커리어 누적 비율 x 누적 개수)를 가진 컬럼이 열 개다. 선형 대리모형
증분(`tools/eda10.py`, base = 커리어 원본 + 최근경기 + 상황):

| 묶음 | 증분 |
|---|---|
| 투수 결과 5개 (success/reverse/middle/ball/strike) | **+83** |
| 구종 3개 (fastball/breaking/offspeed, 분모 `pitchmix_n`) | **+0** |
| 타자 2개 (success/middle, 분모 `asof_batter_n`) | +10 |

**구종 배합은 제구 성공과 무관하다.** 타자는 소폭 — 타자 축의 진짜 신호가
0.138% 뿐인 것과 일관된다 (투수는 0.828%).
대리모형의 +83 이 스크리너 실측 +72.2 와 가까워 **이 대리모형은 믿을 만하다.**

### ⭐⭐ 당해 시즌 성적 복원 — 스크리너 +72.2, 첫 돌파 (2026-08-22)

**EDA 로 찾았다.** 열흘 동안 모델만 두들기고 데이터를 본 적이 없었던 것이 문제였다.

`asof_pitcher_*_rate` 는 **커리어 누적**이라 투수마다 섞인 시즌 수가 다르다.
2024 에서 `asof_pitcher_success_rate` 하나만으로 예측한 스킬:

| 조각 | 스킬 |
|---|---|
| 이력 있는 투수 (80.1%) | **141** |
| 신규 투수 (19.9%) | **906** |

같은 컬럼인데 6배 차이다. 신규 투수는 직전 시즌이 없어서 그 값이 **순수한 당해 시즌
성적**이고, 이력 있는 투수는 2019(리그 .5647)부터 섞인 낡은 값이다.

**복원**
```
커리어 누적 개수 = asof_pitcher_*_rate x asof_pitcher_n   (그 행이 갖고 있다)
그 시즌 시작 시점 = 투수별 룩업 (학습 데이터로 만든다)        (test 행 통계 아님 = 합법)
차이 -> 당해 시즌 투구수 / 개수 -> 당해 시즌 비율
```

다섯 비율 전부에 적용된다. `success` 는 라벨로도 계산되지만
`middle/reverse/ball/strike` 는 투구별 라벨이 없으므로 **asof 컬럼 자체에서 읽는다**:
각 (투수, 시즌) **첫 행**의 `asof_n x 비율` 이 곧 그 시즌 시작 시점의 누적이다.

**스크리너 실측 (base 813.4)**

| 후보 | 점수 | base 대비 |
|---|---|---|
| **`wseason5`** (다섯 비율 전부 + 표본크기) | **885.6** | **+72.2** ★ |
| `wseason4` (success 제외 넷) | 863.9 | +50.5 |

**스크리너로 판정한 후보 17개 중 처음으로 +50 을 넘었다** (직전 최고 `opt254` +7.9).
넷만으로도 +50.5 라는 것은 실패유형·볼/스트라이크 비율에도 독립적인 정보가 있다는 뜻이다.

**왜 이건 되고 최근성 가중 4연패는 안 됐나**
- 그것들은 전부 정보를 **빼거나 대체**했다 (학습 가중, 시즌 절단, 이동평균, 감쇠).
  이건 커리어 값을 그대로 두고 **컬럼을 더한다**.
- `asof_*` fresh 설계(-217)와 정반대다. 주최측 asof 가 **test 에서도 시즌 내 누적**이라
  (test 5행에서 같은 투수 24713 의 `asof_pitcher_n` 이 0 -> 9 로 증가함을 확인)
  학습·추론 양쪽에서 똑같이 신선하다.
- **트리가 스스로 만들 수 없다.** '그 투수의 직전 시즌까지 누적' 은 어떤 컬럼에도 없고
  다른 행에서 끌어와야 한다 — 이득이 났던 `cond_*`(+27)/트랙맨(+9)과 같은 부류이고,
  0 이었던 것들(한 행 안 변환)과는 다른 부류다.

**학습/추론 규칙 일치 검증** (트랙맨 exact/asof 36점 사고 유형)
```
학습:  (투수, 대상시즌) 첫 행의 asof      = 그 시즌 시작 시점 커리어 상태
추론:  train 마지막 시즌 그 투수 마지막 행 + 1투구
실측:  투구수 n0 불일치 0/391명 (최대차 0)
      누적개수 x0 최대차 1 (마지막 투구 자신을 못 세는 구조적 오차 = 비율로 0.0003)
```

⚠️ 스크리너는 디트렌드에 **각 시즌의 실제 리그평균**을 썼다 (2024 포함). 배포본은
   외삽값을 쓴다 (2024 외삽 오차 0.0016). 상수 시프트라 영향은 작지만 0 은 아니다.

빌더: `tools/mk_ws.py` (`WS_RATES` 로 비율 선택) -> `aimers_v10w.ipynb`

### ⛔ game_type=F 는 2023 에 체제가 바뀌었다 — 부호 반전 (2026-08-22)

전이 안정성 감사(`tools/transfer_audit.py`)에서 순열 중요도 **-554(2023) / -479(2024)**
로 96개 중 압도적 최악이었다. 음수는 '그 피처를 쓰는 것이 해롭다' 는 뜻이다.

| game_type | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---|---|---|---|---|---|
| F (10.9%) | .6892 | .5878 | .7038 | **.7087** | **.4729** | .4593 |
| R (89.1%) | .5495 | .5269 | .5128 | .5037 | .5031 | .4897 |

**한 시즌에 -0.236. 드리프트가 아니라 체제 변화이고, F 가 R 대비 +0.20 -> -0.03 으로
부호가 뒤집혔다.** F 는 시즌 내내 고르게 분포하므로 포스트시즌이 아니다.

같은 감사에서 `batter_team_id` -216 / `pitcher_team_id` -190 도 크게 음수지만,
팀 제거는 이미 홀드아웃 -77 로 기각했다. **순열 중요도(모델이 쓰는 방식이 해롭다)와
제거 효과(재학습하면 보완한다)는 다른 이야기다.** 원인을 데이터에서 확인하기 전에는
건드리지 말 것.

### ✅ 제출 전 검증은 두 가지를 따로 봐야 한다 (2026-08-23)

`tools/audit_independence.py` 는 **규정(행 독립)** 만 본다. 상수가 조용히 무시돼
피처가 죽어 있어도 통과한다 — 두 경우 모두 같은 값이 나오면 되기 때문이다.

그래서 `tools/verify_ws_live.py` 를 만들었다. **대조군 방식**이다:
`ws_*` 를 지운 zip 과 예측을 비교해서, 달라지지 않으면 그 상수가 안 쓰이는 것이다.

```
ws_* 있음   평균 0.473586
ws_* 없음   평균 0.454498
행별 차이 평균 0.019176 | 0 이 아닌 행 100.0%   -> wseason 살아 있음 확인
```

⚠️ **커널 산출물에 실제로 이 버그가 있었다.** v10w 계열 노트북의 오프셋 셀이
`train_constants.json` 을 통째로 덮어써서 `ws_*` 네 키가 날아간다.
`script.py` 는 그 키가 없으면 wseason 블록을 건너뛰므로 **학습은 진짜 값,
추론은 전부 NaN** 이 된다. 수정은 `tools/mk_ws.py` 에만 들어가 있고
**디스크의 `aimers_v10w.ipynb` 와 cpua/b/c 노트북에는 없다**
(`assert "ws_league_mean"` 검색 0건). 1058.72 를 낸 `v10w_fixed` 도 사후 패치본이다.
`tools/assemble_cpu.py` 가 조립 시 복구하고 검증한다.

### ❌ 디트렌드 기준을 '더 옳게' 고치면 두 번 다 나빠졌다 (2026-08-23)

`wseason5` 의 디트렌드를 바꾼 시도가 두 건인데 **둘 다 음수**다.

| 판 | 점수 | wseason5(885.6) 대비 |
|---|---|---|
| **`wseason5` 현행 (시즌 리그평균)** | **885.6** | (기준) |
| `bothgt` (wseason + cond_* 둘 다 game_type) | 883.8 | -1.8 |
| `condgt` (cond_* 만 game_type) | 877.4 | -8.2 |
| `wseason5b` (복원값 자체의 시즌 평균) | 872.6 | -13.0 |
| **`ws5gt` (wseason 만 game_type)** | **867.8** | **-17.8** |

**축 전체가 닫혔다 — 네 후보 중 최선이 -1.8(노이즈)이다.**

⚠️ **비가법성이 크다.** 한쪽만 바꾸면 -17.8 / -8.2 인데 둘 다 바꾸면 -1.8 이다.
한 곳만 고치면 `wseason` 과 `cond_*` 가 '리그평균' 을 서로 다르게 정의해 충돌한다.
둘 다 고치면 일관성은 회복되지만 **이득은 0** 이다.
(v8 절개에서 상호작용이 24% 였던 것보다 훨씬 크다 — 같은 상수를 공유하는
피처군을 건드릴 때는 반드시 **전부 같이** 바꿔서 재야 한다.)

⚠️ **같은 실행 안에서 `wseason5` 가 885.6 으로 정확히 재현됐다.** 이전 커널의 885.6 과
소수점까지 같으므로 -17.8 은 노이즈가 아니다. (부수 확인: 스크리너의 실행 간
재현성이 내가 가정한 ±15 보다 훨씬 좋다.)

**메커니즘 (추정)**: `game_type` 은 라벨 체제인 동시에 **투수 수준의 대리변수**다
(2군에서 던진다 = 대체로 약한 투수). game_type 안에서 중심을 맞추면 체제 오차와
함께 **그 수준 정보까지 지워진다.** F 행이 받는 +0.12~0.18 오차는 실재하지만,
그 오차를 없애는 비용이 이득보다 크다.

⛔ **더 중요한 교훈 — '표적과의 상관' 은 점수를 예측하지 못한다.**
이 후보의 근거는 2024 투수 편차와의 상관이 **+0.260 -> +0.477** 로 오른다는 것이었다.
상관이 두 배 가까이 올랐는데 **점수는 17.8 떨어졌다.**
앞으로 상관·R2 같은 대리지표로 후보를 채택하지 말 것. 스크리너로만 판정한다.

### 📋 2026-08-23 스크리너 판정 15건 — 양수 0건 (기준선 `wsboth`)

`wsboth` = `wseason5` + `wsbat` = 리더보드 1,088.83 의 구성. 이제 후보는 이 위에서 잰다.

| 후보 | 2023 | 2024 | 판정 |
|---|---|---|---|
| **`wsboth`** (기준선) | **628.5** | **894**(로컬) / 886(캐글) | — |
| `condb` 타자 조건부 3종 | -3.8 | -8 | ❌ **두 시즌 합의 → 기각** |
| `wsbsit` 투수x상황 3종 | -18.6 | -7.3 | ❌ **두 시즌 합의 → 기각** |
| `tskill` 구종별 실력 x 상황믹스 | — | -9.3 | ❌ |
| `wspmix` / `wsxprob` 구종 성향 | — | +0.9 / -10.7 | ❌ |
| `ws5c10 / c30 / c300` shrink C | — | +0.9 / -7.2 / -24.7 | ❌ 축 종료 |
| `ws5nocond` cond_* 제거 | — | -12.4 | ❌ cond_* 는 잉여가 아니다 |
| `ws5gt / condgt / bothgt` | — | -17.8 / -8.2 / -1.8 | ❌ 축 종료 |
| `seasonpair` 2시즌 묶음 | — | -14.6 | ❌ |
| **`seasonlast2`** 마지막 두 시즌 병합 | — | **-282.9** | ❌ 아래 참고 |

⛔ **그런데 같은 하네스에서 `wsbat` 이 +3.0(2024) 이었고 리더보드는 +30.11 이었다.**
**스크리너 분해능은 ±10 인데 우리가 찾는 것은 +20~30 이다 — 구분하지 못한다.**
제출은 45장 남았고 GPU 본 학습은 63분이다. **병목은 후보도 제출도 아니라 스크리너다.**
-> 앞으로 스크리너는 **재앙 필터**(-50 이하 버림)로만 쓰고 그 위는 리더보드로 판정한다.

### ⛔ `season` 축 완전 종료 — 최근 시즌 구분은 위험이 아니라 자산이다 (2026-08-23)

`tools/eda31.py` 로 **배포 모델에서 season 의 분기 경계를 직접 읽었다:**
```
[2019.5, 2020.5, 2021.5, 2022.5, 2023.5]     최대 경계 2023.5
2024 도 2025 도 다섯 경계를 전부 초과 -> **비트 단위로 같은 잎**
season 분기는 전체 8,000개 중 293개 (3.7%)
```
`season` 은 cat_features 에 없는 int64 라 학습에서 본 값 위로는 경계가 생길 수 없다.

**따라서 'season NaN 마스킹', '빼고 재학습', '추론 때 2024 주기' 는 전부 무효다** —
트리에서 그 조작들이 도달하려는 상태가 **이미 현재 상태**다.

남은 유일한 손잡이는 '마지막 시즌을 특별 취급하지 못하게 경계를 없애는 것' 이었고,
그것도 죽었다:

| | 점수 | wseason5(885.6) 대비 |
|---|---|---|
| `seasonpair` (2시즌씩 3구간) | 871.0 | -14.6 |
| **`seasonlast2`** (마지막 두 시즌 병합) | **602.7** | **-282.9** |

**`season` 제거가 -424 인데 마지막 두 시즌만 병합해도 -283 이다.**
즉 -424 의 **3분의 2가 '가장 최근 두 시즌을 구분하는 능력'** 에서 나온다.
가설과 정반대다 — 최근 시즌 특별 취급은 위험이 아니라 **핵심 자산**이다.

### ⛔ 대리지표 3연패 — 후보 채택에 쓰지 말 것 (2026-08-23)

| 대리지표 | 예측 | 실측 |
|---|---|---|
| 표적과의 상관 +0.260 -> +0.477 (`ws5gt`) | 좋아짐 | **-17.8** |
| 축약 대리모형 +76 (구종 성향) | 큼 | **+0.9** |
| 반쪽 분할 신뢰도 교정점의 97~125% (`condsit`) | `cond_pc` 급 | **-7.3** |

셋 다 내가 만든 것이고 셋 다 틀렸다. **후보 생성용으로만 쓰고 판정에는 쓰지 말 것.**
(반쪽 분할 신뢰도는 그래도 값어치가 있다 — `batter x season` 이 -0.008 이라는 것이
`wsbat` 의 정체를 밝혔고, 커버리지를 곱하면 `pitcher x batter` 의 리더보드 -3.51 을 맞힌다.
**설명에는 쓰되 예측에는 쓰지 말 것.**)

### ⛔⛔ CPU 학습의 이득이 사라졌다 — 피처가 좋아지자 0 이 됐다 (2026-08-24)

CPU(MVS + border_count 254)는 두 번 실측으로 이득이 확인된 축이었다.
그런데 `wseason5`+`wsbat` 위에서 다시 재니 **음수**다.

| | GPU 30모델 | CPU | 차이 |
|---|---|---|---|
| v9m 시절 | 996.998 | 1002.171 (30모델) | **+5.17** |
| v10w 시절 | 1058.718 | 1066.594 (30모델) | **+7.88** |
| **v10wb 시절** | **1088.832** | **1085.883 (20모델)** | **-2.95** |

모델 수 보정(10->30 이 실측 +3.08, 로그 보간으로 20->30 = +1.14)을 얹어도
CPU 30모델 환산이 1087.0 이므로 **CPU 전환 효과 = -1.8** 이다.

**메커니즘은 이미 claude.md 에 적혀 있었다.** 3장 'CPU 가 6배 과대평가된 이유':
> `border_count=254`(더 촘촘한 분할 경계)와 `MVS`(분산 축소 샘플링)는 둘 다
> **표본이 적을 때 더 이득인 추정 효율 기법**이다. 데이터가 충분하면 이미 잘
> 추정돼 있어 덜 중요해진다.

`wseason5`(+61.72)와 `wsbat`(+30.11)이 들어가면서 모델이 훨씬 잘 지정됐고,
샘플링·구간화 정교화가 기여할 여지가 사라졌다. **같은 원리가 이득을 만들었다가
같은 원리로 이득을 없앴다.**

**실무 규칙: CPU 학습 경로를 버린다.** GPU 63분 대 CPU 4.5시간 x 3 인데
이득이 0 이다. 남은 기간 본 학습은 전부 GPU 로 한다.

⚠️ 더 일반적인 교훈: **과거에 실측된 이득도 피처가 바뀌면 다시 재야 한다.**
'실측 두 번' 을 근거로 +6~8 을 예측했는데 -2.95 였다.

### 🐛 `pkill` 이 없어서 대기열 프로세스가 10개까지 쌓였다 (2026-08-24)

`tools/queue_push.sh` 를 우선순위 바꿀 때마다 재시작하면서
`pkill -f queue_push 2>/dev/null || true` 로 기존 것을 죽인다고 생각했는데,
**이 환경에 `pkill` 이 없다** (`command not found`). `|| true` 때문에 조용히 넘어갔다.

결과: 대기열 프로세스가 **10개** 동시에 살아서 각자 독립적으로 push 했다.
push 는 새 버전을 만들며 **돌던 커널을 죽이고 처음부터 재시작**시키므로,
`wbcc`(CPU 4.5시간)가 v7 까지 가면서 8시간을 날렸다.
캐글 화면에서 버전이 쌓인 것을 사용자가 발견해서 알았다.

**조치**: PowerShell `Get-CimInstance Win32_Process` 로 찾아 전부 종료.
스크립트도 RUNNING/COMPLETE 면 건너뛰도록 고쳤다.
**그리고 대기열 자동화 자체를 쓰지 않기로 했다** — 슬롯이 나면 직접 올린다.

⚠️ 교훈: 이 환경에서 `pkill`/`pgrep` 은 없다. 프로세스 관리는 PowerShell 로 할 것.

### 🐛 커널 재푸시가 옛 세션을 **안 죽이고 남긴다** — 슬롯이 조용히 샌다 (2026-08-24)

`kmix24` 의 `wsmixd` 크래시를 고쳐 v2 를 푸시했더니 **v1 이 계속 돌았다.**
사용자가 캐글 화면에서 발견했다 ("kmix24는 왜 두개가 돌아?").

- **`kaggle kernels status` 는 최신 버전 상태만 보여준다.** 좀비 세션은 API 로
  안 보인다. 커널 4개만 도는 줄 알았는데 실제로는 5슬롯이 차 있었고,
  다음 push 가 `Maximum batch CPU session count of 5 reached` 로 막혀서야 알았다.
- 8/24 오전 `wbcc` 사고 때 "push 는 돌던 커널을 죽이고 처음부터 재시작시킨다" 고
  적었는데, **죽이지 않고 병렬로 남기는 경우도 있다.** 둘 다 일어난다.
- ⛔ **`kaggle` CLI 에는 취소 기능이 없다** (`list/files/init/push/pull/output/status`).
  웹 UI 에서 사람이 눌러야 한다.

**실무 규칙**: 돌고 있는 커널에 재푸시하지 말 것. 버그를 고쳐야 하면
**다른 태그로 새 커널을 만들고** 옛것은 웹에서 취소한다. 그리고 push 가
슬롯 초과로 막히면 **좀비를 먼저 의심할 것** — API 숫자를 믿으면 안 된다.

### ❌ prev-game 시즌 경계 보정 — 오염은 실재하지만 고쳐도 +0.98 (2026-08-24)

오염 자체는 실측으로 확인됐다:
```
prev5 에 작년분 섞인 행 22.6% / prev3 14.5% / prev1 5.3%
당해 0~60구 & 이력있는 투수 82,826행(5.6%)에서 prev5 단독 스킬 -3,224
```
(A) 수준 디트렌드 + (B) `cross_N`(작년분 비율)을 둘 다 주고 GPU 본 학습.

| | 2023 스크리너 | 리더보드 |
|---|---|---|
| `wsboth` | 628.5 | 1,088.83 |
| **`prevfix`** | **640.0 (+11.5)** | **1,089.81 (+0.98)** |

**+0.98 은 노이즈 바닥(±1.5) 안이다.**

⛔ **내 논거가 틀렸다.** 2026-08-22 의 +6 을 무효라고 본 근거가
"그때는 `wseason5` 이전이라 모델이 `w_n` 을 몰랐다 = (B)를 배울 재료가 없었다"
였는데, **`w_n` 만 있으면 트리가 스스로 '당해 투구수가 적으면 prev5 를 믿지 마라'
를 학습한다.** 내가 추가한 것은 투수별 등판 크기로 그 판단을 정교화한 것뿐이고,
그 증분은 1점이었다. **1장 규칙(트리가 만들 수 있는 것은 0)의 한 사례다** —
`w_n` 이 v10w 에 들어간 순간 이 축은 이미 닫혀 있었다.

교훈: '손상이 실재한다' 는 것과 '고치면 점수가 오른다' 는 다른 이야기다.
`game_type` 제거(-46.7)와 같은 구조다 — 모델이 이미 보완하고 있었다.

### ⭐⭐ `wsbat`/`wseason5` 의 정체 재해석 — '시즌별 폼' 이 아니라 '커리어 추정치 정화' 였다 (2026-08-23)

`tools/eda33.py` 로 정보원별 천장을 반쪽 분할 신뢰도로 재고 **커버리지까지 곱했다.**

| 축 | 커버리지 | 신뢰도 | 신호 std | x커버 |
|---|---|---|---|---|
| **pitcher (커리어)** | 99.7% | **0.637** | **0.0408** | **0.0407** |
| pitcher x season | 98.7% | 0.235 | 0.0199 | 0.0196 |
| **batter (커리어)** | 99.6% | 0.227 | 0.0179 | 0.0178 |
| **batter x season** | 98.5% | **-0.008** | **0.0000** | **0.0000** |
| pitcher x season x 월 | 90.4% | 0.194 | 0.0256 | 0.0231 |
| pitcher x season x 전후반 | 97.4% | -0.089 | 0.0000 | 0.0000 |
| pitcher x batter (맞대결) | **13.9%** | 0.195 | 0.0328 | **0.0046** |
| pitcher x 타자손 | 99.2% | 0.195 | 0.0163 | 0.0162 |
| pitcher x 이닝 | 94.0% | 0.060 | 0.0114 | 0.0107 |
| batter x 투수손 | 98.9% | 0.027 | 0.0063 | 0.0062 |

**★ `batter x season` 의 신뢰도가 -0.008 이다 — 타자의 시즌별 폼은 전부 표본 잡음이다.**
그런데 `wsbat` 은 리더보드 **+30.11** 을 냈다. 모순이 아니라 **메커니즘이 다른 것**이다.

`wsbat` 이 준 것은 '이 타자의 올해 폼' 이 아니라
**'옛 시즌 리그 수준에 오염되지 않은, 그 타자의 안정적 실력 추정치'** 다.
`asof_batter_*_rate` 는 2019(리그 .5647)부터 섞인 커리어 누적이라 그 자체가 편향돼 있었고,
복원이 그 편향을 씻어낸 것이다.

투수측도 같다 — `pitcher (커리어)` 신호 0.0408 이 `pitcher x season` 0.0199 의 **두 배**다.

⛔ **따라서 `group_oof` 의 '당해 시즌 폼을 아는 것 = 301점' 해석은 과장이었다.**
그 실험은 그 투수의 당해 시즌을 학습에서 **통째로** 뺐으므로 (a) 시즌 고유 성분과
(b) 안정적 실력을 추정할 표본을 같이 없앴다. 값어치의 대부분은 (b) 였다.

**실무 규칙 두 개**
1. **'최근성' 을 더 짜내는 방향은 기대값 0 이다.** 전후반 분할도 -0.089 로 0 이고,
   최근성 가중은 이미 5연패다. 이 축은 완전히 닫혔다.
2. 남은 방향은 **추정기의 편향 제거**다 — 그게 wseason/wsbat 이 실제로 한 일이다.

**부수 확인 (전부 닫힘)**
- `pitcher x batter` 는 신호 0.0328 로 커 보이지만 **커버리지 13.9%** 다 (40구+ 붙은 짝만).
  실효 0.0046 으로 꼴찌권이고, 리더보드 실측(`cond_pb` **-3.51**)과 정확히 맞는다.
  ⚠️ **고차원 축은 커버리지를 곱하지 않으면 반드시 오독한다.**
- `pitcher x season x 월` 은 실효 0.0231 로 2위지만 **test 에서 만들 수 없다**
  (2025년 5월 그 투수 폼은 test 다른 행을 봐야 안다 = 규정 위반).
  배포 가능한 버전(`asof_pitcher_prev{1,3,5}_game_*`)은 이미 모델에 있다.
- `pitcher_team`(포수 대리)은 팀이 10~25개뿐이라 셀 13개로 **이 방법으로는 판정 불가**.

### 🔧 eda32 — 투수 x 상황 축의 반쪽 분할 신뢰도 (2026-08-23)

주최측 '코치의 네 가지 질문'(주자/카운트/후반/최근흐름) 중 우리 `cond_*` 는
카운트와 좌우뿐이다. 나머지를 학습 없이 쟀다 (투수 주효과를 뺀 뒤):

| 축 | 신호 std | count_adv 대비 |
|---|---|---|
| batter_hand (= cond_ph) | 0.0227 | — |
| 점수차 구간 | 0.0180 | 125% |
| base_state | 0.0162 | 113% |
| **count_adv (= cond_pc, LB +27)** | **0.0144** | (교정점) |
| 이닝 구간 | 0.0139 | 97% |
| li / 아웃 / RISP | 0.0074~0.0097 | 51~67% |

방법론 검증: 우리가 이미 쓰는 축(batter_hand)이 신뢰도 0.552 로 압도적 1위다.
후보 `condsit` / `wsbsit` 으로 측정 중.

### ⛔⛔⛔ 2024 홀드아웃은 **거짓 음성**도 낸다 — `wsbat` 이 증명했다 (2026-08-23)

지금까지 2024 홀드아웃의 실패는 전부 '거짓 양성'(홀드아웃 +27 인데 리더보드 -22)이었다.
**이번엔 반대다.**

| | 2024 홀드아웃 | 2023 홀드아웃 | **리더보드 실측** |
|---|---|---|---|
| `wseason5` | 885.6 | 556.8 | +61.72 |
| **`wsbat` (타자측 복원)** | **888.6 (+3.0)** | **628.5 (+71.7)** | **+30.11** |

**2024 는 노이즈라고 했고, 2023 은 크다고 했고, 리더보드는 크다고 했다.**
2023 이 방향을 맞혔고 2024 는 완전히 놓쳤다.

전달률: 2023 기준 **0.42**, 2024 기준으로는 10배다 (환산 불가).

⛔ **가장 비싼 함의: 우리가 2024 홀드아웃에서 '노이즈' 로 기각한 후보들이 살아 있을 수 있다.**
그동안 "+50 미만은 전부 노이즈로 버린다" 를 코드에 박아두고 후보 20개 이상을 버렸다.
`wsbat` 은 그 기준으로 **+3.0** 이었다.

**새 규칙**: 2024 에서 0 근처인 후보는 **2023 에서 한 번 더 재고** 버릴 것.
둘 중 하나라도 크게 양수면 리더보드로 판정한다 (제출은 남아돌고 후보가 병목이다).

### ⭐ 타자 축은 닫혀 있지 않았다 (2026-08-23)

`claude.md` 는 "`cond_p`(투수) +27 vs `cond_b`(타자) +1 — **27배 비대칭**. 제구 성공에
타자는 거의 기여하지 않는다" 고 단정했다. **그 근거가 전부 2024 홀드아웃이었고 틀렸다.**

`wsbat` 이 +30.11 이다. **투수측 복원(+61.72)의 절반**이다.

⚠️ 따라서 **타자측 조건부통계(`cond_b`/`cond_bc`/`cond_bp`, 2024 에서 +1)를 2023 에서
재측정할 것.** 같은 프로필이다.

### ⭐ 2023 이 두 번째 검증 시즌으로 살아났다 (2026-08-23)

F(퓨처스) 행을 **채점에서 빼면** 정상 작동한다. 전에는 전 trial 이 음수(-937~-1360)라
쓸 수 없던 시즌이다 — 원인이 라벨 체제 변경이라는 진단이 맞았다.

```
2023 홀드아웃 (F 제외, 2019~22 학습)
  base        374.7
  wseason5    556.8      +182.1
```

**`wseason5` 의 세 번째 독립 확인이다**: 2024 홀드아웃 +72.2 / 2025 리더보드 +61.72 /
2023 홀드아웃 +182.1. 다른 시즌·다른 학습분에서 같은 방향이다.

**이제 후보를 두 시즌 합의로 거를 수 있다.** 단일 시즌 홀드아웃으로 네 번 헛다리를
짚은 것이 이 프로젝트 최대 병목이었다. 사용법: `SCREEN_HOLDOUT=2023` (F 자동 제외).

### ❌ 밤샘 후보 다섯 — 전부 노이즈이거나 음수 (2026-08-23)

| 후보 | 점수 | wseason5(885.6) 대비 |
|---|---|---|
| `wsbat` 타자측 당해시즌 복원 | 888.6 | +3.0 |
| `ws5c10` shrink C=10 | 886.5 | +0.9 |
| **`wseason5` (기준, C=100)** | **885.6** | — |
| `ws5c30` | 878.4 | -7.2 |
| **`ws5nocond` cond_* 제거** | **873.2** | **-12.4** |
| `ws5c300` | 860.9 | -24.7 |

**① shrink C 축 종료.** C=10/30/100 이 ±7 안에 몰리고 C=300 만 -24.7 이다.
최적점이 넓고 현행 C=100 이 이미 그 안이다. 61점짜리 피처의 유일한 손잡이였는데
손잡이가 아니었다.

**② `cond_*` 는 잉여가 아니다 — 가설 기각.** '당해 시즌 복원이 들어간 뒤로 `cond_*` 는
거의 잉여일 수 있다' 고 적어뒀던 것이 틀렸다. 빼면 -12.4 다.

**③ 타자측 +3.0 은 노이즈.** eda10 대리모형이 +10 이라 했는데 실측이 이 정도다.
투수 5개(+83 -> 실측 +72.2)와 달리 타자는 대리모형조차 작았고 실측은 0 이다.

### ⛔⛔ 전달률은 하나가 아니다 — 변경의 **종류**에 달렸다 (2026-08-23 재정정)

`wseason5` 실측이 '0.3' 을 깼다.

| 변경 | 종류 | 홀드아웃 | 리더보드 | 전달률 |
|---|---|---|---|---|
| CPU 전환 (MVS + bc254) | 파라미터 | +26 | +5.17 | 0.20 |
| 월/요일 제거 | 피처 제거 | +15 | +4.52 | 0.30 |
| 트랙맨 매핑 재구축 | 버그 수정 | +7~9 | +3.56 | 0.40~0.51 |
| **`wseason5` 당해 시즌 복원** | **새 정보** | **+72.2** | **+61.72** | **0.855** |

**앞의 셋은 전부 "같은 정보를 다르게 쓴다" 였고, `wseason5` 는 "다른 행에서 정보를 끌어온다" 였다.**
홀드아웃이 체계적으로 과대평가하는 것은 규제·용량·추정효율 계열이다 (학습 데이터가 18% 적은
소표본 체제라 그쪽에 가산점을 준다 — 3장 'CPU 가 6배 과대평가된 이유' 와 같은 원인).
**새 정보는 표본 크기와 무관하므로 그 편향을 받지 않는다.**

**실무 규칙**: 후보의 기대 리더보드 이득을 환산할 때
- 새 정보(다른 행/다른 테이블에서 끌어옴) -> **x0.85**
- 버그 수정 -> x0.45
- 파라미터·피처 제거 -> **x0.25**

```
현재 1058.72 -> 100인 컷 1119.20 = 격차 60.5
새 정보 부류로 환산 -> 필요한 홀드아웃 +71   (wseason5 하나가 +72.2 였다)
```

### ⭐⭐⭐ `game_type=F` 는 퓨처스이고, 우리 드리프트 서사는 여기서 틀렸다 (2026-08-23)

독립 재현으로 확인했다.

| game_type | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---|---|---|---|---|---|
| F (10.9%) | .6892 | .5878 | .7038 | .7087 | **.4729** | .4593 |
| R (89.1%) | .5495 | .5269 | .5128 | .5037 | .5031 | .4897 |

```
2022->2023 낙폭:   전체 -0.0290 ("6년 중 최대")     R 전용 -0.0006
2025 외삽:         전체  0.4622                    R 전용  0.4849  (F 비중 11.8%)
                                                   혼합    0.4803
```

**"6년 중 최대 낙폭" 은 거의 전부 F 라벨 체제 변경이었다.** 1장의 드리프트 표는
R/F 를 섞은 값이라 이 지점에서 오도한다.

**파급 셋**
1. **2023 홀드아웃 Optuna 참사가 설명된다.** 2019~22 로 학습하면 F 행을 .68 로 보는데
   실제는 .473. 산술로 10.5% x (0.207)^2 / 0.25 = **-1,800점** — 실측 -937~-1360 과 자릿수가 맞는다.
   **F 를 처리하면 2023 이 두 번째 검증 시즌으로 살아난다.** 검증이 2024 하나뿐인 것이
   이 프로젝트 최대 병목이므로 값어치가 크다.
2. **`cond_*` 와 `wseason` 의 디트렌드가 오염돼 있다.** 둘 다 '그 시즌 리그평균' 하나만
   빼는데 2019~22 F 행은 **+0.12~0.18** 의 오차를 받는다. 2024 투수 편차와의 상관:
   현행 +0.260 / (시즌 x game_type) **+0.477** (투구수 가중 +0.420 -> +0.624).
   후보 `ws5gt` / `condgt` / `bothgt` 로 측정 중.
3. ⛔ **`game_type` 을 제거하는 것은 답이 아니다** (`nogt` -46.7 / `dropoldF` -39.9).
   재학습하면 `season` 과 결합해 보완한다. **고칠 곳은 디트렌드 기준이지 피처가 아니다.**

### 🐛 트랙맨 `game_date` 는 포맷이 두 가지다 (2026-08-23)

```
2019~2021   03/29/2019      %m/%d/%Y 파싱 NaT:  2019~21 = 0%
2022~2024   2022-04-05                          2022~24 = 100%   (전체 53.4%)
```

`experiments/archive/aimers_v9m.ipynb` / `experiments/v10w/aimers_v10w.ipynb` / `tools/mk_v8ab.py:73` 가 `format='%m/%d/%Y'` 로
읽는다. **하필 test 에 가장 가까운 세 시즌이 전부 NaT 다.**

- `_d` 는 `build_rest_foul` 안에서만 쓰이고 **`USE_REST_FOUL = False`** 이므로
  **살아있는 제출은 안 망가져 있다.**
- 다만 **`s1r`(휴식·등판밀도, -6.85) 기각은 날짜가 전부 NaT 인 상태로 잰 값이라 무효다.**
  재측정 여지가 있다.

### ✅ train.csv 는 경기 단위로 완전히 복원된다 (2026-08-23)

`(season, month, dow, game_type)` 변화 + 이닝 하락으로 자르면 (독립 재현 확인):

```
R:  720 720 721 720 720 721      <- KBO 정규시즌 = 10팀 x 144 / 2 = 720
F:   90  76  86 104  89 101
총 4,868경기 | 경기당 투구 중앙 300
검증) 복원 등판 45,862개에서 asof_pitcher_prev1_game_success_rate 유일 비율 1.000000
검증) 투수 내 asof_pitcher_n 증분 +1 비율 1.000000
```

**1장의 "경기 ID도 날짜도 없어 경기내 피로도 복원 불가능" 은 train 에 대해서는 틀렸다**
(test 는 여전히 맞다 — 규정 금지).

다만 **피로도 자체가 거의 없다**: 선발 등판 내 1-15구 +0.0055 -> 90구+ -0.0023, 타순 반복도 평평.
**잃고 있던 게 아니었다.**

### 📐 train x trackman 투구 1:1 정렬이 된다 — teacher 상한 +581~+702 (2026-08-23)

경기 안 `(inning, T/B, balls, strikes, outs)` 상태열 지문으로 맞춘다.
전체 일치 2,702경기 / 811,244투구(55.0%), 앞 40구 지문까지 확장하면 4,222경기(86.7%).
독립 검증(키에 안 쓴 컬럼): 투수손 99.96% / 타자손 99.77%.

부산물 **gold 매핑**: 투수 777명, 2024 투구가중 커버리지 **99.95%** (v2 는 598명 / 94.31%).
⚠️ 단 gold 에는 `season` 컬럼이 없고 `pitcher_trackman_id` 가 유일하지 않다.
`step15` 는 season 이 없으면 trackman_id 단독으로 merge 하는데 **그게 1.6배 팽창 사고의
경로다.** 교체는 무위험이 아니다 — 시즌 키를 붙여서 넣을 것.

**⚠️ 누수 검증을 반드시 통과시킬 것 (`tools/eda28.py`).** 연속 측정 8개는 (투수 x 경기)를
사실상 지문화하므로 무작위 fold 는 같은 경기의 다른 투구를 통해 경기 효과를 훔쳐 온다.

| 우리 96피처 위 증분 | 무작위 fold | **경기그룹 fold** | 생존율 |
|---|---|---|---|
| 그 투구 측정 8개 | +778 | **+581** | 75% |
| 구종군만 | +392 | +376 | 96% |
| **투수평균 뺀 편차 8개** | +818 | **+702** | 86% |

**누수는 25% 뿐이고 본체는 진짜다.** 편차가 원값보다 잘 살아남는 것은
'릴리스 흔들림 -> 미스' 라는 물리적 해석과 일관된다.

⚠️ **그래도 증류 이득은 별개다.** teacher 가 보는 정보는 student 피처에 대한 조건부
기대값을 바꾸지 않으므로 표적이 같고, 이득 경로는 분산 감소뿐인데 우리는
**in-sample 2.232% ~= OOF 2.21%** 로 분산에 묶여 있지 않다. 유일한 근거는
**teacher/student 예측 상관 0.76** (트리 계열은 0.92~0.97 에서 막혔고 NN 최선이 0.78).

### ❌ 구종 성향 축 종료 — 네 후보 전부 0 (2026-08-23)

`step16_calc_expected_difficulty` 는 `exp_fb_prob / exp_br_prob / exp_off_prob`
= P(구종 | 투수, count_advantage) 를 계산해놓고 **return 하지 않는다** —
릴리스 std 와 곱해 스칼라 하나로 뭉갠 뒤 버린다. 그래서 두 방향을 다 쟀다.

| 후보 | 점수 | base(813.4) 대비 | **wseason5(885.6) 대비** |
|---|---|---|---|
| `pmix` (12칸 카운트 x 타자손, shrink) | 825.4 | +12.0 | — |
| `xprob` (step16 세 확률을 그대로 export) | 823.2 | +9.8 | — |
| `wspmix` | 886.5 | +73.1 | **+0.9** |
| `wsxprob` | 874.9 | +61.5 | **-10.7** |

**단독으로도 노이즈고 `wseason5` 위에서는 정확히 0 이다. step16 을 고칠 이유가 없다.**

⚠️ **이 축을 판 이유가 EDA 오류였다.** 소규모 대리모형에서 성향이 **+76** 으로 나왔는데,
그 base 에 **투수-시즌 평균 물리량 8개**가 빠져 있었다(그게 레퍼토리를 담는다).
**4-11 이 "피처를 뺀 축약 모델로 설계 결정을 내리지 말 것" 이라고 적어둔 바로 그 함정이다.**
EDA 로 후보를 고를 때 base 는 **실제 96피처에 가깝게** 만들 것.

### ⭐⭐ 예측 규칙 정정 — **2023 x 0.3** 이 맞다. 2024 는 거짓 음성을 두 번 냈다 (2026-08-26)

`mcaux` 실측이 '작은 쪽' 규칙을 깼다 (2024 증분 +2.8 인데 LB **+12.85**).
여섯 사례를 다시 맞춰보면 **2023 에 0.3 을 곱하는 것**이 훨씬 잘 맞는다.

| 변경 | 2024 | 2023 | 0.3x2023 | **실측 LB** |
|---|---|---|---|---|
| `wseason5` | +72.2 | +182.1 | +55 | **+61.7** |
| `wsbat` | **+3.0** | +71.7 | +22 | **+30.1** |
| `auxrevns` | +22.5 | +55.0 | +17 | **+13.5** |
| **`mcaux`** | **+2.8** | +46.6 | **+14** | **+12.85** |
| `auxnsb` 증분 | -0.3 | +15.1 | +4.5 | +0.002 ❌ |
| `auxrev` | +40.2 | **-18.1** | ~0 | +4.14 ❌ |

**4건이 맞고, 틀린 둘은 크기가 작은 경우다.** 2024 는 `wsbat`(+3.0 -> +30.1)과
`mcaux`(+2.8 -> +12.85)에서 **두 번 거짓 음성**을 냈다.

⛔ 8/25 에 정리한 '두 시즌 최솟값 x0.6' 은 `auxrev` 한 사례(2023 이 음수)에
과적합한 것이었다. **정정: 2023 을 주 지표로 쓰고 x0.3 을 곱한다.
2024 가 0 근처여도 2023 이 크면 버리지 말 것.**

⚠️ **가장 비싼 함의**: 이 프로젝트는 오랫동안 2024 홀드아웃만으로 후보를 걸렀고
("+50 미만은 전부 노이즈로 버린다" 를 코드에 박았다) 그 기준으로 20개 이상을
버렸다. `wsbat` 이 그 기준으로 +3.0 이었고 LB +30.1 이었다.
**2024 기준으로만 기각한 후보는 2023 에서 재측정할 것.**

### ⛔⛔⛔ 타겟 세분화는 **결과 축에서만** 통한다 — 구종 축은 LB **-82.3** (2026-08-27)

```
v10wz (5분류 = 실패유형)          1,116.1659
ptc6  (2분류 x 구종 3종 = 6분류)  1,033.8371   = **-82.33**
```

`ptc6` 는 5분류를 구종으로 **교체**한 판이다. 이진 기준선과 비교하면 축이 분리된다:

```
v10wq (이진 + aux)   1,103.3169
ptc6  (이진 x 구종)  1,033.8371   = **구종 축 단독으로 -69.48**
```

**실패유형 분해는 +12.85 인데 구종 분해는 -69.5 다. 같은 '타겟을 쪼갠다' 인데 부호가
반대이고 크기가 5배다.** 차이는 쪼개는 **축의 성질**이다.

| | 쪼개는 축 | 성질 | 결과 |
|---|---|---|---|
| `multicls` | 몰림 / 반대 / 둘다 / 크게벗어남 | **결과의 하위 분할** | **+12.85** |
| `ptc6` | 직구 / 변화구 / 오프스피드 | **결과와 직교하는 잡음 차원** | **-69.5** |

- 실패유형은 우리가 예측하려는 사건(제구 실패) **안을** 나눈다. 유형별 드리프트가
  서로 반대라(eda41) 나누면 상쇄되던 과정이 분리된다 — 타겟이 **정밀해진다**.
- 구종은 타겟 밖의 축이다. 다중분류로 만들면 모델이 1000그루의 용량을 **"이 투수가
  다음에 무슨 공을 던질까"** 를 가르는 데 쓴다. 그건 투수 레퍼토리라 쉽고 강한 신호라
  트리가 그쪽으로 쏠리는데, **추론 시점에 제구 성공을 맞히는 데는 기여가 0 이다.**
  `auxrev` 의 보조모델이 중요도 31% 를 `season x game_type` 에 쓰고 죽었던 것과 같은
  '용량 유출' 이다.

**⛔ 그리고 이건 예고돼 있었다. 구종 축은 이미 네 번 0 이었다.**

| 측정 | 결과 |
|---|---|
| teacher 실험: 실제 던진 구종 +478 인데 **구종 성향**은 | **정확히 0** (E 894 vs B 906) |
| eda10 대리모형: 구종 3개 증분 | **+0** |
| `wsmix` (구종 배합 당해시즌 복원) | **-16.6** (2023) |
| `wspmix` / `wsxprob` (구종 성향 피처) | +0.9 / -10.7 |

claude.md 에 "**배합은 성향이라 이미 base 에 녹아 있다**" 고 적혀 있었다.
그런데도 "타겟으로 쓰는 건 다르다" 는 논거로 GPU 2.5시간과 제출 한 장을 썼다.

**실무 규칙**: 한 축에서 **독립적인 0 이 셋 이상** 쌓이면, 그 축을 쓰는 *방식*을
바꾸는 후보도 기대값 0 으로 잡을 것. 신호가 없는 것과 신호를 잘못 쓰는 것은 다르고,
위 넷은 전부 전자를 가리키고 있었다.

⛔ **`ptc15`(5분류 x 구종 = 15분류)는 제출하지 않는다.** 학습은 끝났지만
산술이 명확하다: `1,116.17 - 69.5 = 1,047` 근처이고, 15칸으로 쪼개면 가장 작은 칸이
전체의 **0.47%**(둘다 x 오프스피드 6,887행)라 파편화로 더 나빠진다.
결과 축(+12.85)과 잡음 축(-69.5)을 더하는 것이므로 합은 크게 음수다.

### ⛔⛔ 판정 축 7분류 -6.69 · `bsx` 단독 -7.74 — 근거 두 개가 같이 무너졌다 (2026-08-28)

```
v10wzc (최고점)   1,121.8905
v10wj7  7분류     1,115.2046   **-6.69**
v10wbs  bsx 단독  1,108.4304   **-7.74** (v10wz 1,116.17 대비)
```

**① 드리프트 배율은 점수를 예측하지 못한다.**
`multicls`(+12.85)의 기전을 "유형별 드리프트가 서로 반대라 상쇄되던 과정이 분리된다"
로 보고, 같은 계산을 판정 축(볼/스트라이크/인플레이)에 했더니 **더 컸다**:

```
실패유형 5분류   순변동 0.060 / 총변동 0.144 = 2.4배   -> LB +12.85
판정 6칸         순변동 0.060 / 총변동 0.164 = 2.74배  -> LB **-6.69**
```

설계는 깨끗했다 — 성공(52%)만 3분할해 최소 칸이 5분류와 같은 3.41% 로 유지됐고
(파편화 0), 판정은 결과의 하위 분할이라 `ptc6`(-82.3)의 직교 잡음 축 함정도 아니었다.
그런데 음수다. **배율이 크다고 좋아지는 게 아니므로, `multicls` 가 통한 이유는
드리프트 분해가 아니다.** 진짜 기전은 여전히 모른다.

**② 2023 스크리너의 신호 바닥은 ±10 이 아니라 +40 이다.**

| | 2023 | LB |
|---|---|---|
| wseason5 | +182 | +61.7 |
| wsbat | +72 | +30.1 |
| auxrevns | +55 | +13.5 |
| mcaux | +47 | +12.85 |
| *— 위까지 x0.3 규칙이 맞는다 —* | | |
| **bsx** | **+16** | **-7.74** |
| auxnsb | +15 | +0.002 |
| cnt12h | +4 | +5.72 |

**2023 이 +40 아래면 부호조차 못 맞힌다.** `cnt12h` 가 맞은 것은 운이었다 (n=1).
⛔ 스크리너는 이제 후보 선별에도 쓸 수 없다. 큰 발견(+40 이상)을 확인하는 용도만 남는다.

### ⛔⛔ 캘린더 축 종료 — 월 단독 **-10.18**, 월+요일 **+0.08** (2026-08-29)

`w_n`(당해 시즌 투구수)은 캘린더 없이는 해석이 안 된다. 데이터로는 명확하다
(2024, R, 같은 `w_n` 300~600 인 행):

| 월 | 그 투수의 시즌 총투구 중앙값 |
|---|---|
| 4월 | **2,397** (에이스, 이제 시작) |
| 6월 | 988 |
| 9월 | **602** (시즌 끝나가는 변두리 자원) |

월별 `w_n` 중앙값도 43(3월) -> 1,956(10월)로 **45배** 퍼진다. 즉 `w_n` 은
`시즌진행도 x 등판페이스` 의 곱인데 모델은 곱만 본다. `DROP_CAL` 은
**v9m(996점, 8/22)** 에서 +4.52 로 채택된 것이고 그때는 `w_n` 이 없었으므로,
"과거에 실측된 이득도 피처가 바뀌면 다시 재야 한다"(CPU 전환 +26 -> -2.95)에
정확히 해당한다고 보고 재측정했다.

```
             요일 없음        요일 있음
월 없음      1,121.8905      (미측정)
월 있음      1,111.7101      1,121.9711
             ────────────────────────────
             상호작용 +10.26
```

**결과: 가설 기각. 최선이 +0.08(노이즈)이고 월 단독은 -10.18 로 진짜 음수다.**

- 왜 0 인가 (추정): `DROP_CAL` 은 원본 컬럼만 지웠고 **step1 의 두 플래그는 살아 있다** —
  `is_heat_wave_game`(월 7,8) + `is_weekend_day_game`(월 4,5,9,10 & 주말)이
  사실상 {3,6}/{4,5,9,10}/{7,8} 3분할을 준다. 거친 진행도는 이미 있었고
  세밀한 8칸은 새 정보가 아니었다.
- ⛔ **월 단독이 -10.18 인 기전은 모른다. 지어내지 않는다.**
  이 프로젝트에서 만든 기전 설명은 `multicls`(드리프트 배율 -> j7 -6.69 로 반증),
  `s1d`(정규화가 원인 -> s1n 이 28%만 설명), `opt254`(가법 추정 -> 부호 반전)
  세 번 다 실측에 깨졌다.

⚠️ **비가법성 4번째 확인 — 이번엔 상수를 공유하지도 않는 쌍이다.**
기존 사례는 v8 절개(상호작용 24%), `ws5gt`/`condgt`/`bothgt`(-17.8/-8.2 인데 둘 다면 -1.8),
범주형 조합(bsx +16.2 + cnt12 +3.4 = -1.2). 이번 것은 **함께 제거된 이력**밖에
공유하지 않는데 상호작용이 +10.26 이다.
**함께 넣거나 뺀 적이 있는 피처군은 반드시 전부 같이 재라. 한 개만 재서 나온
부호를 그 피처의 효과로 읽으면 안 된다.**

### ⛔ 용량 축은 5분류에서도 닫혔다 — 이번엔 실측이다 (2026-08-29)

`iterations=1000` / `depth 8` 은 **이진 타겟 시절 v5(990점, 96피처)** 에서 리더보드로
포화를 확인한 값이다. v10wz 부터 잎이 5차원이니 `optbest` 교훈("다른 파라미터
영역에서 잰 효과를 더하지 말 것", 그때는 부호까지 뒤집혔다)이 적용된다고 보고,
`tools/mk_ntree.py` 로 **재학습 0 / GPU 0** 에 `ntree_end` 만 걸어 실측했다.

```
v10wzc (1000)   평균 0.477048  std 0.036549   1,121.8905
n700            평균 0.479601  std 0.034240   1,120.4225   = -1.47
n400            평균 0.479922  std 0.031149   (미제출)
```

**★ 총합이 노이즈 안이어도 예측을 미리 박아두면 잔차가 답을 준다.**

```
재중심화 어긋남  = 100000 x Δ평균² / r(1-r) = 100000 x 0.002553² / 0.2494 = -2.6
수축 (λ = 0.034240/0.036549 = 0.937, λ*≈0.96)
                = (1.92λ - λ²)/0.9216 대비                              = +1.3
                                                                   ────────
예측 소계 (트리를 잘라도 신호는 안 잃는다고 가정)                        -1.3
실측                                                                  -1.47
                                                                   ────────
잔차 = 트리 700~1000 (300그루)의 실제 기여                             -0.17
```

**300그루가 0.17점이다. 이진 시절 같은 구간은 +4.74 였다.**
부스팅은 한계 기여가 단조 감소하므로 1000->1400 은 그 이하이고 과적합이면 음수다.
`aimers-v10wi1400`(`tools/mk_iters.py`)은 만들어두고 **올리지 않는다.**

부수 소득 둘:
- **재중심화 손실식이 리더보드에서 검증됐다.** eda38 의 A모형(오차 0.0236 -> 224점)과
  같은 식이 Δ0.002553 -> -2.6 을 맞혔다. 앞으로 평균이 움직이는 어떤 변경이든
  비용을 제출 전에 계산할 수 있다.
- **수축 모형 λ\*≈0.96 이 5분류에서도 유효하다.** 예측이 과대 퍼짐 상태가 아니다.

⛔ 나는 이 제출을 **하지 말라고 했다** ("기대 -1±1.5 라 아무것도 못 배운다").
틀렸다. 예측값이 있으면 노이즈 안의 측정도 잔차로 결론을 낸다.
**싼 실측을 기대 효과 크기를 근거로 말리지 말 것.**

### ⛔⛔⛔ 남은 격차의 대부분은 **원리적으로 도달 불가능**하다 (2026-08-29, `tools/eda49.py`)

`group_oof` 는 무작위 fold 2024 행 1,092 / 투수x시즌 그룹 791 이었고 현재 홀드아웃이
914 이므로 **178 이 남아 있었다.** 그 178 중 얼마가 잡을 수 있는 것인지 직접 쟀다.
무작위 fold 가 갖고 우리가 못 갖는 것은 하나뿐이다 — 그 투수의 **이 투구 이후** 기록.

2024, 같은 shrink(C=100, 리그평균)로 공정 비교한 **단독 피처** 스킬:

| 예측자 | 스킬 | 가중평균 n |
|---|---|---|
| 상수 r | -0 | — |
| **(c) 커리어 누적 = 운영측 `asof_pitcher_success_rate`** | **43** | 3,928 |
| **(a) 당해 시즌 진행분 = 우리가 가진 것** | **600** | 687 |
| **(b) 당해 시즌 전체 = 무작위 fold 가 사실상 갖는 것** | **724** | 1,374 |

```
(b) - (a) = +124   <- '이 투구 이후를 안다' 의 값어치. test 에는 원리적으로 없다
(a) - (c) = +557   <- wseason5 이 실제로 회수한 몫
```

**남은 178 중 124 가 도달 불가능 몫이다.** 단독 피처 비교라 전체 모델과 1:1 은 아니지만
자릿수는 분명하다 — **투수 정체성 채널에 남은 회수 가능분은 작다.**

시즌 진행도별로도 일관된다 (0~100구 +200 / 900+구 +62). 시즌 초에 진행분 표본이
얇아 격차가 크고, 그건 시간이 지나면 저절로 줄어드는 종류다.

⭐ **부수 확인: 운영측 `asof_pitcher_success_rate` 의 단독 스킬은 43 이다.**
당해 시즌 복원값(600)의 14분의 1. `wseason5` 가 왜 +61.72 였는지가 이 한 줄에 있다.

### ⛔ 이적(팀 변경) — 효과도 커버리지도 없다 (2026-08-29, `tools/eda47.py`)

`pitcher_team_id` 는 test 행이 담은 2025 정보이고, 타겟이 "포수가 요구한 곳에 던졌는가"
이므로 팀을 옮기면 요구를 내는 주체가 바뀐다. 모델은 이걸 볼 수 없다 — 2025 이적 투수는
학습에 없던 (투수, 팀) 조합으로 나타날 뿐이고 트리는 '바뀌었다' 를 표현할 수단이 없다.

```
투수 이적  드리프트 보정 변화량 차이 -0.00204  t = -0.36   (이적 51 / 700쌍)
타자 이적                        -0.00353  t = -0.77
2024 커버리지: 이적 투수 11명 = 투구의 **3.76%**
```

**효과가 없고, 있어도 커버리지가 3.76% 라 0.4점이다.** eda33 교훈(고차원 축은 커버리지를
곱하지 않으면 반드시 오독한다)이 그대로 적용된다.

### ⛔ 구장(홈팀) 효과 — 실재하지만 시즌 간 상관 0.21 (2026-08-29, `tools/eda48.py`)

claude.md 에 구장 얘기가 한 번도 없었다. 행 안에서 복원된다:
`top_bottom=T` 면 `pitcher_team_id` 가 홈팀, `B` 면 `batter_team_id` 가 홈팀.

```
투수-시즌 평균을 뺀 뒤의 구장 편차 std   0.00520   (투수 편차 std 는 0.047)
구장 13 은 t = -8.95 로 강하게 유의
인접 시즌 상관 평균                      **0.207**
  구장 13:  -0.0433 -0.0320 -0.0070 -0.0259 +0.0161 +0.0153   <- 부호 반전
홈/원정 차이                             t = -0.78 (없음)
천장 10.8점 x 상관 0.21  ->  약 2점
```

`wn`/`game_type` 세그먼트 상수를 죽인 것과 같은 패턴이다 — 표본 내 유의, 해 넘기면 소실.

⭐ 12(두산)와 13(LG)은 **같은 구장(잠실)** 인데 편차가 +0.0004 vs -0.0124 다.
즉 물리적 파크 효과가 아니라 **홈팀 효과**이고 모델은 팀 ID 로 이미 갖고 있다.

### ⛔⛔ `cond_*` 는 국소 최적이다 — 서로 다른 두 변경이 **같은 -4.75** (2026-08-29)

```
v10wzc    1,121.8905               피처 117
v10wcw    1,117.1450   **-4.75**   cond_* 에 표본두께 컬럼 4개 추가 (피처 121)
v10wc12   1,117.1520   **-4.74**   cond_pc/phc 의 키를 count_advantage 4단계 ->
                                    cnt12 12칸으로 교체 (피처 117, C 100/50 -> 35/17)
```

**피처 수도 오프셋도(-0.0690 vs -0.0679) 다른 두 판이 0.007 차이로 같은 자리에 떨어졌다.**
노이즈 바닥이 ±1.5 인데 독립적인 두 draw 가 이만큼 붙는 건 우연으로 보기 어렵다.

읽을 수 있는 것: **`cond_*` 는 국소 최적에 있고 어느 방향으로 건드리든 비용이 비슷하다.**
shrink C 축이 넓다는 것(10/30/100 이 ±7 안)과 `ws5nocond` -12.4 와 일관된다.
⛔ 기전은 모르고 두 점으로 단정하지 않는다. **이 축은 더 건드리지 말 것.**

`v10wc12` 의 논거였던 eda46 은 여전히 유효하다 — 투수 유리 카운트(28.4%)에 투수
신호가 타자 유리 카운트의 64% 있는데 우리는 45% 만 뽑는다. 다만 **그 결손을
`cond_*` 의 키 정밀화로는 메울 수 없다는 것이 실측됐다.**

구조 검증은 전부 정상이었다 (결측 31.7/32.1% 로 4단계 때와 같은 수준, 행 독립
1.11e-16, ws_*/wb_* 생존). 즉 구현 실패가 아니라 축 자체가 닫혀 있다.

### ⛔ `cond_*` 표본 두께 노출 — LB **-4.75**, '미해결' 항목이 닫혔다 (2026-08-29)

claude.md 가 유일하게 '미해결로 남겨둔다' 고 적어둔 항목이었다: `cond_*` 의 의미가
학습 시즌마다 다르다(2019 행은 과거 0시즌, 2024 행은 5시즌, test 는 6시즌)는 것.
`build_cond_table` 이 계산해놓고 **버리던** `_w`(그 조합의 과거 표본 수)를
`n/(n+C)` 로 4개 내보냈다. `prevfix` 의 기전("`w_n` 만 있으면 트리가 스스로
'표본이 얇으면 믿지 마라' 를 학습한다")을 `cond_*` 에 옮기는 것이 논거였다.

```
v10wzc  1,121.8905      피처 117
v10wcw  1,117.1450      피처 121      = **-4.75**
2024 홀드아웃 914 (v10wzc 도 914) / 오프셋 -0.0690 / 검증 3종 통과
```

**-4.75 는 노이즈 바닥(±1.5) 밖이다.** 이 축은 닫혔다.

⚠️ 기전은 모른다. 다만 `cond_p_w` 의 `n` 은 그 투수의 직전 시즌까지 누적 투구수이고
모델은 `asof_pitcher_n` 과 `w_n` 으로 그 차이에 이미 도달할 수 있다 — 새 정보가
아니라 **재조합**이었을 가능성이 있다(`auxrev` +40.2 -> +4.14 를 죽인 압축 부류).
가설이며 검증하지 않았다.

⛔ 실패 하나가 더 있었다. 첫 판(`v10wc12` v1)이 3분 만에 `KeyError: 'count_advantage'`
로 죽었는데, 원인이 **학습이 아니라 룩업 CSV 저장 셀의 4-3 라운드트립 검증**이었다.
내 가드는 학습 직전에 뒀으므로 도달조차 못 했다.
**키를 바꾸는 변경은 그 키를 참조하는 곳을 노트북 전체에서 먼저 훑을 것.**
(검증은 지우지 않고 `cnt12` 용으로 갈아끼웠다 — 칸 수 12 와 NaN 0건.)

### ⛔ 점수를 세그먼트로 쪼갰다 — 새는 슬라이스는 없다 (2026-08-29, `tools/eda42.py`)

claude.md 의 모든 수치는 전체 평균이었다. 처음으로 **점수가 어디서 나오는지** 쪼갰다.
분해식은 합이 정확히 전체 점수가 되게 잡는다 (채점과 같은 전역 r 을 쓴다):

```
기여_s = 비중_s x (1 - MSE_s / r(1-r)) x 100000        합 = 전체 점수
MSE_s > r(1-r) 인 구간은 음수로 기여한다 = 상수 예측보다 나쁘다
```

가설은 `game_type=F`(평가의 11.8%, 2023 에 라벨 체제가 바뀌어 그 해 홀드아웃을
통째로 못 쓰게 만든 구간)가 새고 있다는 것이었다. **정반대였다.**

| | 비중 | MSE | 자체스킬 | 기여 |
|---|---|---|---|---|
| F | 11.8% | **0.24699** | 544 | **133** |
| R | 88.2% | 0.24767 | 891 | 755 |

행당 기여가 **F 1,128 / R 857 로 F 가 32% 더 번다.** 자체 스킬 544 가 작아 보이는 것은
F 의 자체 베이스레이트가 더 치우쳐(.4593) 분모가 작기 때문이고, **채점에 쓰이는 것은
전역 r 이라 MSE 가 전부다.** 로컬(894)과 캐글(889) 두 기계에서 같은 결론이 나왔다.

⚠️ **자체 스킬로 세그먼트를 판단하지 말 것.** 구간마다 분모가 다르므로 서로 비교할 수
없다. 전역 r 로 계산한 기여만이 합산 가능하고 채점과 일치한다.

유일한 편차는 카운트 축이다: **투수 유리 카운트(28.4%)가 비중대로면 253 을 낼 자리에서
158 만 낸다** (행당으로 타자 유리 카운트의 45%). 0-2/1-2 에서는 투수가 일부러 존을
벗어나게 던지므로 의도적 볼과 제구 실패가 라벨상 구분되지 않는 것으로 보인다.

### ⛔⛔ 세그먼트별 재중심화 종료 — 오라클 +12.4, 실행가능 최선 +6.5 (2026-08-29)

재중심화는 전 구간에 로짓 상수 **하나**를 민다 (그것만으로 홀드아웃 +126). 구간마다
상수를 두면 어떤가. 규정상 자명하게 안전하고(행 자기 컬럼만 본다) 상수라 전달률이
1.0 에 가까운, **유일하게 남아 있던 축**이었다. `tools/eda43.py`.

**2024 의 세그먼트 편향이 전부 미미하다** (최대 `Pitcher` -0.0063). 상수로 벌려면
`비중 x 401,000 x 편향차²` 이므로 0.02 대가 필요한데 한 자리 아래다.

| 축 | 오라클(천장) | 실행가능(2023 상수를 2024 에) |
|---|---|---|
| **count_adv** | **+8.6** | **+6.5** |
| inning | +1.5 | +0.4 |
| game_type | +0.6 | **-1,418.8** |
| wn (당해 투구수) | +0.6 | **-240.8** |
| newpit | +0.1 | -0.1 |
| count_adv x wn | +12.4 | -240.9 |

⭐⭐ **'한 시즌 앞' 검증이 두 번째로 재앙을 막았다.** 오라클만 보면 `game_type` +0.6,
`wn` +0.6 로 무해해 보이는데 실제로 적용하면 **-1,419 / -241** 이다. 2023 의 F 편향이
체제 변경 때문에 +0.1752(2024 는 +0.0034)이고, `wn 0-60` 도 +0.0660 -> +0.0028 로
한 해 만에 23배 줄어든다. 그 상수를 다음 해에 옮기면 즉사한다.
(eda38 에서 A모형이 224점, 8/24 에 46점을 같은 방식으로 막았다.)

⛔ **그리고 내가 가장 유력하다고 지목한 축(`wn` 시즌 초 구간)이 최악에서 두 번째였다.**
근거는 '거기서 prev5 단독 스킬이 -3,224 로 무너진다' 였는데, 손상이 큰 구간과
**상수로 고칠 수 있는 구간은 다르다.** `game_type` 제거(-46.7)와 같은 구조다.

**살아남은 것은 `count_adv` 하나뿐이고 편향이 실제로 안정적이다:**

```
          2023      2024
Batter   +0.0025   +0.0062
Neutral  -0.0014   -0.0014      <- 소수 넷째 자리까지 동일
None     +0.0020   +0.0020      <- 동일
Pitcher  -0.0033   -0.0063
```

전달률 76%(+8.6 -> +6.5). 다만 이 수치는 스크리너 하네스(이진 107피처)의 것이고
배포본은 5분류 117피처라 크기는 옮겨진다는 보장이 없다.

### ⛔ isotonic 축 종료 — 재스케일링 천장이 +1.95 다 (2026-08-29)

배포 파이프라인에서 폴드별 isotonic 을 **떼본 적이 한 번도 없었다** (보정 축을 닫을 때
잰 것은 전부 *추가* 보정이었다). 20,000행에 배포 script.py 를 두 번 돌려 제출 없이 쟀다.

```
isotonic 있음   평균 0.471081  std 0.035483
isotonic 없음   평균 0.472995  std 0.033073
Δ평균 +0.001915 -> 재중심화 어긋남 -1.5 (상수 재측정으로 회수 가능)
λ = 0.9321      -> 수축 +1.0
순효과 +1.0 = 노이즈
```

부수적으로 **재스케일링 축 전체의 천장이 계산됐다.** λ\* = 0.96 에서
`rel(1.00)=0.99826 / rel(0.96)=1.00000 / rel(0.93)=0.99915` 이므로
**예측을 어떻게 늘이든 줄이든 최대 +1.95 다.** 이 축은 산술로 닫혔다.

### 📐 100인 컷 실측 (2026-08-29, SSR 페이로드 직접 파싱)

```
100번째 개인 = 팀 35위 = 1,163.8707     (팀 100개 = 299명, 평균 2.99)
우리 v10wzc            1,121.8905       팀 순위로도 101위 밖 (팀100위 1,134.15)
                       격차 +41.98
8/25 1,146.84 -> 8/28 1,158.83 -> 8/29 1,163.87  =  하루 약 +5
마감(9/2) 외삽 ~1,184  ->  그 시점 기준 격차 +62
```

전달률로 환산하면 새 정보 부류는 홀드아웃 **+73**, 세그먼트 상수 부류는 **+62** 가
필요하다. 오늘 연 다섯 축의 합계 상한이 +20 이 안 된다.

### 📐 100인 컷 실측 (2026-08-28, SSR 페이로드 직접 파싱)

```
100번째 개인 = 팀 36위 = **1,158.83**   (상위 100팀 = 296명, 평균 2.96명)
8/26 1,146.84 -> 8/28 1,158.83  =  **+6.0/일**
마감(9/2) 외삽 ~1,186
```
⚠️ 1위 1,295.68 은 "비회원" 이 8/28 밤 제출 1회로 올린 것 — claude.md 가 기록한
실격 정황과 같은 모양이나 컷에는 1명분만 영향을 준다.

파싱법: `curl` 로 리더보드 HTML 을 받아 Nuxt SSR 페이로드의 `.score=` 와
`team_info=[...]` 안의 `user_id:` 개수를 세면 된다. API 는 없다.


### ⭐ 범주형 조합은 **정확히 둘**에서 최적이다 — `cnt12h` LB **+5.72**, 넷은 -4.61 (2026-08-27)

```
v10wz   조합 0개 (5분류)                  1,116.1659
v10wzc  조합 2개 (cnt12 + hand4)          1,121.8905   **+5.72**  <- 최고점
v10wzb  조합 4개 (+ bs_out + bs_cnt)      1,117.2829   **-4.61**
```

**claude.md 의 "두 개를 넘기면 상쇄된다" 가 base 를 바꿔도 성립한다.** 그 경고는
이진 base 의 스크리너에서 나온 것(`bsx` +16.2 + `cnt12` +3.4 = **-1.2**)이라
5분류 base 로 옮겨간다는 보장이 없다고 보고 시험했는데, **옮겨왔다.**
CTR 경쟁 가설과 일관된다 — 같은 상황 신호를 여러 CTR 이 나눠 갖고 각자 과적합한다.

**이 축은 `cnt12` + `hand4` 에서 닫힌다. 더 얹지 말 것.**

### ⛔⛔⛔⛔ 2024 홀드아웃은 **음수도 뒤집는다** — 세 번째 거짓 음성 (2026-08-27)

| | 2024 홀드아웃 | 실측 LB |
|---|---|---|
| `wsbat` | +3.0 | **+30.11** |
| `mcaux` | +2.8 | **+12.85** |
| **`cnt12h`** | **-6** | **+5.72** |

앞의 둘은 "0 근처라 못 읽는다" 로 설명됐다. **이번 것은 명백한 음수인데 양수다.**
따라서 2024 홀드아웃은 **어느 방향으로도** 후보를 거를 수 없다 —
양수도 음수도 정보가 아니다. 남은 용도는 '파이프라인이 안 죽었나' 확인뿐이다.

⚠️ 그런데 같은 날 `v10wzb`(2024 909, `v10wzc` 914 대비 -5)는 **실제로도 -4.61** 이었다.
   즉 2024 가 항상 틀리는 것도 아니다. **상관이 없다는 뜻이고, 그래서 못 쓴다.**

⛔ **비용**: 이 프로젝트는 오랫동안 "+50 미만은 노이즈" 를 2024 기준으로 코드에 박고
후보 20개 이상을 버렸다. 그중 살아 있는 것이 얼마나 되는지는 알 수 없다.


### ⛔ prev-game 분모 역산 — 아이디어는 맞았고 LB **-3.00** (2026-08-27)

`asof_pitcher_prev{1,3,5}_game_{success,middle}_rate` 6개는 `성공수/투구수` 인
**유리수**이고 소수 6자리로 저장돼 있다. 분모 D 이하 기약분수 사이 최소 간격이
`1/(D(D-1))` 이므로 D=400 이면 6.3e-6 으로 반올림 오차 5e-7 의 12배다 —
**분모가 역산된다.** 같은 윈도우에 분수가 둘이라 최소공배수로 약분도 대부분 지워진다.

즉 1장의 "**prev-game 6개는 분모가 test 에 없어서 분해 불가능**" 은 **틀렸다.**
분모는 test 행이 이미 갖고 있다 — 값 안에.

**정답 대조** (train 경기 복원 4,868경기 / 45,862등판)

| | success 단독 | **lcm 결합** | 약수 보장 | 초과(오답) |
|---|---|---|---|---|
| prev1 | 61.2% | **83.1%** | 100.0% | 0.0% |
| prev3 | 60.7% | **83.2%** | 100.0% | 0.0% |
| prev5 | 55.7% | **83.0%** | 100.0% | 0.0% |

⚠️ 윈도우별 분모 상한을 나눠야 한다. 400 으로 통일하면 선발의 직전 5등판(450구+)이
   상한 밖이라 `pn5` 결측 17.9% 에 중앙값이 `pn3` 보다 작아진다. 250/700/1200 을 쓴다.

**결과: `v10wz` 1,116.17 -> `v10wpn` 1,113.17 = -3.00** (노이즈 바닥 ±1.5 밖 = 진짜 음수).
2024 홀드아웃은 920 -> 923 (+3) 이었다.

⛔ **'약수 보장 100% / 초과 오답 0%' 를 안전 보증으로 읽은 것이 잘못이다.**
**수치 피처에서 약수는 틀린 값이다.** 참 20 이 2 로 약분되면 10배 오차이고 그게
17% 행에서 일어난다. 게다가 무작위가 아니다 — 0.5, 0.6 같은 둥근 비율이 더 잘
약분되므로 **오차가 신호 자체와 상관돼 있다.** '틀리지 않고 약분될 뿐' 이라는 말은
분수로서는 참이지만 피처로서는 거짓이다.

⛔ **그리고 claude.md 에 이미 답이 있었다.** `prevfix`(+0.98) 항목:
> `w_n` 만 있으면 트리가 스스로 '당해 투구수가 적으면 prev5 를 믿지 마라' 를 학습한다.

모델은 `asof_pitcher_n` / `w_n` / `inning` / `is_pure_starter` 로 보직·부하를 이미
잡고 있었다. 거기에 17% 가 틀린 축을 여섯 개 더한 것이다. **`prevfix` 와 같은 구조이고
같은 결론이다 — 손상이 실재한다는 것과 고치면 점수가 오른다는 것은 다른 이야기다.**

⭐ 다만 **역산 기법 자체는 유효하고 재사용 가능하다** (`tools/mk_prevn.py`).
비율 x 개수 구조의 컬럼이 새로 생기면 이 방법으로 분모를 딸 수 있다.
그리고 규정상 가장 깨끗한 부류다 — 행 A 자기 값 하나만 쓰므로 train 룩업조차 필요 없고,
행 독립 시험을 자명하게 통과한다 (실측 2.22e-16).

### ⛔ 블렌드 최종 확인 — 타겟 정식화가 달라도 0.9633 (2026-08-27)

트리 계열끼리는 0.92~0.97 로 막혔는데(fam3), **손실함수가 다른 두 배포본**은
안 재봤었다. `tools/pred_corr.py`, GPU 0 / 제출 0 / 3분.

```
                v10wz(5분류)  v10wq(이진+aux)  v10wpn
v10wz               1.0000         0.9633      0.9935
v10wq               0.9633         1.0000      0.9606
```

**0.9633 — 우리 배포본 중 최저지만 여전히 포화 구간이다.** 홀드아웃 +7 x 전달률 0.25
= LB **+2** 이고, 그걸 하려면 서로 다른 피처셋·상수를 가진 두 모델을 한 `script.py`
안에서 평균내는 zip 을 조립해야 한다. 런타임 오류는 제출 한 장을 태운다.
**기대 +2 에 그 위험을 쓰지 않는다. 앙상블 축 완전 종료.**


### ⭐⭐⭐ 5분류 타겟 `multicls` — LB **+12.85** (2026-08-26)

```
v10wq 1,103.3169  ->  v10wz 1,116.1659   = +12.85
```
타겟을 이진에서 **5분류**로 바꾼다: 0 성공 / 1 몰림만 / 2 반대만 / 3 둘다 / 4 크게벗어남.
라벨은 asof 인접 행 차분으로 복원한다 (eda40, success 검산 일치율 1.000000).

**왜 되는가** (eda41): 유형별 드리프트가 서로 **반대**다 (2019->2024, R 전용):
```
middle +0.0484 | reverse +0.0543 | both +0.0015 | wild **-0.0414**
총 변동 0.144 -> 순변동 0.060.  이진 타겟이 실제 움직임의 2.4배를 가린다.
```
실패의 *양*보다 실패의 *구성*이 훨씬 크게 바뀌었다. 나누면 상쇄되던 과정이 분리된다.

**스크리너** (wsboth 기준): multicls 단독 2024 +35.7 / 2023 +37.8.
`auxrevns` 위에 얹은 `mcaux` 는 2024 +25.3 / 2023 +101.6 = **증분 +2.8 / +46.6**.
2024 는 중복(둘 다 실패유형 구조를 뽑는다), 2023 은 보완적이었고 **2023 이 맞았다.**

**구현 요점** (`tools/mk_multicls.py`):
```
CalibratedClassifierCV 는 다중분류에 못 쓴다
  -> 성공 클래스 확률을 뽑아 **이진 y** 로 IsotonicRegression 직접 적합
폴드 분할은 이진 y 로 (기존 구성과 폴드를 맞춘다)
성공 클래스 열은 classes_ 에서 런타임 조회 -> train_constants 에 success_col 저장
  -> script.py 가 _const.get("multiclass") 면 그 열을 쓴다
⚠️ 오프셋 셀이 train_constants.json 을 통째로 덮어쓰므로 병합은 그 셀 **맨 끝**에서
   (+ ws_league_mean assert 로 소실 감지)
검산: 클래스0 개수 == success 개수 (assert)
```
GPU MultiClass 는 예상보다 빨랐다 — 30모델에 **48분** (이진과 비슷).
본 학습 홀드아웃은 920 으로 v10wq/v10wn(920/919)과 **같았다** — 2024 는 이 이득을
전혀 못 봤다. 그런데 리더보드는 +12.85 다.

### 📐 두 시즌이 갈리면 **작은 쪽 x 0.6** 이 리더보드다 (2026-08-25, 4건)

`auxrev` 사고(2024 +40.2 / 2023 -18.1 인데 2024 만 보고 제출 -> LB +4.14) 이후
전 사례를 이 규칙으로 다시 맞춰봤다.

| 변경 | 2024 | 2023 | 최솟값 | x0.6 예측 | **실측** |
|---|---|---|---|---|---|
| `wseason5` | +72.2 | +182.1 | +72.2 | +43 | **+61.7** |
| `auxrev` | +40.2 | **-18.1** | -18.1 | ~0 | **+4.1** |
| `auxrevns` | +22.5 | +55.0 | +22.5 | +14 | **+13.5** |
| `auxnsb` 증분 | **-0.3** | +15.1 | -0.3 | ~0 | **+0.002** |

4건 중 3건이 맞는다. 예외는 `wsbat`(2024 +3.0 / 2023 +71.7 -> LB **+30.11**) 하나다.

**핵심은 큰 쪽을 믿으면 안 된다는 것이다.** `auxrev` 의 +40.2 도 `auxnsb` 의 +15.1 도
큰 쪽이었고 둘 다 전달되지 않았다. claude.md 의 '두 시즌 합의' 규칙을
**"둘 다 양수여야 하고, 크기는 작은 쪽으로 잡는다"** 로 강화한다.

### ⛔ 보조 타겟 축은 `reverse` 하나에서 닫혔다 (2026-08-25)

```
v10wn (reverse)         1,103.3145
v10wq (reverse + ball)  1,103.3169   = **+0.002**
```
스크리너는 2024 -0.3 / 2023 +15.1 이었고 **2024 가 맞았다.**

| 후보 | 2024 | 2023 |
|---|---|---|
| `auxrevns` (reverse) | +22.5 | +55.0 |
| `auxnsb` (+ball) | +22.2 | +70.1 |
| `auxns3` (+strike) | +27.4 | +54.5 |
| `auxns4` (+middle) | +24.2 | +63.3 |

2024 에서 넷이 +22~27 로 스크리너 분해능(±10) 안이다. **라벨을 더 넣어도 안 오른다.**
보조 타겟도 범주형 조합과 같은 비가법성을 보인다 — 하나가 최적이다.

**남은 후보는 `multicls`(타겟을 5분류로) 하나다**: 2024 +35.7 / 2023 +37.8 로
**두 시즌이 일치하는 유일한 후보**이고, 최솟값 규칙으로 LB **+21** 이 예측된다.
다만 그 수치는 `wsboth` 기준이고 배포본은 이미 `auxrevns` 를 갖고 있으므로
**쌓이는지**가 관건이다 (`kz24`/`kz23` 에서 측정 중, 5분류라 8시간 걸린다).

### ⭐⭐⭐ 파생피처의 재료에서 `season`/`game_type` 을 빼면 전달률이 6배 오른다 (2026-08-25)

같은 아이디어, 보조 모델의 입력만 바꿔 두 번 제출했다.

| | 스크리너 2024 | 스크리너 2023 | **리더보드** | 전달률 |
|---|---|---|---|---|
| `auxrev` (season 포함) | **+40.2** | **-18.1** | +4.14 | **0.10** |
| `auxrevns` (season 제거) | +22.5 | **+55.0** | **+13.50** | **0.60** |

**홀드아웃 숫자는 절반인데 리더보드는 3배다.**

**기전은 eda31 에 이미 적혀 있었다**: `season` 은 cat_features 에 없는 int64 라
분기 경계가 학습에서 본 값 위로 안 생긴다. 최대 경계가 **2023.5** 이므로
2024 도 2025 도 같은 잎이다. 따라서 **`season` 을 재료로 만든 파생피처는
학습 시대에 얼어붙은 값을 2025 에 뱉는다.** 보조 모델 중요도가
`game_type` 15.94% + `season` 15.13% = **31%** 였으니 그만큼이 죽은 무게였다.

⛔ **그리고 그 성분은 무해한 게 아니라 해로웠다** — `auxrev` 의 2023 홀드아웃이
**-18.1** 이다. 두 시즌 부호가 갈렸는데 나는 2024 만 보고 제출했다
(claude.md 자신의 '두 시즌 합의' 규칙 위반). 그 한 장이 이 사실을 알려준 셈이다.

**실무 규칙 (일반화)**
```
파생·압축·보조 피처를 만들 때 재료에서 season / game_type 을 뺀다.
본 모델은 그 둘을 직접 갖고 있으므로 잃는 것이 없다.
```
⚠️ 디트렌드 용도(그 시즌 리그평균을 **빼는** 것)는 별개다 — `cond_*` 와 `w5_*` 는
그 방식이고 정상 작동한다. 금지되는 것은 season 을 **입력 피처로 주는** 것이다.

**따라서 전달률 표의 '압축 0.10' 은 부류가 아니라 결함이었다.**

| 변경 종류 | 전달률 | 사례 |
|---|---|---|
| 새 정보 (다른 행/테이블) | 0.85 | wseason5 |
| **보조 라벨 (season 제거)** | **0.60** | **auxrevns +22.5 -> +13.50** |
| 버그 수정 | 0.45 | 트랙맨 매핑 |
| 파라미터·피처 제거 | 0.25 | CPU 전환, 월/요일 |
| **season 오염된 파생** | **0.10** | auxrev +40.2 -> +4.14 |

**미측정 후속**: 배포본은 보조 타겟이 `reverse` 하나뿐이다. eda41 신호 크기는
`reverse` 102% / `ball` 61% / `strike` 55% / `middle` 45% 이고 `ball`·`strike`
라벨도 복원 확인됐다 (평균 0.3696 / 0.4433, 결측 0.05%).
후보 `auxnsb`(rev+ball) / `auxns3`(+strike) / `auxns4`(+middle) 측정 중.

### 📐 100인 컷 실측 (2026-08-25, 리더보드 직접 파싱)

```
1위 1,209.31 | 팀 100위 1,126.67 | 100팀 = 227명 (평균 2.27)
누적 91명 팀41위 1,147.42 | 94명 팀42위 1,147.25 | 97명 팀43위 1,146.97
누적 101명 팀44위 **1,146.84**  <- 100번째 개인
```
**8/23 의 1,136.61 에서 이틀 만에 +10.2 (하루 ~5점)** 올랐다.
⚠️ 1,400점대 1위가 삭제됐지만 컷은 안 내려갔다 (현재 1위 1,209.31).

### ⛔⛔ `auxrev` 리더보드 **+4.14** — 전달률 0.10, 새 등급이 생겼다 (2026-08-25)

```
v10wp 1,089.8100  ->  v10wa 1,093.9490   = +4.14
스크리너 +40.2  ->  전달률 **0.103**
```
+4.14 는 노이즈 바닥(±1.5) 위라 **진짜 양수이긴 하다.** 다만 예측(+20, 1110)이
4배 틀렸다.

**원인은 구조적이다 — 보조 모델이 뭘 보는지가 전부였다:**

| 보조 모델 P(reverse\|X) 의 중요도 | |
|---|---|
| `game_type` | **15.94%** |
| `season` | **15.13%** |
| `w_reverse` | 14.10% |
| `wb_success` | 5.74% |
| `asof_pitcher_reverse_rate` | 4.38% |

**절반이 `season x game_type`(드리프트)이고 나머지는 본 모델이 이미 가진 피처다.**
즉 `aux_rev` 는 새 정보가 아니라 **기존 신호의 압축**이다.

- 배포 모델에서 `aux_rev` 중요도 **10.12% (1위)** — game_type(4.67)+season(4.16)보다 크다.
- 홀드아웃 +40 인 이유: 원재료보다 나은 기저함수라 2024 를 잘 맞힌다.
- 리더보드 +4 인 이유: `season` 경계가 2023.5 에서 끝나므로(eda31) `aux_rev` 는
  **2024 수준에 고정된 값**을 2025 에 뱉는다. 외삽이 안 된다.
- **`game_type` 과 같은 병이다** (중요도 1위인데 순열 중요도 -554).
  모델이 많이 쓰는 것과 도움이 되는 것은 다르다.

**⛔ 전달률 표에 네 번째 등급을 추가한다**

| 변경 종류 | 전달률 | 사례 |
|---|---|---|
| 새 정보 (다른 행/테이블) | **0.85** | wseason5 +72.2 -> +61.72 |
| 버그 수정 | 0.45 | 트랙맨 매핑 |
| 파라미터·피처 제거 | 0.25 | CPU 전환, 월/요일 |
| **압축 (기존 피처의 재조합)** | **0.10** | **auxrev +40.2 -> +4.14** |

**기존 피처를 재조합한 것은 홀드아웃에서 크게 나오고 리더보드에서 거의 안 나온다.**
'한 행 안에서 계산되는 파생피처는 죽어 있다'(3번 확인)의 변형이다 — 트리가 못
만드는 게 아니라 **만들어줘도 새 정보가 아니어서** 미래 시즌에 안 통한다.
⚠️ 범주형 조합(`cnt12h` +19.0/+4.3)도 같은 부류다. 기대를 LB +2 로 낮출 것.

**미검증 후속**: 보조 모델에서 `season`/`game_type` 을 빼고 학습하면 `aux_rev` 가
드리프트 대신 순수한 투수·상황 reverse 성향만 담는다. 지금은 그 31% 가 2024 에
고정된 채 2025 로 넘어가며 죽는 구조다. 다만 남는 것도 `w_reverse` 의 압축이라
기대는 낮다.

### ⭐⭐⭐ 실패유형 보조 타겟 `auxrev` — 스크리너 +40.2, 대조군 통과 (2026-08-24)

팀원(송도원) 제안의 **확장형**. 원래 제안(조건부 피처)은 두 시즌 다 0 이하였는데
(`condfail` +2.3/-17.8, `condfailc` -2.5/-2.2), **보조 타겟**으로 쓰니 살아났다.

교차적합한 `P(reverse|X)` 를 피처 **하나**로 얹는다.

| | wsboth | auxrev | aux3 | auxperm(대조군) |
|---|---|---|---|---|
| 로컬 2024 | 894 | **930 (+36)** | 933 (+39) | — |
| 캐글 2024 | 889 | **929 (+40)** | — | **887 (-1.9)** |

⛔ **대조군이 이 결과의 전부다.** `reverse=1 => success=0` 이라 누수가 나면 정확히
이 모양이 나온다. 라벨만 무작위로 섞어 똑같이 돌린 `auxperm` 이 **기준선으로
돌아왔고**(-1.9), 섞은 라벨로 학습한 보조모델의 OOF 표준편차가 **0.00452**
(사실상 상수)였다. 배울 게 없으니 아무것도 안 배웠다. 그리고 캐글 929 가
로컬 930 을 **다른 하드웨어에서 독립 재현**했다.

**왜 되는가**: 본 모델은 이진 `y` 만 보므로 `P(reverse|X)` 를 **배울 수 없다**
(그 라벨을 못 보니까). 그리고 `reverse` 는 여섯 실패유형 중 유일하게 투수 수준
신뢰도가 `success` 보다 높다 (0.941 vs 0.911, 신호 102% — eda41).

**⚠️ 구현은 누수 방지가 전부다** (`tools/mk_auxrev.py`):
```
학습 행    out-of-fold 값만
추론 행    폴드 모델 3개의 평균 (zip 에 aux_rev_*.cbm 3개를 싣는다)
오프셋셀   검증 시즌 라벨을 **안 본** 별도 판(AUX_HO)으로 교체
          -> 안 하면 오프셋이 낙관 측정된다. 재중심화는 지금 +134 짜리다
행 순서    (pitcher_id, asof_pitcher_n) 정렬 + success 검산 assert
```

**본 학습 실측** (`aimers_v10wa.ipynb`): 라벨 복원 검산 1.000000 /
aux_rev OOF 평균 0.2288 (실제 0.2290) / 오프셋 -0.0684 /
**홀드아웃 817 -> 932 (재중심화 +115)**. 독립성 검증 1.67e-16 통과.
v10wp 와 예측 상관 0.9781, 예측 std 0.0355 -> 0.0371 (aux_rev 작동 확인).

⚠️ **전달률이 미지수다.** `aux_rev` 는 결국 `X` 의 함수라 데이터가 무한하면
본 모델이 직접 배우고 잉여가 된다 — 즉 **이득이 데이터 양에 따라 줄어드는 구조**다
(배포 1.48M vs 홀드아웃 1.22M). CPU 전환이 +26 -> +5.17 로 죽은 그 패턴이다.
반면 '본 모델이 원리적으로 못 배우는 라벨' 이라는 점은 새 정보 쪽이다.
**`kaux23`(2023, 0.95M 행)이 이걸 가른다**: 2023 이득 >> 2024 이득이면 추정 효율
(전달률 낮음), 비슷하면 새 정보 (전달률 높음).

### ⭐ 범주형 조합은 **두 개까지** — 강하게 비가법적이다 (2026-08-24)

| 후보 | 2024 | 2023 | |
|---|---|---|---|
| **`cnt12h`** (cnt12 + hand4) | **+19.0** | **+4.3** | ✅ 두 시즌 양수 중 최대 |
| `bsx` (bs_out + bs_cnt) | +3.8 | +16.2 | ✅ |
| `cnt12` | +8.3 | +3.4 | ✅ |
| `catall` (넷 전부) | +5.2 | — | 다 넣으면 오히려 나빠진다 |
| `catx` (hand4 + bsx) | +8.3 | +0.7 | |
| `cnt12b` (cnt12 + bsx) | +3.6 | **-1.2** | ❌ |
| `hand4` 단독 | +6.5 | **-11.4** | ❌ 뒤집힘 |
| `cnth` (cnt12 x hand, 24칸) | **-6.9** | — | ❌ 곱하면 나빠진다 |
| `cntg` (cnt12 x game_type, 24칸) | +3.0 | — | |

**원리 둘**
1. **저카디널리티 조합을 여러 개 따로 주되 서로 곱하지 말 것.** `cnt12`(12칸) +8.3 인데
   손을 곱해 24칸으로 만들면 -6.9 다. 반면 `cnt12`+`hand4` 를 **별개 컬럼**으로 주면 +19.0.
2. **두 개를 넘기면 상쇄된다.** `bsx`(+16.2) + `cnt12`(+3.4) = **-1.2**.
   CTR 경쟁으로 보인다 — 같은 상황 신호를 여러 CTR 이 나눠 갖고 각자 과적합한다.
   `ctr2`(300쌍 자동 생성) -5 와 같은 이야기다.

`hand4` 가 단독으로 뒤집히는데 `cnt12` 와 함께면 안전한 것도 이 비가법성이다.
제출 구성은 **`cnt12h` 하나**로 확정 (`tools/mk_cnt12.py` -> `aimers_v10wc.ipynb`).

### 📐 실패 유형은 **2비트 코드**이고, 유형별 드리프트가 서로 반대다 (2026-08-24, eda41)

eda40 의 복원 라벨을 **보조 타겟**으로 쓸 근거가 있는지 쟀다 (팀원 송도원 제안의 확장).

⚠️ **먼저 내 정의가 틀렸다.** `wild = 1 - success - middle - reverse` 로 썼다가
50,266행(3.4%)이 음수로 나왔다. 반올림 오차가 아니다 — 교차표(147만 행):

| success | middle | reverse | 행수 | |
|---|---|---|---|---|
| 0 | 0 | 0 | 194,196 | 순수 '크게 벗어남' |
| 0 | 0 | 1 | 287,401 | 반대방향만 |
| 0 | 1 | 0 | 170,297 | 몰림만 |
| 0 | 1 | 1 | **50,266** | **둘 다** (실패의 7.2%) |
| 1 | 0 | 0 | 772,140 | 성공 |

- **`success=1` 에서 middle/reverse 가 1인 행은 0개** — 완전 중첩 확정.
- **middle 과 reverse 는 배타적이 아니다.** 포수가 바깥에 앉았는데 가운데-몸쪽으로
  몰리면 '몰림' 이면서 '반대방향' 이다. 배타적 3분할이 아니라 **2비트 코드**다.
  올바른 정의는 `wild = 실패 & !middle & !reverse` 다.
- 5분류로 정리: 0 성공 / 1 몰림만 / 2 반대만 / 3 둘다 / 4 크게벗어남.

**★ (A) 유형별 드리프트가 서로 반대다** (2019->2024, R 전용, 실패율 +0.0598)

| 시즌 | 실패율 | middle | reverse | both | wild |
|---|---|---|---|---|---|
| 2019 | .4505 | .1301 | .1884 | .0307 | **.1628** |
| 2021 | .4872 | .1501 | .2623 | .0446 | **.1195** |
| 2024 | .5103 | **.1785** | .2427 | .0322 | .1214 |

```
middle  +0.0484 ( 80.9%)   reverse +0.0543 ( 90.8%)
both    +0.0015 (  2.6%)   wild    **-0.0414 (-69.1%)**   <- 반대 방향
총 변동 0.144  ->  순변동 0.060.  이진 타겟이 실제 움직임의 **2.4배를 가린다.**
```
실패의 *양*보다 실패의 *구성*이 훨씬 크게 바뀌었다. 이것이 '추정 효율' 벽을
우회하는 유일한 기전이다 — 유형별 모델링은 같은 것을 더 잘 재는 게 아니라
**서로 상쇄되던 세 과정을 분리**하는 것이다.

**★ (B) `reverse` 가 `success` 보다 안정적이다** (반쪽 분할, 투수 569명, 양쪽 100구+)

| 타겟 | 신뢰도 | 신호 std | success 대비 |
|---|---|---|---|
| success | 0.911 | 0.0452 | (교정점) |
| **reverse** | **0.941** | **0.0462** | **102%** |
| ball | 0.797 | 0.0275 | 61% |
| strike | 0.747 | 0.0247 | 55% |
| middle | 0.807 | 0.0203 | 45% |
| wild | 0.797 | 0.0188 | 42% |
| both | 0.709 | 0.0077 | 17% |

**"포수 요구 반대방향으로 던진다" 가 이 데이터에서 가장 안정적인 투수 특성이다** —
전체 제구 성공률보다도. 보조 타겟으로 쓸 값어치가 가장 큰 것은 `reverse` 다.

⚠️ **유보 둘**
1. `season` 경계가 2023.5 에서 끝나므로(eda31) 유형을 분리해도 **2025 로 외삽은 못 한다.**
   드리프트를 고치는 게 아니라 각 유형의 X<->y 관계가 이진보다 안정적이어야 이득이 난다.
   (B)는 투수 수준 증거이지 투구 수준 증거가 아니다.
2. 이득 경로가 '정보 추가' 가 아니라 **'더 나은 기저함수'(추정 효율)** 로 흐를 위험.
   우리는 `in-sample 2.232% ~= OOF 2.21%` 로 거기 안 묶여 있고, 증류(-23/-38)와
   CPU 전환(-2.95)이 정확히 그 벽에서 죽었다.

⚠️ **교차적합 필수.** `reverse=1 => success=0` 이므로 보조 예측을 in-sample 로 적합해
   피처로 쓰면 타겟이 통째로 샌다. 학습 절반은 out-of-fold, 검증 절반은 폴드 평균.

후보: `auxrev`(P(reverse|X) 하나) / `aux3`(셋 다) / `multicls`(타겟을 5분류로).

### ⭐ 범주형 2-way 조합 축은 살아 있다 — 내 사전 확률이 틀렸다 (2026-08-24)

`is_same_hand` 가 배포 모델 중요도 **2.59%(4위)** 로 `pitcher_hand`(0.55)와
`batter_hand`(1.19)를 합친 것보다 크다. 중요도는 '많이 쓴다' 는 뜻이지 '도움이
된다' 는 뜻이 아니므로(`game_type` 은 1위인데 순열 중요도 -554), **빼봤다.**

| 후보 (2024 홀드아웃, `wsboth` 888.6 기준) | 점수 | 대비 |
|---|---|---|
| **`nosh`** is_same_hand 하나 제거 | 871.6 | **-17.0** |
| `cnt12` 정확한 12칸 카운트 (+1개) | 896.9 | **+8.3** |
| `catx` hand4 + bs_out + bs_cnt (+3개) | 896.9 | **+8.3** |

**`nosh` -17.0 이 이 축이 실재한다는 직접 증거다.** 나는 "한 행 안에서 계산되는
파생피처는 구조적으로 죽어 있다"(3번 독립 확인)에 걸었는데 **범주형에는 안 통한다.**

**왜 예외인가**: depth 8 대칭트리는 `pitcher_hand` 와 `batter_hand` 를 두 레벨에
걸어 4칸을 **만들 수 있다.** 못 만드는 게 아니라 **비싸다** — 115개 피처와
경쟁하며 매 트리에서 레벨 두 칸을 써야 한다. 조합을 직접 주면 CTR 하나로 끝난다.
수치형은 트리가 임계값으로 싸게 근사하므로 이 논리가 안 걸린다.

**`cnt12` 는 정보 손실 복구다.** `count_advantage` 의 `'None'` 이 **0-0 과 3-2 를
같이 담는다**(4-3). 야구에서 가장 다른 두 카운트다. `is_full_count`/`is_first_pitch`
가 그걸 되돌리려는 땜질이고, 셋을 합쳐 중요도 3.07% 를 쓰면서도 "0-2 와 2-0 은
다르다" 를 말하지 못한다.

⚠️ `ctr2`(모든 2-way 자동 생성)는 **-5** 였다. 25개에서 300쌍을 만들면 대부분
노이즈에 고카디널리티라 희석된다. **선택된 저카디널리티 조합**만 통한다.

### ⛔ 분해 가능한 asof 열 개를 전부 캤다 — 채널 소진 (2026-08-24)

test 행이 2025 정보를 담는 통로는 `asof_*` **19개(비율 16 + 개수 3)** 가 전부다.

| 묶음 | 분해 | 상태 |
|---|---|---|
| 투수 결과 5 (success/reverse/middle/ball/strike) | ✅ 분모 `asof_pitcher_n` | **채택 LB +61.72** |
| 타자 2 (success/middle) | ✅ 분모 `asof_batter_n` | **채택 LB +30.11** |
| **구종 3 (fastball/breaking/offspeed)** | ✅ 분모 동일 | ❌ **`wsmix` -16.6 (2023)** |
| prev-game 6 (prev{1,3,5}x{success,middle}) | ❌ **분모가 없다** | 원본·디트렌드판 사용 중 |

★ `asof_pitcher_pitchmix_n` 은 `asof_pitcher_n` 과 **모든 행에서 동일**(최대차 0)이고
세 구종 비율 합은 0.9995 = 완전 분할이다. 즉 구종은 `wseason5` 와 같은 기계로
복원되는데 **-16.6** 이다. 사전 증거 둘과 일치한다: eda10 대리모형이 +0 이라 했고
(투수 5개는 +83 -> 실측 +72.2 로 잘 맞힌 모형), teacher 실험에서 **실제 던진 구종**은
+478 인데 **성향**은 정확히 0 이었다. 배합은 성향이라 이미 base 에 녹아 있다.

prev-game 6 이 분해 불가능한 이유는 분모(그 등판의 투구수)가 test 에 없어서다.
train 에서는 경기 복원으로 알 수 있지만(4,868경기/45,862등판 검증 완료)
test 에서 알아내려면 다른 행을 봐야 해서 **규정 위반**이다.

**즉 `wseason` 트릭(train 룩업으로 test 행에 숨은 2025 정보를 해독)은 소진됐다.**
남은 합법적 '다른 행' 정보원은 train(2019~24)과 trackman(2019~24) 둘뿐이고
둘 다 과거 데이터다.

### ❌ 증류 — 두 시즌 부호가 갈렸고, 그 배열 자체가 답이다 (2026-08-24)

| | 2024 | 2023 |
|---|---|---|
| `wsboth` | 889(캐글)/894(로컬) | 629 |
| `distill` | 866 / 856 | 646 |
| 차이 | **-23 / -38** | **+17** |
| teacher OOF 스킬 | 2,707 / 2,687 | 2,871 |

```
2023 홀드아웃  학습 ~0.95M 행  ->  +17
2024 홀드아웃  학습  1.22M 행  ->  -23 / -38
배포          학습  1.48M 행  ->  더 음수
```
soft target 은 **규제**다. 3장에 적힌 대로 홀드아웃은 소표본 체제라 규제에
가산점을 주고, 2023 은 2024 보다 데이터가 27% 더 적어 가산점이 더 크다.
**데이터가 늘수록 단조 감소하는 배열**이므로 배포에서는 더 음수다.

⚠️ '두 시즌 중 하나라도 양수면 LB 로 판정' 규칙은 **새 정보 부류**를 위한 것이다
(새 정보는 표본 크기와 무관해 그 편향을 안 받는다). 증류는 규제라 대상이 아니다.
teacher 가 2,707 을 알면서 student 에게 0 을 준 것이 이 축의 결론이다.

### ❌ 추정기 편향 제거 3후보 · 트랙맨 제거 · 타자 트랙맨 — 전부 0 (2026-08-24)

eda33 이 "wsbat/wseason 이 이긴 방식은 최근성이 아니라 **커리어 추정치의 편향
제거**" 라고 재해석했고, 그 방향 후보 셋을 전부 쟀다 (다음 시즌 편차 예측 R2 증분).

| | 잰 것 | 증분 | |
|---|---|---|---|
| eda34 | 상대 타자의 질 | +0.0004 | ❌ |
| eda35 | 상황 믹스 (보직) | **-0.0027** | ❌ |
| eda36 | 혹사 · 경험 궤적 | +0.0035 (셋 다 넣어서) | ❌ |

원인이 같다 — **투수-시즌 간 분산이 애초에 없다.** 상대 타자 질 std 0.0022,
상황 난이도 0.0044 인데 투수 편차 std 는 0.0471 이다. KBO 10팀 균형 편성이라
다들 비슷한 타선을 비슷한 상황 믹스로 상대한다. 씻어낼 편향이 없다.
혹사는 부호까지 반대였다(많이 던지면 다음 해 아주 조금 **낫다**, +0.017).

| 후보 | 2024 | 2023 |
|---|---|---|
| `notm` 트랙맨 20개 전부 제거 | — | +2.6 |
| `notmstd` std 18개만 제거 | — | -2.2 |
| `noflag` 바닥 플래그 14개 제거 | — | +4.2 |
| `bttm` 타자측 트랙맨 | -2.6 | +2.6 |

**트랙맨은 중요도 12.9% 를 먹는데 통째로 빼도 0 이다.** 그 12.9% 가 다른 피처로
완전히 대체 가능하다는 뜻이다 — `w_success`/`wb_success` 가 같은 것(투수 질)을
더 잘 말하고 있다. 다만 **제거해도 이득은 0** 이다.

### ✅ 재중심화 오프셋 재검증 — 현행 유지 (2026-08-24, `tools/eda38.py`)

eda37 에서 재중심화가 **760 -> 894 = +134** 를 지고 있는 것이 드러났다
(claude.md 의 '+18' 은 v5 피처 기준, 편향 +0.008). 지금 편향은 **+0.0182** 다.
`w5_*` 가 시즌 리그평균을 빼서 시즌 중립이라 모델의 수준 앵커가 학습 시대 쪽으로
더 세게 붙는다. **배포 점수의 134점이 상수 하나에 걸려 있으므로** 네 해의 b 를
현행 107 피처로 다시 쟀다.

| Y | b(전체) | b(R) | b(F) | 낙폭(전체) | 낙폭(R) |
|---|---|---|---|---|---|
| 2021 | +0.0014 | +0.0127 | -0.0949 | +0.0000 | -0.0142 |
| 2022 | +0.0025 | +0.0030 | -0.0010 | -0.0038 | -0.0091 |
| 2023 | **+0.0229** | **+0.0027** | **+0.1961** | -0.0290 | -0.0006 |
| 2024 | +0.0182 | +0.0178 | +0.0216 | -0.0139 | -0.0134 |

**2023 의 편향은 전부 F 였다** (R 은 +0.0027). 2024 는 R/F 균일이라 진짜 모델 편향이다.

한 점 빼고 맞히기(누적 손실): **B(b~낙폭 전체) 97** < D(현행) 177 < A(연도) 211
< **C(b~낙폭 R) 276**. 2025 외삽 시 현행과의 차이가 만드는 손실은
B 3점 / C 2점 / **A 46점** 이다.

- **현행 유지가 맞다.** D 의 177 은 거의 전부 2023(168) 인데 F 체제 충격 일회성이고
  정상 연도인 2024 에서는 손실이 **9** 였다. 2025 에 알려진 체제 변화는 없다.
- ⚠️ A(연도 외삽)를 그냥 썼으면 46점을 잃었다. **검증표가 또 잡았다** (8/21 에는 224점).
- ⛔ **'이론적으로 옳은' C 가 최악(276)이다.** 8/23 에 F 체제 변경을 발견한 뒤
  "R 전용 낙폭이 옳다" 고 봤는데 틀렸다. **채점이 혼합 행에서 이뤄지므로 b 도
  혼합 낙폭을 따른다.** 대리지표/이론 4연패.

### 🔧 eda39 — 전이 안정성 스캔 (2026-08-24)

피처를 구간으로 나눠 '리그평균 대비 편차' 프로파일을 옛 시즌(2019~21)과
최근(2023~24)에서 각각 만들어 가중 상관을 본다. 학습이 필요 없다.
`위험 = 중요도 x (1 - 상관)`.

⚠️ **저카디널리티 정수에 `qcut(rank)` 을 쓰면 안 된다.** 같은 값 안을 **행 순서로**
쪼개는데 행 순서가 시즌과 상관돼 인공물이 나온다 (`strikes_before` 가 -0.993 으로
나왔다). 고유값 12개 이하는 값 그대로 쓸 것.

결과: 트랙맨 `past_*` 19개가 전부 -0.20~-0.76 이고 위험 상위 16 중 12개다.
가장 안정적인 것은 `w5_success` +0.997 / `w5_reverse` +0.995 / `balls_before` +0.992.
다만 **불안정하다고 빼면 좋아지는 것은 아니다** — `notm` 이 0 이었고 `game_type` 은
이 표에서 최악인데 제거하면 -46.7 이다.

### ⛔⛔ 홀드아웃 1점은 리더보드 **0.3점**이다 (2026-08-22, 재정정)

성공한 변경 셋의 전달률을 전부 재보니 일관된다.

| 변경 | 홀드아웃 | 리더보드 | 전달률 |
|---|---|---|---|
| 트랙맨 매핑 재구축 | +7~9 | +3.56 | 0.40~0.51 |
| 월/요일 제거 | +15 | +4.52 | 0.30 |
| **CPU 전환 (MVS + bc254)** | **+26** | **+5.17** | **0.20** |

**셋 다 0.2~0.5, 평균 0.3.**

> ⚠️ **바로 아래의 x1.26 곱셈 모형은 증분에 쓰면 안 된다.** 그건 공식 베이스라인과
> v5 의 **절대 점수 비율**이라 "파이프라인 전체 교체" 같은 큰 구조 변화에나 맞는다.
> 우리가 하는 일은 증분 개선이고, 증분의 전달률은 그 4분의 1 이다.
> (절대 비율 자체도 415.6->549.51 이 1.322, 784->990.95 가 1.264,
> 822->1002.17 이 1.219 로 **점점 떨어진다** — 관계가 오목하다.)

```
현재 1002.17 -> 100인 컷 1119.20 = 격차 117
전달률 0.3 로 환산 -> 필요한 홀드아웃 개선 +390
```

**참고로 우리 파생피처 51개 + 조건부통계 + 트랙맨을 전부 합친 것이 홀드아웃 +35 이고,
공식 베이스라인(415.6)에서 우리(790)까지의 거리 전체가 +374 다.**
즉 지금까지 만든 것을 통째로 한 번 더 만들어야 컷에 닿는다.

**왜 CPU 가 6배 과대평가됐나 (가설)**: 홀드아웃은 **학습 데이터가 18% 적은 소표본 체제**다
(~2023 1.22M vs ~2024 1.48M). `border_count=254`(더 촘촘한 분할 경계)와
`MVS`(분산 축소 샘플링)는 둘 다 **표본이 적을 때 더 이득인 추정 효율 기법**이다.
데이터가 충분하면 이미 잘 추정돼 있어 덜 중요해진다.
같은 이유로 홀드아웃이 규제를 과대평가한다 — **하나의 원인, 두 가지 증상**이다.

⚠️ **따라서 `opt254c1`(CPU Optuna 최적 조합)도 크게 할인해서 읽을 것.**
하이퍼파라미터 탐색은 정확히 이 편향의 한복판에 있다.

**CPU 학습은 유지한다** (+5.17 은 진짜다, 노이즈 ±1.5). 다만 **5.6시간을 쓸 값어치가
있는지는 별개**다 — GPU 는 63분이다. 시간이 급하면 GPU, 최종본은 CPU.

### 📐 홀드아웃 -> 리더보드는 덧셈이 아니라 곱셈이다 (2026-08-21 정정)

주최측이 공지한 **공식 베이스라인의 리더보드 점수 549.51** 이 두 번째 실측점을 줬다.
우리는 그 베이스라인을 직접 돌려 **2024 홀드아웃 415.6** 을 이미 재놨었다.

| | 홀드아웃 | 리더보드 | 차이 | 비율 |
|---|---|---|---|---|
| 공식 베이스라인 | 415.6 | 549.51 | +134 | x1.322 |
| v5 | 784 | 990.95 | +207 | x1.264 |

**덧셈 오프셋은 134 vs 207 로 크게 다르고 비율은 1.32 vs 1.26 으로 거의 같다.**
스킬 스코어는 상수 예측이 0 이므로 원점을 지나는 게 맞고, 관계는 곱셈이다.

```
100인 컷 1,119.20  ->  홀드아웃 846 ~ 891  (모형에 따라)
우리 현재                홀드아웃 790
                        필요 +57 ~ +101, 중앙값 약 +80
```

**예전에 "+122 필요" 라고 적었던 것은 +207 을 상수로 놓아서 부풀린 값이다.**
부수적으로 Murphy 결론도 약해진다 — 우리 예측을 완벽 보정하면 840 인데, 목표가 912 가
아니라 846~891 이면 840 은 그 코앞이다. 분해능 기준으로 **+1% ~ +5.4%** 가 필요하다.

### ⭐ CPU 와 GPU 는 같은 CatBoost 가 아니다 (2026-08-21)

**증상**: 완전히 동일한 파라미터·분할(행 수 1,221,585 / 253,507 일치)·isotonic 인데
CPU 스크리너 **816**, GPU 오프셋 셀 **784~797**. 차이 약 **+26** 으로 노이즈(±13)의 2배.

**원인**: CatBoost 는 처리 장치에 따라 기본값을 다르게 잡는다. CPU 에서 실제 해석된 값을
`get_all_params()` 로 확인했다.

| 파라미터 | CPU 기본 | GPU 기본 | 절개 실측 |
|---|---|---|---|
| `border_count` | 254 | 128 | 128 로 낮추면 **-12** |
| `bootstrap_type` | **MVS** | Bayesian | Bayesian 으로 바꾸면 **-10** |

**합 -22 가 관측된 격차 26 과 맞는다.** 따로 재서 더한 값이 전체와 일치하므로 우연이 아니다.

- 우리는 `bagging_temperature`(Bayesian 전용 파라미터)를 넘기고 있었는데 **CPU 는 그걸
  무시하고 MVS 를 썼다.** 같은 코드가 두 기계에서 다른 알고리즘을 돌려온 것이다.
- `border_count` 는 254 에서 포화한다 (512 는 +2).
- **MVS 는 CPU 전용이라 GPU 로는 옮길 수 없다.** GPU 에서는 `border_count=254` 한 줄로
  절반(+12)만 회수되고, 나머지 절반은 **CPU 학습으로만** 가진다.

| 구성 | 홀드아웃 | 리더보드 환산 |
|---|---|---|
| 현행 (GPU 기본값) | ~790 | 기준 |
| GPU + `border_count=254` | ~802 | +15 |
| **CPU (MVS + 254)** | **~816** | **+33** |
| CPU + Optuna 최적 파라미터 | ~822 | +41 |

로컬 16코어 실측으로 30모델에 **5.6시간**이라 하룻밤이면 된다 (`tools/mk_local.py`).

### ⚠️ Optuna 의 +39 는 대부분 GPU 결함 복구였다 (2026-08-21)

목적함수를 OOF 가 아니라 **2024 홀드아웃**으로 바꿔 GPU 에서 20 trial 을 돌렸다
(`tools/mk_optuna.py`). 첫 trial 에 현행 v4 파라미터를 강제로 넣어 대조군으로 썼다.

```
대조군(v4)  766.2      최고  805.3  (+39.1)
최고 조합: d6 lr0.0135 l2 9.8 it1400 bc128 ctr2
상위 5개가 전부 max_ctr_complexity=2
```

**그런데 그 최고 조합을 CPU 3-fold 하네스에서 재보니 822, 즉 CPU 기준선 816 대비 +6 뿐이다**
(`optbest`). 즉 +39 의 대부분은 새 파라미터 영역이 아니라 **GPU 기본값 결함의 복구**였다.
같은 이유로 "상위 5개가 전부 ctr2" 도 CPU 에서는 재현되지 않는다 (`ctr2` -5 / `ctr3` +0).

**교훈**: 하이퍼파라미터 탐색 결과를 읽을 때는 **그 하네스의 기본값이 무엇인지** 먼저
확인할 것. 탐색이 '개선' 으로 보고한 것이 사실은 '기본값 복구' 일 수 있다.

### ❌ 소속팀 제거 (`s1mt`) — 홀드아웃 -77 로 제출 전 기각 (2026-08-21)

월/요일 제거가 +4.52 였으므로 "season 과 곱해져 시즌별 칸을 외운다" 는 메커니즘이
소속팀에도 적용될 것으로 봤다. **틀렸다.**

| | 홀드아웃 | OOF Skill |
|---|---|---|
| `s1o` 기준선 | 793 | 2.173% |
| `s1m` 월/요일만 | 808 | 2.146% |
| **`s1mt` + 소속팀** | **716** | **2.091%** |

팀 정체성은 포수·투수진이 유지되므로 **전이가 잘 되는 정보**다. 홀드아웃과 OOF 가 같은
방향을 가리켰고(드문 일이다), 홀드아웃의 유일하게 신뢰할 수 있는 용도인 '크게 음수인
것을 버리는 필터' 에 정확히 해당해 제출하지 않았다.

**부작용**: 월/요일 +4.52 를 설명할 메커니즘이 없어졌다. **일회성 이득이지 더 파낼 벤이
아니다.** (더 그럴듯한 설명: `game_month` 는 '드리프트 안에서의 시점' 대리변수라,
이미 어긋난 수준 정보를 확신 있게 주입한다.)

### 📄 주최측 문제 소개 자료 검토 (2026-08-21)

`LG AI연구원 해커톤 문제 소개` PDF 18쪽. 대부분 팀·직무 소개이고 새 정보는 적다.
확인한 것 셋:

1. **기대승률(Win Expectancy)을 "경기 중요도 정보" 로 강조** — `home/away_win_expectancy`
   둘 다 이미 쓰고 있다. 원본 49컬럼 중 ID·타겟 빼고 전부 쓴다.
2. **트랙맨 신호로 구속·회전수·무브먼트·릴리스를 명시.** 그런데 우리는 `past_fb_speed_mean`
   하나만 '평균' 이고 나머지 18개는 전부 **표준편차**다. 회전수·무브먼트·릴리스의
   **수준(level)은 한 번도 안 썼다.** 미검증.
3. **코치의 질문 네 가지**(주자 상황 / 카운트 / 경기 후반 / 최근 흐름) 중 조건부 통계로
   만든 것은 카운트뿐이다. **주자 상황·이닝 축은 없다.** 미검증.

⚠️ 다만 2·3 은 **꼬리가 없는 후보**다. 우리 피처 엔지니어링 전체가 +35 인데 신규 18개가
+50 을 낼 가능성은 사실상 없다. 100인 컷이 목표라면 우선순위가 낮다.

### 시간 홀드아웃 Optuna (2026-08-20) — 2023 시즌은 튜닝에 못 쓴다

"미래 시즌 예측"을 목적함수로 삼으려고 2023 홀드아웃(~2022 학습)으로 20 trial 을 돌렸더니
**전 trial 이 음수**였고(최고 -937, 최악 -1360) 옵티마이저가 계속 더 약한 모델(depth 4,
150회 = 격자 최소점)로 기어갔다. 커널 버그가 아니다 — 같은 코드의 2024 확인 구간이 v5 실측
784 를 재현했다.

**2019~2022 로 학습한 모델은 2023 을 상수 예측보다 못 맞힌다.** 2022→2023 이 -0.0289 로
6년 중 최대 낙폭인데 학습분에 그 체제가 없기 때문이다. 2024 를 맞힐 땐 2023 이 학습에
들어가 있어서 +784 가 나온다. 실전은 후자(2024까지 학습 → 2025 예측)라 당장의 위험은 아니지만,
**2023 홀드아웃은 목적함수로 쓸 수 없다.** 여기서 나온 신규 파라미터는 2024 확인에서도
전 구간 현행보다 나빴다(신규 900회 773 vs 현행 500회 792).

### 피처 구성 감사 (2026-08-20) — 파이프라인 전체가 원본 대비 +35

같은 조건(~2023 학습 → 2024 예측, v4 파라미터, seed [42,7], 반복수 격자)에서 각자의 최적점:

| 설정 | 점수 | 최적 반복 |
|---|---|---|
| A. 원본 컬럼만 (ID 제외, 45개) | 760 | 350 |
| B. 원본 + `pitcher_id`/`batter_id` 범주형 (47개) | 640 | 250 |
| **C. 현행 파이프라인 (96개)** | **795** | 500 |

- **`step1~13` 파생피처 51개의 기여는 사실상 0.** 조건부통계 +27, 트랙맨 +9 를 합치면
  이미 +36 이라 나머지가 남지 않는다. 기존 측정치들의 산술 결과이며 모순은 없다.
- **ID 를 CatBoost 범주형으로 넣는 건 -120 이고 반복을 늘릴수록 붕괴한다**(1200회에서 212).
  과거 시즌 투수를 외운다. ID 계열은 이 방식으로는 안 된다.
  (단, 위 '앙상블 규모' 주의 — 이 실험도 모델 2개다. NN 임베딩은 별개로 봐야 한다.)

### 릴리스 동역학 (v6) — 기각 (2026-08-20)

트랙맨의 `trackman_game_id` / `pitch_no` 를 써서, 기존 (투수 x 월) 표준편차가 뭉개고 있던
세 성분을 분해했다: (a) 투구 간 기계적 흔들림 (b) 등판 간 드리프트 (c) 상황별 의도적 변화.
실제로 within(0.0304) 과 between(0.0274) 이 비슷한 크기여서 절반이 다른 성분이었다.

만든 것 12개: `seq_jump`(연속 투구 간 릴리스 이동), `within_*`, `between_*`,
터널링(구종군 간 릴리스 중심 거리), `fb_speed_decay`(등판 내 구속 감소, 평균 -0.52km/h),
`cnt_rel_gap`(불리 카운트의 릴리스 흔들림 차), `n_outing`.

6-seed 스크리닝에서 +20(±22) 이었고 절대점수 기준 신규 최저(736)가 기준선 평균(728)보다
높아 유망해 보였으나, **리더보드 -2.48**. 코드는 `build_release_dynamics` 로 보존하고
`USE_RELEASE_DYNAMICS=False` 로 꺼두었다.

**교훈**: 트랙맨은 매핑을 고쳐도(+3.56) 파생을 정교화해도(-2.48) 더 나오지 않는다.
이 방향은 소진된 것으로 본다.

### `asof_*` 재설계 — 기각 (2026-08-20)

가장 크게 기대했던 카드였다. 운영측 `asof_pitcher_success_rate`가 커리어 누적이라
드리프트를 못 따라가고 2024에 +0.0253 과대평가하니, **최근 N구 이동평균**으로
다시 만들면 되지 않겠느냐는 발상. train 은 투수별 완전 시간순이고 `asof_pitcher_n`이
행마다 정확히 +1 이라 최근 N구 윈도우를 정확히 계산할 수 있다 (확인함).

문제는 **추론 시점에 쓸 수 있는 값**이다. test(2025)는 자기들끼리 누적을 못 만드니
(규정) 각 투수의 2024 말 값으로 고정된다. 그래서 두 설계를 다 쟀다.

| 설정 | 원본 | 재중심화 |
|---|---|---|
| A. roll300 **fresh** (학습만 매투구 갱신, 검증은 고정) | **-217(±15)** | **-244(±17)** |
| B. roll100 frozen | -17(±10) | -5(±8) |
| B. roll300 frozen | -22(±11) | -8(±15) |
| B. roll1000 frozen | -21(±7) | -10(±9) |
| B. ewm500 frozen | -38(±6) | -27(±6) |
| B. roll300 디트렌드 frozen | -16(±13) | -9(±11) |

- **A 의 -217 이 이 프로젝트에서 학습/추론 분포 불일치의 대가를 처음으로 정량화한 값이다.**
  학습행이 매 투구 갱신되는 값을 보고 검증행은 1년 묵은 고정값을 받으면 이만큼 무너진다.
  트랙맨 `exact` 모드(897점)와 같은 실패이며, 앞으로 "학습에서만 신선한 피처"는 만들지 말 것.
- **B 는 전부 소폭 마이너스.** 원인은 중복으로 보인다 — `cond_p`가 이미 같은 신호를
  시즌 디트렌드 + 0으로 shrink + 조건부까지 해서 담고 있는데, 여기 얹은 건 shrink 없는
  날것의 이동평균이라 저경험 투수에서 노이즈만 늘린다. `asof_*` 시즌 디트렌드가
  -13 이었던 것과 같은 패턴.
- 수준 편향도 완전히는 안 잡힌다 (2024 실제 0.4861 / 운영측 0.5114 /
  roll100 frozen 0.5043 / roll1000 frozen 0.5122).
- 구현 한계 하나: 고정값을 '직전 시즌 말'에서만 가져와 2023 을 통째로 쉰 투수는
  2022 기록이 있어도 NaN (2024 결측 24.4%). 캐리포워드로 고치면 검정력이 조금 오르나
  최선이 -5 라 부호를 뒤집을 크기는 아니다.

**남은 불씨**: "최근성"을 새 피처로 얹지 말고 **이미 성공한 `cond_*` 안에 시즌 감쇠
가중**으로 넣는 것. 피처 수가 안 늘고 중복도 안 생긴다. 미측정.

### 실행 불가능으로 확인된 것 (로드맵에서 제외)

- ~~**`middle_rate`/`reverse_rate` 분해 보조모델**: 투구별 실패유형 라벨이 없다~~
  ⛔ **2026-08-24 정정 — 틀렸다. 완벽하게 복원된다** (`tools/eda40.py`, 팀원 송도원 제안).
  이 항목은 `wseason5`(시즌 단위 분해) 이전에 쓴 것이다. 같은 트릭을 **투구 단위**로
  내리면 된다 — `asof_pitcher_n` 이 투수 내에서 정확히 +1 씩 증가하기 때문이다.
  ```
  투수 내 인접 행 n 증분 +1              1,474,300 / 1,474,300 = 1.000000
  다섯 라벨 전부 누적개수 차분이 {0,1}                        = 1.000000
  ★ 복원 success vs control_success 일치율                 = 1.000000  (147만 행)
  ```
  `asof` 는 '직전까지' 이므로 행 i 와 i+1 로 **투구 i** 의 라벨이 나온다.
  `success` 는 정답을 알아서 **검산이 된다** — 147만 행 전부 일치했다.
  구조: 성공 행에서 middle/reverse 는 정확히 0 (실패에서만 발생, 완전 중첩).
  실패 중 middle .314 / reverse .481, 나머지 .205 가 '크게 벗어남'.
  ball .370 / strike .443, 나머지 .187 이 인플레이.
  **이진 타겟 하나가 4분할 실패유형 + 3분할 판정으로 늘어난다.**
  ⚠️ 규정: 차분은 **train 에서만**. test 인접 행 차분은 주최측이 '규칙 위반' 이라
     답한 사안이다(2jin1 08-17). train 유래 값에는 제약이 없다(DACON.GM 08-19).
  ⚠️ 행 순서: `(pitcher_id, asof_pitcher_n)` 정렬이 전제다. `run_full_pipeline` 은
     `time_idx` 로 정렬하고 원복하지 않는다(4-15). `pf_*` 에서 이 함정에 그대로 빠졌다.
     **success 검산(일치율 1.0)을 assert 로 박을 것.**
- **경기내 피로도(투구수/이닝)**: train/test 에 경기 ID도 날짜도 없고, test 내부 행 순서
  사용은 규정 금지. (등판 *사이의* 휴식은 트랙맨 `game_date` 로 가능하다 — 위 1장 참고.)

### 노트북 계보 (혼동 방지용)

**루트에는 현행 제출본 하나만 둔다.** 나머지는 전부 `experiments/` 아래다
(2026-08-29 정리. 그전에는 루트에 노트북 22개가 쌓여 있었다).

| 파일 | 버전 | 리더보드 |
|---|---|---|
| **`aimers_v10wzc.ipynb`** | **v10wzc — 현행 최고** | **1,121.8905** |
| `experiments/v10w/aimers_v10wz.ipynb` | 5분류 타겟 | 1,116.1659 |
| `experiments/v10w/aimers_v10wq.ipynb` | 이진 + aux 2종 | 1,103.3169 |
| `experiments/archive/aimers_v9m.ipynb` | v9m | 996.9977 |
| `experiments/archive/aimers_tuned_ensemble.ipynb` | v5 | 990.9528 |

⚠️ **빌더의 `BASE`/`OUT` 기본값도 같이 옮겨졌다.** `tools/mk_*.py` 는 노트북을 맨
파일명으로 물고 있었으므로 이동과 함께 16개 파일의 경로를 고쳤고, 모든 `BASE`
기본값이 실제로 존재하는지 검증했다(누락 0건). 새 빌더를 쓸 때 base 를 지정하려면
`experiments/v10w/` 경로를 쓸 것.

```
aimers_v10wzc.ipynb             본 학습 (현행 최고, 1,121.8905)
claude.md  README.md            문서
tools/                          빌더 · EDA · 검증 도구
experiments/
  v10w/       v10w 계열 19개 (v10w~v10wzb, ptc6, ptc15, mcbase)
  archive/    v9m · tuned_ensemble · skfold · offset
  capacity/   v7 · converge · loss · topt        반복수·손실함수·튜닝 (전부 소진)
  features/   baseline · asof_screen · tmdyn · bat · pct
  models/     fam · fam2 · fam3 · nn · nn3
  colab/      코랩 실행본
```

나머지 셋은 꺼져 있을 때 v5 와 **완전히 같은 경로**를 탄다 — 절개 실험에서
`COND_DECAY=1.0` 일 때 `cond_p.csv` 가 v5 와 비트 단위로 동일함을 확인했다.
따라서 v9m 과 v5 의 행동 차이는 `DROP_CAL` 하나뿐이다.

나머지 진단·스크리닝 노트북은 전부 `experiments/` 아래에 있다.
각 노트북이 무엇을 재서 무슨 결론이 났는지는 **`experiments/README.md`** 에 한 줄씩 있다.

```
aimers_v9m.ipynb                본 학습 (v9m, 리더보드 996.9977) — 현행
aimers_tuned_ensemble.ipynb     이전 기준선 (v5, 990.9528)
tools/                          매핑 재구축, 공식 베이스라인 재현
experiments/
  capacity/   v7 · converge · loss · topt        반복수·손실함수·튜닝 (전부 소진)
  features/   baseline · asof_screen · tmdyn · bat · pct
  models/     fam · fam2 · fam3 · nn · nn3
  archive/    skfold(932.96 기반) · offset(축소 커널)
```

| 버전 | 내용 | 점수 |
|---|---|---|
| **v10wp (현행)** | v10wb + prev-game 경계 보정 | **1089.8100** (+0.98 = 노이즈) |
| wbc20 | v10wb + CPU 학습 20모델 | 1085.8832 (**-2.95**) |
| v10wb | v10w + 타자측 당해시즌 복원 (GPU) | 1088.8317 |
| submit_cpu30 | v10w + CPU 학습 30모델 | 1066.5937 |
| v10w_fixed | v9m + 당해 시즌 복원(wseason5) | 1058.7184 |
| local_cpu | v9m + CPU 학습(MVS + bc254) | 1002.1710 |
| v9m | v5 + 월/요일 제거 | 996.9977 |
| v5 | v4 + 트랙맨 매핑 재구축 | 990.9528 |
| v4 | Optuna + fold10 + seed3 + 조건부통계 + 재중심화 | 987.3936 |
| v3 | 조건부통계·재중심화 없던 버전 | 959.4258 |
| v6 | 릴리스 동역학 12개 | 988.4720 ❌ |
| v7 | 반복수 700 / 500 | 986.21 / 967.89 ❌ |

**Kaggle 커널 실행 참고**: `homekeggle/aimers-skfold`(→`aimers_skfold.ipynb`)와
`homekeggle/aimers-tuned-ensemble`(→`aimers_tuned_ensemble.ipynb`) 두 커널로 API push/pull
워크플로가 구축됨 (`kernel-metadata.json`, 리포 루트 — 현재는 skfold를 가리킴, 다른 노트북 돌릴
땐 `code_file`/`id` 바꿔서 push). `kaggle kernels push -p .` → 상태 폴링 →
`kaggle kernels output <kernel> -p ./kaggle_output`로 결과 회수.
**Windows(cp949 로케일)에서는 반드시 `PYTHONUTF8=1` 환경변수를 붙여서 실행할 것** — 안 붙이면
노트북의 한글/특수문자(em dash 등)를 cp949로 디코딩하려다 크래시남
(`'cp949' codec can't decode byte ... illegal multibyte sequence`).

**GPU 종류(P100 vs T4 x2) API로 선택 불가**: 공식 `machine_shape` 필드가 존재하긴 하는데
(GitHub `Kaggle/kaggle-api` `main` 브랜치 문서 기준) PyPI에 릴리스된 `kaggle` 패키지(현재 최신
1.7.4.5 포함)엔 아직 구현이 안 들어가 있음 — 웹 에디터에서 "Save & Run All"로 인터랙티브하게
돌릴 때만 T4 x2 선택 가능, API push는 항상 계정 기본값(P100)으로 감. CatBoost는 `devices`
미지정 시 GPU를 전부 자동으로 쓰므로, T4 x2를 쓰면 코드 수정 없이 더 빨라질 여지는 있음
(단, 이번엔 P100 기준으로 학습해서 959.43을 얻었으므로 성능 자체엔 문제없음).

---

## 4. 겪었던 핵심 버그들 (재발 방지용 — 반드시 숙지)

### 4-1. `prior_mean` 미저장
`step7_bayesian_smoothing`의 `prior_mean`은 학습 시 `train.csv`에서 계산한 값을
`model/train_constants.json`에 **반드시 저장**해야 한다. 저장 안 하면 추론이 하드코딩
기본값으로 폴백 → 핵심 피처(중요도 상위권)가 학습/추론 간 어긋남.
~~`asof_pitcher_success_rate.mean()`이 아니라 `control_success.mean()`을 써야 함~~
→ **2026-08-19 측정 결과 무관함이 확인돼 정정.** 3안을 2024 홀드아웃 3-seed 짝지어 비교:

| prior | 값 | 기준선(현행) 대비 |
|---|---|---|
| A 현행 `asof_pitcher_success_rate.mean()` | 0.535228 | (기준) |
| B `control_success.mean()` | 0.523766 | 원본 +5(±9) / 재중심화 **-2**(±13) |
| C 시즌별 리그평균 (2024는 외삽 0.4877) | 시즌마다 | 원본 +13(±22) / 재중심화 +5(±20) |

셋 다 노이즈 안이고 B는 부호조차 안 맞는다. 이유: `smoothed = (n*curr + 50*prior)/(n+50)`
인데 `asof_pitcher_n < 200`인 행이 **9.0%**뿐이라 prior 가 힘을 쓰는 구간이 좁다.
A와 B는 값 차이가 0.011이라 최대 변화폭 0.0112로 사실상 같은 피처다.
C는 최대 0.0466까지 벌어지는데도 효과가 없는데, 전체 피처를 쓰면 모델이 드리프트를
다른 경로로 이미 흡수하기 때문으로 보인다 (4-11과 같은 패턴).
**현행 A를 유지한다. 다만 "반드시 저장해야 한다"는 본문은 그대로 유효하다.**

### 4-2. `.dropna()`로 트랙맨 테이블 가공 금지
`step16~18` 출력을 `dropna()`해서 저장하면, 학습 때 NaN이던 자리가 추론 때
merge_asof로 "더 과거의 다른 값"을 끌어와 채워버림. **원본 그대로 저장.**

### 4-3. `count_advantage`의 `'None'` → NaN 자동 변환
`'None'`은 0-0/3-2 카운트를 뜻하는 **실제 문자열**(전체의 ~25%)인데, `pd.read_csv` 기본
설정이 이를 NaN으로 읽어버려 `by=['pitcher_id','count_advantage']` merge가 전량 실패한다.

```python
_NA = ['', 'NaN', 'nan', 'NULL', 'null', 'NA', 'N/A', 'n/a']  # 'None' 제외!
pd.read_csv(path, keep_default_na=False, na_values=_NA)
```

### 4-4. CatBoost 범주형 NaN 처리
CatBoost는 `cat_features`에 실제 float NaN이 있으면 에러를 던진다.
`astype(str).astype('category')`로 NaN을 문자열 `'nan'`으로 변환해야 함 (버그 아니라 필수 처리).

### 4-5. LightGBM 네이티브 포맷 관련 (LightGBM 재도입 시 주의)
- `lgb.Booster.feature_name()` 호출이 환경에 따라 access violation(세그폴트급 크래시)을 일으킨 적 있음 → 호출 자체를 피하고 `selected_features.json` 순서를 그대로 신뢰할 것.
- `.txt` 네이티브 저장은 `pandas_categorical` 매핑을 이미 자동으로 포함/복원함 — 수동으로 덮어쓰지 말 것 (덮어쓰다가 오히려 크래시 유발한 적 있음).
- 두 라이브러리(CatBoost+LightGBM)를 같은 프로세스에서 같이 쓸 때 Windows에서 OpenMP 충돌로 크래시 나는 사례 있었음 (`KMP_DUPLICATE_LIB_OK=TRUE`로 완화 시도, 리눅스 서버에서는 미재현 — 로컬 Windows 리허설 결과를 실제 서버 결과와 혼동하지 말 것).

### 4-6. 모델이 안 불러와지면 조용히 0으로 폴백 금지
```python
if len(preds) == 0:
    raise RuntimeError(f"모델 로드 실패. model 폴더 내용: {os.listdir('model')}")
```
과거에 zip에 모델 파일이 누락됐는데도 크래시 없이 `final_preds=0.01`로 채워져
"정상 종료 + 사실상 0점"이라는, 원인 특정이 가장 어려운 실패 유형을 겪은 적 있음.

### 4-7. 검증 방법론 자체의 함정
- `TimeSeriesSplit` 홀드아웃 검증은 정직하지만, "최근 데이터를 더 많이 학습에 쓸수록 좋다"는
  사실은 과소평가한다 (최근 데이터를 검증용으로 떼어두게 되므로).
- **`StratifiedKFold` 채택 이후 로컬 홀드아웃 검증은 완전히 무효화됨** (모델이 검증 데이터를
  이미 학습에 사용). 로컬 검증 셀은 "파이프라인이 안 죽는지, 피처가 상식적인지" 확인 용도로만
  쓰고, 설정 간 우열은 **리더보드로만** 판단할 것.
- 단, 하이퍼파라미터 튜닝(Optuna)은 예외 — "이 파라미터가 데이터를 잘 맞히는가"는
  `StratifiedKFold`로도 정직하게 측정 가능.
- 로컬 검증 시 "최근 데이터의 X%"를 행 개수 기준으로 자르면, 연도별 데이터량 편차 때문에
  실제로는 최근 시즌의 극히 일부만 떼어지는 함정이 있음 (로컬 1054 vs 실제 810으로 샌 사례).
  **반드시 시즌 단위로 통째로 분할할 것.**
- **로컬 검증 시 학습/추론 경로를 각각 따로 실행해서 "평균값"만 비교하지 말 것.**
  행이 뒤섞여도 평균은 비슷하게 나올 수 있다. **반드시 같은 `row_id` 기준으로 행 단위(1:1) 비교.**

### 4-8. Optuna로 찾은 `best_params`에 `cat_features` 누락 (`aimers_tuned_ensemble.ipynb`)
`study.best_params`는 `trial.suggest_*`로 탐색한 값만 담고 있다. 최종 학습용 파라미터 딕셔너리를
`BEST_PARAMS = dict(study.best_params)` + 수동으로 `iterations`/`eval_metric`/`task_type`/
`early_stopping_rounds`만 추가해서 만들면 **`cat_features`가 빠진다.** CatBoost는 pandas
`category` dtype 컬럼이 있는데 `cat_features`가 없으면 자동 인식이 아니라 즉시
`CatBoostError`를 던진다 (직접 재현 확인). Optuna 탐색용 `objective()` 내부에서는 명시적으로
`cat_features`를 넘겨서 멀쩡히 돌아가므로, **탐색 단계는 무사통과하고 최종 학습(30개 모델) 첫
fold에서 크래시** — 방치하면 GPU 쿼터만 태우고 아무 것도 못 건진다.
**수정**: `BEST_PARAMS["cat_features"] = cat_features`를 반드시 추가.

### 4-9. `StratifiedKFold.split()`의 반환 순서 착각으로 서브샘플 비율이 의도보다 커짐
`skf.split(X, y)`는 `(train_idx, test_idx)` 순서로 반환한다. "30%만 빠르게 서브샘플링"하려고
```python
_sub_idx, _ = next(StratifiedKFold(n_splits=3, ...).split(X_full, y_full))  # 버그: train_idx(약 67%)를 씀
```
처럼 **첫 번째 원소**를 받으면 의도한 33%가 아니라 **약 67%**가 뽑힌다. 크래시는 안 나서
발견하기 어렵고, Optuna 40 trial × 3-fold가 조용히 2배 이상 느려진다.
**수정**: `_, _sub_idx = next(...)` (두 번째 원소가 test_idx ≈ 33%).

---

### 4-10. ⚠️ 단일 seed 홀드아웃 비교는 ±32점 노이즈 — 피처 판정에 쓰면 안 됨
2024 홀드아웃에서 **완전히 동일한 설정을 seed만 바꿔 돌리면 685 vs 717로 32점이 움직인다.**
이것 때문에 조건부 투수통계를 단일 seed로 평가했을 때 "효과 ±0"으로 잘못 판정할 뻔했다
(하필 기준선이 가장 잘 나온 seed와 붙었음). **같은 seed로 짝지어(paired) 최소 3회 반복**하고
차이의 평균과 표준편차를 함께 볼 것. 짝지으면 seed 효과가 상쇄돼 검정력이 크게 올라간다.

### 4-11. ⚠️ 피처를 뺀 축약 모델로 설계 결정을 내리지 말 것 (두 번 오도당함)
`step1~13` 파생피처 없이 원시 컬럼만으로 돌린 축약 실험이 **두 가지 결론을 모두 반대로** 만들었다.

| 항목 | 축약 모델 결론 | 실제(전체 피처) |
|---|---|---|
| 재중심화 이득 | +224~301점 | **+18점** |
| 최근 시즌 편중 학습 | +224점 (좋음) | **-30~-120점 (나쁨)** |

이유: 축약 모델은 `smoothed_pitcher_success_rate`/모멘텀 피처가 없어 드리프트를 못 따라가므로,
평균예측이 0.5107로 크게 뜨고(전체 피처는 0.4940) "최근 데이터만 쓰기"가 그 결함의 임시방편으로
작동했다. 전체 피처가 있으면 데이터 양이 최신성을 이긴다. **속도 때문에 피처를 빼는 순간
결론의 방향 자체가 뒤집힐 수 있다.**

### 4-12. 학습 뒤쪽 셀의 사소한 NameError로 2시간 GPU를 날릴 뻔함 (+ 회수 방법)

v4 실행에서 재중심화 셀이 `RANDOM_SEED`(정의된 적 없는 이름)를 참조해
**30개 모델 학습이 다 끝난 직후** `NameError`로 죽었다. 캐글 커널은 ERROR로 종료됐다.

- **교훈 1**: 학습(1시간+) *뒤에* 오는 셀은 반드시 먼저 문법·이름 검사를 하고 push할 것.
  `python -c "import ast; ast.parse(open('x.py').read())"`는 NameError를 못 잡는다.
  셀 안에서 쓰는 전역 이름이 앞 셀에 실제로 있는지 눈으로 확인하는 수밖에 없다.
  (이 노트북의 seed 변수명은 `SEEDS` 리스트다. 단일 seed가 필요하면 `SEEDS[0]`.)
- **교훈 2 (회수 방법)**: 크래시해도 **`kaggle kernels output`으로 산출물은 회수된다.**
  모델 30개 + isotonic 30개 + 룩업 테이블이 전부 남아 있었다. 전체 재실행(2시간) 대신
  **부족한 것만 만드는 축소 커널**을 따로 돌리는 게 맞다:
  Optuna(70분)는 저장된 `best_params.json`을 그대로 읽어 생략, 최종 학습(56분)도 생략,
  전처리(83초) + 오프셋 3 fold(4분)만 → **5분 30초**에 끝났다 (`aimers_offset.ipynb`).
  전처리가 결정적이라 룩업 테이블 7개가 원본과 **md5까지 동일**함을 확인하고 합쳤다.
- **교훈 3**: 로컬에서 제출 zip을 조립할 때는 오프셋이 `script.py`까지 실제로 전달되는지
  **오프셋 0으로 바꾼 대조 실행과 평균예측을 비교**해 확인할 것. 상수가 조용히 무시돼도
  크래시는 안 난다 (4-6과 같은 유형).

### 4-13. 노트북을 bash heredoc 으로 편집하지 말 것

`python - <<'PY' ... PY` 로 노트북 셀을 패치했더니 **백슬래시가 한 겹 벗겨져
`print("\n=== ...")` 의 `\n` 이 실제 줄바꿈이 됐고, 셀이 문법 오류로 깨졌다.**
따옴표로 감싼 heredoc(`<<'PY'`)인데도 이 환경에서는 리터럴이 보장되지 않는다.

**패치 스크립트는 반드시 파일로 써서 실행할 것** (`python scratchpad/patch.py`).
그리고 노트북을 건드린 뒤에는 항상 전 셀 문법 검사를 돌린다:

```python
import ast, json
nb = json.load(open('x.ipynb', encoding='utf-8'))
for c in nb['cells']:
    if c['cell_type'] == 'code':
        ast.parse(''.join(c['source']))
```

### 4-14. 변경 하나를 측정할 땐 Optuna 를 끌 것 (`RUN_OPTUNA`)

v4 는 Optuna 를 재탐색해서 파라미터가 같이 바뀌었다(depth 9→8, lr 0.0247→0.0228).
그래서 987.39 라는 결과가 "세 가지 피처 변경" 때문인지 "파라미터 변화" 때문인지
구분할 수 없었다. v5 부터는 설정 셀의 `RUN_OPTUNA = False` 로 v4 파라미터를 고정한다
(`V4_BEST_PARAMS`). 덕분에 v5 의 +3.56 은 트랙맨 매핑 하나의 효과로 귀결된다.
부수 효과로 실행시간이 **128분 → 62.7분**으로 줄었다 (Optuna 가 70분을 먹고 있었다).

Optuna 를 다시 켤 때는 그것 하나만 바꿔서 돌릴 것.

### 4-15. `run_full_pipeline` 은 행 순서를 바꾼다 — `y` 를 `df_train` 에서 가져오지 말 것

`trackman_mode='asof'` 분기가 `merge_asof` 를 위해 `df_proc.sort_values('time_idx')` 를 하고
**원복하지 않는다** (원복은 추론용 `script.py` 쪽에만 `__orig` 로 들어있다).
따라서 `df_processed` 의 행 순서는 `df_train` 과 다르다.

진단 커널에서 `X` 는 `df_processed`, `y` 와 `season` 은 `df_train` 에서 가져왔다가
**라벨과 분할 마스크가 통째로 어긋났다.** 크래시는 없고 점수만 0 근처로 나온다
(원본 -1 ~ +5). 실험 하나를 통째로 날렸다.

```python
y = df_processed['control_success']            # O
season = df_processed['season'].to_numpy()     # O
y = df_train['control_success']                # X — 순서가 다르다
```

**자체 검증을 넣을 것.** v5 는 이 홀드아웃에서 784 가 나오는 게 알려져 있으므로, 현행
구성이 그 근처를 재현하는지 먼저 확인하고 나머지를 판단한다. 이번엔 그 자체 검증 덕분에
표를 못 믿는다는 걸 즉시 알았다.

## 5. 제출 파이프라인 (`script.py`) 필수 구조

```python
import os
os.environ.setdefault("KMP_DUPLICATE_LIB_OK", "TRUE")  # 최상단, import보다 먼저

# STEPS_SRC 텍스트 그대로 삽입 (학습 노트북과 동일 소스 공유)

def main():
    # 1. test.csv 로드 (data/ 또는 open/ 후보 경로 탐색)
    # 2. model/train_constants.json에서 prior_mean 로드 (없으면 RuntimeError)
    # 3. step1~13 피처 엔지니어링
    # 4. model/feat_diff.csv, feat_speed.csv, feat_rp.csv와 merge_asof
    #    (na_values 명시! count_advantage 'None' 보존 필수)
    # 5. step14 범주형 변환 + astype(str) NaN 처리
    # 6. model/cb_fold_*.cbm 을 glob으로 스캔해서 전부 로드 + isotonic 보정 + 평균
    #    (개수 하드코딩 금지 -> N_SPLITS/SEEDS 바뀌어도 script.py 수정 불필요)
    # 7. 모델 0개면 RuntimeError로 명시적 실패 (조용히 0.01 폴백 금지)
    # 8. output/submission.csv 저장

if __name__ == "__main__":
    try:
        main()
    except Exception:
        os.makedirs("output", exist_ok=True)
        with open("output/error_log.txt", "w") as f:
            f.write(traceback.format_exc())
        raise
```

### 필수 제출 zip 구성
```
script.py, requirements.txt
model/cb_fold_{seed}_{fold}.cbm  (N_SPLITS x len(SEEDS)개)
model/isotonic_fold_{seed}_{fold}.pkl  (동일 개수)
model/selected_features.json, model/train_constants.json, model/best_params.json
model/feat_diff.csv, model/feat_speed.csv, model/feat_rp.csv
```

### 제출 전 필수 검증
학습 노트북 안에서 `script.py`를 **실제로 서브프로세스 실행**해서, 정답을 아는 홀드아웃
시즌으로 Brier/Skill을 직접 채점하는 검증 셀을 반드시 통과시킬 것. "안 죽는지"만 확인하는 건
불충분함 — 과거에 크래시 없이 정상 종료되지만 skill이 naive보다 나쁜(≈0점) 사고가 여러 번 있었음.

### 평가 서버 환경 (주최측 공지, 2026-08-18 확인)

인터넷 접속 불가(외부 다운로드 코드/모델 동작 안 함). 아래 패키지가 **버전 고정된 채로 이미
설치**되어 있음 — `requirements.txt`에 다시 적지 말 것 (버전이 어긋나면 설치 에러 발생):

```
pandas==2.0.3, numpy==1.26.4, scipy==1.15.3, scikit-learn==1.8.0, joblib==1.5.3,
threadpoolctl==3.6.0, torch==2.7.1+cu128, transformers==4.46.3 등
```

현재 `requirements.txt`는 `catboost` 한 줄만 있음 — 목록에 없으므로 맞는 구성.
**LightGBM 재도입 시 `lightgbm`을 여기 추가해야 함** (사전 설치 목록에 없음).

**제출 오류는 두 종류이며 일일 제출 횟수 반영 여부가 다르다:**
- **설치 오류** (zip 구조 불일치, 패키지 설치 실패) → 일일 제출 횟수에 **반영 안 됨**
- **제출 오류** (`script.py` 실행 중 발생하는 모든 오류) → 일일 제출 횟수에 **반영됨**

즉 런타임 버그로 인한 실패는 그날의 제출 기회를 실제로 태운다 — 위 "제출 전 필수 검증"이
단순 권장이 아니라 **일일 제출 횟수를 아끼기 위한 필수 절차**인 이유.

---

## 6. 로드맵 (마감 9/2)

| 기간 | 작업 |
|---|---|
| ~8/18 | ✅ Optuna 튜닝 + fold10 + seed3 앙상블 — 959.4258 |
| 8/19 | ✅ v4(조건부통계+재중심화+죽은피처제거) 987.3936 / ✅ v5(트랙맨 매핑 v2) — **990.9528로 채택** |
| 8/20 | ❌ `asof_*` 재설계 기각 (A -217 / B -5~-27). 3장 별도 항목 참고 |
| 8/20 | ❌ v6 릴리스 동역학 기각 (988.4720). 트랙맨 방향 소진 판단 |
| 8/20 | ❌ RMSE 손실 기각 (Logloss 와 동점). ❌ 2023 홀드아웃 Optuna 기각. ✅ 피처 구성 감사(+35) |
| 8/20 | ❌ v7 반복수 축소 기각 (986.21 / 967.89). **앙상블 규모 오류 발견 — 3장 참고** |
| 8/21 | ❌ depth 9 (-0.57) / ❌ seed 10 (-1.26). **CatBoost 용량 축 소진 확정 — 3장 참고** |
| 8/21 | ❌ 트리 계열 다양화 종료 (LightGBM/XGBoost/Lossguide 블렌드 +7, 상관 0.92~0.97). ❌ 앙상블 규모 가설 기각 |
| 8/21 | 📏 공식 베이스라인 실측 415.6 (우리 803 = 2배). ❌ NN 1차 실패(단독 365) — 단 상관 0.67 로 다양성은 확인 |
| 8/21 | ❌ NN v3 (단독 519, 상관 0.78, 블렌드 +1 — 좋아질수록 트리와 닮아간다). ❌ 백분위 변환 +0. ❌ 타자측 +1 |
| 8/21 | ⛔ **보정 축 완전 종료** — 천장 51점, 실행 가능 접근은 -48~-566. ❌ season 제거 -424. ❌ 수축 가설 기각(λ*=0.96). ✅ 편향 시계열: b ~ 하락폭 확인, 현행 상수가 이미 최적(+1) |
| 8/21 | ❌ **v8 968.5952 (-22.35)** — 네 가지 동시 투입. 홀드아웃 +27 이었다. 절개 실험 4건 야간 실행 |
| 8/21 | ✅ **절개 5건 완료.** 월/요일 +4.52 (유일한 양수) / 맞대결 -3.51 / 휴식·파울 -6.85 / 감쇠 -11.05 |
| 8/21 | 🔧 **스크리너 도입** — 후보를 제출 없이 25분에 판정. 첫날 후보 10개 전부 노이즈/음수 |
| 8/21 | 📐 격차 재계산: 베이스라인 549.51 실측 반영 -> 필요치 **홀드아웃 +122 -> +80** |
| 8/21 | ⭐ **CPU/GPU 분해** — `border_count` -12 + `bootstrap_type`(MVS) -10 = 격차 26 설명. **CPU 학습으로 +33(LB 환산)** |
| 8/21 | ❌ `s1mt` 소속팀 제거 홀드아웃 -77 로 제출 전 기각. ❌ 전이 공격 종료(-183) |
| 8/22 | ❌ **`s1n` 979.93 (-7.95)** — 정규화 없는 감쇠도 음수. 최근성 가중 축 완전 종료(4번째). 내 '정규화가 원인' 진단은 11.05 중 3.11 만 맞았다 |
| 8/22 | ✅ 캐글 CPU 커널 **동시 실행** 확인 (base 813 = 로컬 816). 1 seed 10모델 = 4.5시간이라 **커널 하나가 하룻밤에 제출 후보 하나**를 만든다 — 검증을 리더보드로 옮길 수 있다 |
| 8/22 | ✅ **`v9m` 996.9977 (+6.04) — 새 최고점.** v5 이후 첫 전진 |
| 8/22 | ❌ `s1lr` 미제출 확정 (v9m 과 예측 상관 **0.9704**, 스크리너 +7). 선형 baseline 축 완전 종료 |
| 8/22 | ✅ **`local_cpu` 1002.1710 (+5.17)** — 처음으로 1000 돌파. 단 기대 +33 의 6분의 1 |
| 8/22 | ⛔⛔ **증분 전달률 0.3 확정.** 필요치가 홀드아웃 +80 -> **+390** 으로 재계산됐다 |
| 8/23 | ⭐⭐⭐ **`v10w_fixed` 1058.7184 = v9m +61.72.** 역대 최대 도약. 스크리너 +72.2 의 **0.855** 가 전달됐다 |
| 8/23 | ⛔ **'전달률 0.3' 폐기.** 종류별로 다르다 — 새 정보 0.85 / 버그 수정 0.45 / 파라미터·피처제거 0.25 |
| 8/23 | ❌ 구종 성향 4후보 전부 0 (`wspmix` +0.9 / `wsxprob` -10.7). EDA 축약 base 오류(4-11) 였다 |
| 8/23 | ⭐ **`game_type=F` = 퓨처스, 2023 체제 변경.** R 전용 2022->23 낙폭은 **-0.0006** — 드리프트 서사 정정 |
| 8/23 | 🐛 트랙맨 `game_date` 이중 포맷 (2022~24 전부 NaT). 살아있는 코드는 무사하나 **`s1r` 기각은 무효** |
| 8/23 | ✅ 경기 복원 4,868개(R 720/시즌). teacher 상한 경기그룹 fold 로 **+581~+702** (누수는 25%) |
| 8/23 | ❌ **game_type 디트렌드 축 종료** — ws5gt -17.8 / condgt -8.2 / bothgt -1.8. 상관은 +0.260->+0.477 였다 |
| 8/23 | ✅ **CPU 30모델 조립 완료** (`out/submit_cpu30.zip`). ws_* 복구 + 독립성 통과 + wseason 생존 대조군 확인 |
| 8/23 | 🐛 **커널 산출물의 ws_* 소실 버그를 조립 단계에서 잡았다** — 그대로 올렸으면 61점짜리 사고였다 |
| 8/23 | ⛔ **대리지표(상관·R2)로 후보를 채택하지 말 것.** 디트렌드 '정정' 은 두 번 다 음수(-13, -17.8) |
| 8/23 | ⭐⭐ **`submit_cpu30` 1066.5937 = v10w +7.88.** CPU 전환(오프셋 3개 평균) |
| 8/23 | ⭐ **2023 이 검증 시즌으로 부활** (F 제외). wseason5 가 거기서 +182.1 — 세 번째 독립 확인 |
| 8/23 | ❌ 밤샘 후보 5개 전부 0 이하: wsbat +3.0 / ws5c10 +0.9 / ws5c30 -7.2 / **ws5nocond -12.4** / ws5c300 -24.7 |
| 8/23 | ⛔ shrink C 축 종료 · `cond_*` 잉여 가설 기각 · 타자축 종료 |
| 8/23 | ⭐⭐ **`v10wb` 1088.8317 = v10w +30.11.** 타자측 당해시즌 복원 |
| 8/23 | ⛔⛔⛔ **2024 홀드아웃이 거짓 음성을 냈다** — wsbat 은 2024 +3.0 / 2023 +71.7 / LB +30.11 |
| 8/23 | ⭐ **타자 축은 닫혀 있지 않았다.** '27배 비대칭' 단정의 근거가 전부 2024 홀드아웃이었다 |
| 8/23 | ❌ `tskill` 876.3 (-9.3) — 구종 성향 축 세 형태 모두 0 이하, 완전 종료 |
| 8/23 | 📐 **100인 컷 정정: 팀 66위 = 1,136.61** (리더보드가 팀 멤버를 표시. 상위권에 4~5인팀 다수) |
| 8/23 | ❌ **스크리너 15건 전부 0 이하** — 그런데 같은 하네스에서 wsbat 이 +3.0 이고 LB 는 +30.11 이었다 |
| 8/23 | ⛔ **스크리너를 관문으로 쓰지 않는다.** 분해능 ±10 < 목표 +20~30. 재앙 필터로만 쓰고 LB 로 판정 |
| 8/23 | ⛔ `season` 축 종료: 배포 모델 경계 최대 2023.5 = 2025 는 2024 와 비트 단위 동일 (eda31). `seasonlast2` **-283** |
| 8/23 | ❌ `condb`/`wsbsit` 두 시즌 합의로 기각. 타자 축은 `wsbat` 하나로 끝났다 |
| 8/24 | ❌ **`v10wp` 1089.8100 (+0.98)** — prev-game 보정은 노이즈. 오염은 실재하나 `w_n` 이 이미 처리하고 있었다 |
| 8/24 | 🐛 queue_push 가 RUNNING 커널을 다시 push 해 wbcc(4.5h)를 네 번 재시작시켰다. 사용자가 버전 6개를 발견 |
| 8/24 | ⛔⛔ **`wbc20` 1085.88 (-2.95)** — CPU 학습 이득이 사라졌다. 피처가 좋아지자 추정효율 기법이 무의미해졌다 |
| 8/24 | ⛔ **CPU 경로 폐기.** 본 학습은 전부 GPU(63분). 과거 실측 이득도 피처가 바뀌면 다시 재야 한다 |
| 8/24 | 🐛 `pkill` 부재로 대기열 프로세스가 10개 쌓여 wbcc 를 계속 재시작시켰다. 대기열 자동화 폐기 |
| 8/24 | 진행중: `bttm`(타자 트랙맨) 2023/2024 — 남은 유일한 미측정 축 |
| 8/22 | ❌ 단조 제약 기각 (mono -2 / mono2 -44). ❌ MVS x Bayesian 상관 0.9964 — 블렌드 불가 |
| 8/22 | ❌ optbest 절개: `opt254` +7.9(노이즈) / `opt254c1` **-7.5**. '839' 예측이 반대로. ❌ `seasonbase` -5.3. **용량 축은 CPU 에서도 닫혀 있다** |
| 8/22~ | **여기서부터는 큰 스윙만.** 100인 컷 1,119.20 까지 +124 = **스킬 13% 상대 개선**이다. 역대 최대 도약이 2.8%/2.9% 두 번뿐이었으므로 **그 규모를 네 번** 해야 한다. 피처 절개(+25)와 Optuna 재탐색(+30)을 다 성공해도 산술적으로 모자란다. 세 자릿수가 있는 곳은 **전이 손실**뿐 — 학습분 스킬 2.21% vs 미래 0.80%, **64%(=141점)를 잃는다.** 미검증 카드: 선형 baseline + 트리 잔차(`Pool(baseline=...)`). 트리는 외삽을 못 하고 선형은 한다. 재중심화(+18)가 그걸 상수 하나로 때운 조잡한 버전이라는 게 이 방향의 유일한 증거다 |
| 8/22~ | (참고) 예전 목록. 결함 있는 스크리닝(300회/depth6/모델1~3개)으로 기각했던 것들 재검증: 최근 가중 학습, 2019 시즌 제외 학습, `cond_*` 시즌 감쇠. 기대 +0~20. 그 외에 127점을 메울 카드는 확인된 바 없다 |
| 8/29 | ⛔ **캘린더 축 종료**: `wm`(월만) 1,111.71 **-10.18** / `wmd`(월+요일) 1,121.97 +0.08. 상호작용 +10.26 = 비가법성 4번째 |
| 8/29 | ⛔ **용량 축 5분류에서도 닫힘(실측)**: `n700` 1,120.42 -1.47, 잔차 -0.17 = 트리 700~1000 의 기여. **1400 취소** |
| 8/29 | ✅ 재중심화 손실식 `100000xΔ²/r(1-r)` 리더보드 검증 · 수축 λ\*≈0.96 5분류에서도 유효 |
| 8/29 | ⛔ **세그먼트 분해**: 새는 슬라이스 없음. F 는 행당 **+32% 초과 기여** (MSE 0.24699 < R 0.24767) |
| 8/29 | ⛔ **세그먼트별 재중심화 종료**: 오라클 +12.4 / 실행가능 최선 `count_adv` **+6.5**. `game_type` **-1,419** · `wn` **-241** |
| 8/29 | ⛔ **isotonic 축 종료**: 순효과 +1.0. 재스케일링 천장이 **+1.95** 로 확정 |
| 8/29 | 📐 컷 **1,163.87** (100번째 개인 = 팀 35위). 격차 **+41.98**, 마감 외삽 +62 |
| 8/29 | ⛔ **`v10wcw` 1,117.15 (-4.75)** — cond_* 표본 두께 노출. claude.md 의 유일한 '미해결' 항목이 닫혔다 |
| 8/29 | ⛔ **`v10wc12` 1,117.15 (-4.74)** — cond 키 12칸. `v10wcw` 와 **0.007 차이**로 같은 자리 = cond_* 는 국소 최적 |
| 8/29 | ⛔⛔⛔ **남은 격차의 124/178 이 도달 불가능** (eda49): 당해진행분 600 vs 시즌전체 724. 커리어 누적 단독은 **43** |
| 8/29 | ⛔ 이적 t=-0.36 · 커버리지 3.76% (eda47) / 구장 효과 시즌간 상관 **0.21**, 천장 2점 (eda48) |
| 8/28~8/31 | 여유 시: LightGBM 재도입, 기각했던 피처들의 30-앙상블 재검증 |
| 9/1~9/2 | **버퍼 — 신규 시도 금지.** 그 시점 최고 검증 버전 고정, 최종 제출만 |