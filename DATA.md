# 데이터 — 공식 설명과 실제가 어긋난 지점

주최측 데이터 설명서는 재배포하지 않는다. 이 문서는 **직접 파보고 알아낸 것**만 적는다.
아래 항목은 대부분 공식 설명에 없고, **점수를 만든 것은 전부 이쪽이었다.**

```
train.csv              1,475,092행 x 49컬럼   2019~2024
test.csv                     비공개 x 48컬럼   2025  (배포본엔 형식 확인용 5행)
trackman_history.csv   1,793,078행 x 30컬럼   2019~2024  ** 2025 없음 **
```

`train` 과 `test` 는 **시즌이 완전히 분리**돼 있다. 그래서 이 문제는 언제나
**미래 시즌 예측**이고, 그 한 문장이 모든 설계 판단의 근거다.

## 컬럼 구성 (49개)

| 묶음 | 개수 | |
|---|---|---|
| 식별·상황 | 21 | season · inning · top_bottom · game_type · 팀/선수 ID · 손 · 점수차 · li · 승리기대 |
| **`asof_*`** | **19** | 그 투구 **직전까지의 누적 통계**. 운영 측 공식 제공, leak-free |
| 주자 | 4 | runner_on_1b/2b/3b · base_state |
| 카운트 | 3 | balls_before · strikes_before · outs_before |
| row_id · 타겟 | 2 | control_success = 포수가 요구한 곳에 던졌는가 (0/1) |

---

## ① `asof_*` 는 시즌별이 아니라 **커리어 누적**이다

공식 설명은 "직전까지의 누적" 이라고만 한다. 실제로는 **시즌 경계에서 리셋되지 않는
통산 누적**이다 (`asof_pitcher_n` 중앙값 1,776 / 최대 15,449).

이 한 줄이 이 프로젝트에서 **가장 비싼 발견**이었다. 같은 컬럼 하나로 2024 를 예측한
스킬이 투수 부류에 따라 6배 갈린다:

```
이력 있는 투수 (80.1%)    141      2019년(리그 .5647)부터 섞인 낡은 값
신규 투수     (19.9%)    906      직전 시즌이 없어 **이미 순수한 당해 시즌 성적**
```

그 시즌 시작 시점의 누적(= train 으로 만든 투수별 룩업)을 빼면 당해 시즌 값이 남는다.
투수 5개 + 타자 2개를 그렇게 복원했고 **+92점**이다.

⚠️ 부작용도 있다 — 커리어 누적이라 시즌 드리프트를 못 따라가서 **2024 에 +0.0253
과대평가** 중이다. 가장 중요한 피처가 최근 시즌일수록 위로 편향돼 있다.

## ② `asof_*` 인접 행 차분으로 **투구 단위 라벨이 복원된다**

`asof_pitcher_n` 이 투수 내에서 **정확히 +1 씩** 증가한다 (147만 행 검증, 비율 1.000000).
`asof` 는 '직전까지' 이므로 행 i 와 i+1 의 누적 개수 차분이 **투구 i 의 라벨**이다.

```
복원 success vs 정답 control_success   일치율 1.000000  (147만 행)
```

정답 컬럼이 있는 `success` 로 검산이 되고, 나머지(`middle` 몰림 / `reverse` 반대방향 /
`ball` / `strike`)는 정답이 없다. `middle` 은 **타자측 누적 컬럼으로 독립 복원**해
교차 검증했다 — 다른 정렬 키, 다른 컬럼, 다른 엔티티인데 147만 행 일치율 1.000000.

여기서 5분류 타겟(+12.9)과 보조 타겟(+13.5)이 나왔다.

⚠️ **규정**: 차분은 **train 에서만**. `test.csv` 인접 행 차분으로 정답을 복원하는 것은
주최측이 "규칙 위반" 이라고 명시한 사안이다.

## ③ `game_type` 의 F 는 퓨처스이고, **이 데이터셋에선 100% 한 팀 경기다**

공식 설명은 "경기 유형 코드" 뿐이다. Q&A 로 R = 1군 정규시즌 / F = 퓨처스(2군) 확인.

```
F 161,004행 (10.9%)  ->  팀13 관여 비율 **1.0000**
R                    ->  팀13 관여 비율   0.1986   (10팀이면 이론값 0.2)
```

**KBO 사실이 아니라 데이터를 어떻게 모았는가의 사실이다.** 이 데이터셋의 퓨처스는
전부 한 팀의 경기다.

## ④ 2023 에 **라벨 산출 방식**이 바뀐다 (팀13 한정)

| game_type | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---|---|---|---|---|---|
| F | .6892 | .5878 | .7038 | **.7087** | **.4729** | .4593 |
| R | .5495 | .5269 | .5128 | .5037 | .5031 | .4897 |

한 시즌에 −0.236. 그리고 팀13 관여 경기(R)의 제구 성공률이 2023-05 에
**−0.0220 → +0.0469 로 부호 반전**한다.

**난이도 변화가 아니라 라벨 정의 변화다.** 실패 *안에서의* 구성비가 실패율만큼
움직이기 때문이다 (반대만 +6.5pp / 몰림만 −8.0pp). 난이도라면 구성비는 그대로여야 한다.

구장 가설은 대조군으로 배제했다 — **12번 팀과 13번 팀은 같은 잠실**인데 12번은 평평하다.
트랙맨으로 확인하니 같은 달에 릴리스 위치 3개와 속도 2개가 동시에 움직였고,
**잠실 안에서 그 팀 경기만** 움직였다. 하드웨어가 아니라 그 팀 경기 자료였다.

⚠️ 파급: 1장의 드리프트 표(전체 성공률 .5647 → .4861)는 R/F 를 섞은 값이라
**2022→2023 낙폭 −0.0290 의 거의 전부가 이 라벨 변경**이다. R 전용은 −0.0006 이다.
2025 리그평균 외삽도 R/F 를 분리해야 한다 (전체 3년 선형 0.4622 vs 분리 **0.4805**).

## ⑤ `trackman_history.csv` — 세 가지 함정

**2025 가 없다.** test 시점 투수의 최신 트랙맨은 **2024 값으로 근사**할 수밖에 없다.
그 대가를 재보면: 같은 시즌 트랙맨의 증분 R² 는 +0.047 인데 **직전 시즌 기준으로는
+0.011** 이다. **77%가 staleness 로 사라진다.**

**1군 전용이 아니다.** 2024 기준 38%가 2군(퓨처스, `MIN_*` 팀코드)이고 올스타(`KBO_*`)
기타(`ACE_*`)도 섞여 있다. 906명 중 430명이 1군·2군 혼재다.

**주최측 제공 매핑이 약 91% 틀려 있었다.** `pitcher_id_mapping.csv` 는 965행뿐이고
`distance` 컬럼이 말해주듯 **구종비율 하나로만** 매칭돼 있다. 순환하지 않는 지표로 재면:

| | 시즌 간 일관성 | 2024 커버리지 |
|---|---|---|
| 기존 매핑 | **1.9%** | 28.2% |
| 재구축(팀→투수 2단계) | **90.9%** | **94.0%** |

⚠️ 재구축에서 배운 것 — **2군을 걸러내면 오히려 나빠진다** (결측 9.7% → 14.1%).
정확도보다 **커버리지**가 중요하다.

⚠️ `game_date` 는 **포맷이 두 가지**다. 2019~21 은 `%m/%d/%Y`, 2022~24 는 `%Y-%m-%d`.
하나로 파싱하면 **test 에 가장 가까운 세 시즌이 전부 NaT** 가 된다.

## ⑥ 경기 단위로 완전히 복원된다

`(season, month, dow, game_type)` 변화 + 이닝 하락으로 자르면:

```
R:  720 720 721 720 720 721     <- KBO 정규시즌 = 10팀 x 144 / 2 = 720. 정확히 맞는다
F:   90  76  86 104  89 101
총 4,868경기 | 등판 45,862개 | 경기당 투구 중앙값 300
```

검증: 복원 등판에서 `asof_pitcher_prev1_game_success_rate` 유일 비율 1.000000.

⚠️ 다만 **경기내 피로도는 거의 없다** — 선발 등판 내 1~15구 +0.0055 → 90구+ −0.0023,
타순 반복도 평평하다. 복원해도 벌 게 없었다.

## ⑦ 그 외 확인한 것들

- **`li` / `home_win_expectancy` 는 상황의 순수 함수다.** 4,040개 상황 칸 안에서
  고유값이 정확히 1개, 잔차 0. 팀 전력도 시즌 득점환경도 안 들어 있다 → 트리가 만든다
- **`asof_pitcher_pitchmix_n` 은 `asof_pitcher_n` 과 모든 행에서 동일**(최대차 0)
- **`prev{1,3,5}_game_*` 은 시즌 경계를 넘는다.** 이력 있는 투수의 시즌 첫 등판에서
  `prev1` 결측이 0.0% — 작년 마지막 등판을 가리킨다
- **`count_advantage` 의 `'None'` 은 0-0 과 3-2 를 같이 담는다.** 야구에서 가장 다른
  두 카운트다. 그리고 `pd.read_csv` 기본값이 이 문자열을 NaN 으로 읽어 merge 를
  전량 실패시킨다 → `keep_default_na=False` 필수
- **`row_id`**: train 은 시간순 완벽 정렬(`TRAIN_0000001`~`TRAIN_1475092`)이지만
  test 는 **별도 시퀀스이고 시간순이 아니다**. 피처로 쓰면 test 를 "2019년 시즌 초" 로 읽는다

## 규정 요약

행 A 의 예측에 쓸 수 있는 것은 넷뿐이다 — ① 행 A 의 입력 변수 ② 그것만으로 만든
파생변수 ③ 공식 학습 데이터 ④ 학습 데이터로만 만든 통계·모델.

주최측이 제시한 판정 시험: **어떤 행의 예측값은 test 에 그 행 1개만 있을 때와
전체가 함께 있을 때 동일해야 한다.** `tools/audit_independence.py` 가 이걸 자동화한다
(최종 제출본 실측 2.22e-16).
